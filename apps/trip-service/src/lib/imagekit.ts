/** Client ImageKit — déplacé dans `@packages/libs/imagekit` (C-PR8b) ; ce fichier reste l'import historique du trip-service. */
import imagekit from "@packages/libs/imagekit";
export default imagekit;
