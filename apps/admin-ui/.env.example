# admin-ui (chantier C) — même origine (D48) : Next proxifie /api/* vers le gateway,
# les cookies admin_* sont first-party sur localhost:3001.
API_PROXY_TARGET=http://localhost:8080
NEXT_PUBLIC_API_BASE_URL=/api
# Sentry navigateur (vide = inactif)
NEXT_PUBLIC_SENTRY_DSN=
