// `expo-env.d.ts` (généré par `expo start`, ignoré par git — voulu par le
// template) porte cette même référence : sans elle, un poste ou une CI qui
// n'a jamais lancé Expo perd les déclarations `*.css` d'expo/types (TS2882).
// Cette copie SUIVIE rend le typecheck déterministe partout.
/// <reference types="expo/types" />
