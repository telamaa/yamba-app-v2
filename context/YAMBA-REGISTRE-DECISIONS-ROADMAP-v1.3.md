# 📕 YAMBA — Registre des décisions & Roadmap maîtresse

> **Version** 1.3 · 21 juillet 2026 — *révisions : jalons reformulés (fin du vocable « MVP ») · registre des arbitrages A1–A24 consolidé (§2bis) · leçons capitalisées fusionnées (§6) · corrections factuelles (§0bis : chemins, CI 11 checks, ports, numéros de PR) · vélocité recalibrée post-B1 (§4) · backlog maître avec statuts ✅/🔄/🔲 (§7)*
> **Statut** : document de référence — toute divergence d'implémentation doit être arbitrée contre ce document, puis répercutée ici.
> **Document jumeau** : `YAMBA-REGLES-METIER-V2.md` (v1.2 — les règles métier issues de ces décisions).
> **Documents liés** : `SPECIFICATIONS-WORKFLOW-BOOKING-YAMBA.md` (workflow deal) · `DOC-METIER-TRIP-LIFECYCLE.md` (RG-01→RG-27) · `DOC-DEV-TRIP-LIFECYCLE.md` · handoff courant `YAMBA-CONTEXT-HANDOFF-B1-PR4.md` · guides `docs/deal-service/PR{3,4}-EXPLICATION-{METIER,TECHNIQUE}.md`.

---

# 0bis. Erratum & faits prouvés (v1.3 — prime sur tout document antérieur)

| Fait | Réalité PROUVÉE | Corrige |
|---|---|---|
| Schéma Prisma | `prisma/schema.prisma` (RACINE du repo, 913 l) | Toute mention de `packages/libs/prisma/schema.prisma` |
| Seeds | `packages/libs/prisma/scripts/` (seed-deals.ts, seed-outbox.ts) — import Prisma en RELATIF (`../index`), exécution `npx tsx` (ne lit pas les alias tsconfig → contrats en relatif aussi) | Handoffs antérieurs |
| Import Prisma (services) | `import prisma from "@packages/libs/prisma"` | — |
| Config Nx | **AUCUN `project.json` dans aucun service** — cibles inférées par plugins ; `npx nx` toujours, jamais global | Handoff PR3 (« joindre la config Nx ») |
| CI | **11 checks** ; required sur `dev` : 9 historiques + TypeScript (deal-service) + Tests unitaires (deal-service) — required PARTIELLEMENT confirmé (Tests ✓ vu Required le 21/07 ; TypeScript deal à confirmer dans Settings → Branches) | — |
| Ports (A18) | 6001 auth · 6002 trip · 6003 deal · **6004 notification (PR4bis)** · 6005 chatting · 6006 admin · 6007 recommendation · 6008 payment (B2) · 6009 media (B2) · 6010 search · 6011 analytics · 8080 gateway · 9092 Redpanda | — |
| Guides PR | `docs/deal-service/PR{3,4}-EXPLICATION-{METIER,TECHNIQUE}.md` | Handoff PR4 §2 (disait `docs/`) |
| Tests deal-service | **218 verts** = 188 machine + 14 mapper + **16 relay** (pas 15) | Notes de session intermédiaires |
| PRs B1 | PR1 = **#70** · PR2 = **#71** · PR3 = **#72\*** (à confirmer) · PR4 = **#73** (code, 5 commits) **+ #74** (guides docs, checks comptés 11/11) — leçon §6.1 : noter le numéro AU MERGE | Handoff PR4 §1 |
| kafkajs | `kafkajs@^2.2.4` épinglée ; env `KAFKA_BROKERS` (déf. `localhost:9092`), `OUTBOX_RELAY_ENABLED` (déf. `true`) | — |

---

# 1. Cadre de décision

Toutes les décisions de ce registre sont classées selon le principe des **portes** :

- **Porte à sens unique** 🚪→ : chère ou impossible à changer plus tard (schéma de données, contrats d'API, contrats d'événements, modèle de sécurité, sémantique métier). **Prise immédiatement au niveau d'exigence maximal.**
- **Porte à double sens** 🚪↔ : substituable sans toucher au métier (broker, provider, lib, hébergement). **Prise au plus simple qui respecte le contrat, remplacée quand la charge le justifie.**

Objectif produit assumé : **solide, propre, pro, secure, long terme** — les fondations irréversibles sont impeccables dès le jour 1 ; l'infrastructure grandit avec la charge.

**Principe des jalons (nouveau en v1.3).** La roadmap est découpée en trois jalons (§3.0). Leur ordre exprime des **dépendances techniques** (on ne peut pas opérer une boucle qui n'existe pas ; on ne peut pas étendre une plateforme qu'on ne sait pas opérer) — **jamais une hiérarchie de valeur**. Tout ce qui figure aux jalons 2 et 3 appartient à la vision produit au même titre que le jalon 1 ; rien n'y est « optionnel » ou « post-produit ». Le vocable « MVP » est retiré du référentiel : il laissait croire que le jalon 1 était un produit livrable seul, alors que le lancement public exige les jalons 1 **et** 2.
---

# 2. Registre des décisions

## 2.1 Architecture & plateforme

| ID | Décision | Pourquoi | Compromis assumé | Porte |
|---|---|---|---|---|
| **D1** | Le module Booking vit dans un **`deal-service` dédié (port 6003)** | Frontière de domaine claire ; chaque futur domaine (notifications, médiation) aura sa frontière ; cohérent avec la vision événementielle | Plus de plomberie (gateway, auth middleware, déploiement) qu'un module dans trip-service | 🚪→ |
| **D2** | **Architecture événementielle en protocole Kafka dès B1** : événements de domaine versionnés (miroir de la state machine), interface `EventBus`, **transactional outbox** (événement écrit dans MongoDB dans la même opération que la transition, puis relayé), transport `kafkajs`, **Redpanda** en dev (compatible Kafka 100 %, un binaire, sans ZooKeeper/JVM), broker managé compatible Kafka en prod le moment venu | Le découplage producteur/consommateur permet d'ajouter notifications, analytics, audit sans toucher au métier ; le journal persistant = **audit trail des deals** (précieux pour la médiation) ; l'outbox est le pattern impossible à retrofitter | Un conteneur de plus en dev ; discipline de versionnement des événements. Note : Upstash a décommissionné son offre Kafka — prod = Confluent Cloud ou Redpanda self-host | 🚪→ (contrats + outbox) / 🚪↔ (hébergement broker) |
| **D3** | **OpenAPI 3.1 généré depuis les schémas Zod** (`zod-openapi`) : le même objet Zod valide les requêtes à l'exécution ET génère la spec. Abandon de swagger-autogen. Clients mobiles **générés** depuis la spec (orval / openapi-generator) | La doc ne peut plus dériver du code (source de vérité unique) ; OAS 3.1 = meilleur support des générateurs TS/Kotlin/Swift ; prérequis industriel de la version mobile | Migration du doc auth curaté (Swagger 2.0) ; discipline : tout endpoint naît avec son schéma Zod | 🚪→ (contrat) |
| **D6** | **RBAC dans le schéma User dès B1** : champ `role` (`USER` / `ADMIN`, extensible `MEDIATOR`) | L'admin dashboard (chantier C) l'exige ; deux lignes de schéma maintenant, migration coûteuse après | Néant | 🚪→ |
| **D8** | **Template de service** : chaque service naît avec logs structurés **pino** + **correlation ID** propagé depuis le gateway, CORS, error middleware, auth, OpenAPI-from-Zod | Suivre une requête de bout en bout à travers les microservices ; impossible à retrofitter sur des logs passés | Néant | 🚪→ |
| **D10** | **CI GitHub Actions obligatoire** : chaque PR exécute `tsc --noEmit` (tous projets), JSON parse + diff structurel FR/EN des messages, grep anti-fuite. **Une PR rouge ne se merge pas** | Le ritual manuel saute le jour de fatigue ; la machine ne fatigue pas ; protection maximale pour un solo founder | Une demi-journée de setup | 🚪↔ (mais prioritaire) |

## 2.2 Paiements & argent

| ID | Décision | Pourquoi | Compromis assumé | Porte |
|---|---|---|---|---|
| **D11** | **Interface `PaymentProvider` abstraite dès B2** — implémentation n°1 Stripe | Stripe ne verse pas au Congo ; l'infrastructure de paiement du marché cible est **Mobile Money** (MTN MoMo / Airtel Money) ; l'abstraction maintenant rend l'ajout indolore | Une couche d'indirection | 🚪→ |
| **D16** | **Commission unique en % prélevée côté Expéditeur**, frais Stripe **absorbés** dedans, **plancher** de commission, affichage en 2 lignes max (« Transport X € » + « Service & protection Y € »). Le Voyageur voit son **net**, point | Aucune marketplace pro n'expose les coûts de son prestataire (invite à la contestation) ; simplicité de lecture = confiance | Marge légèrement variable selon les frais Stripe réels | 🚪→ (affiché = promis) — **✅ acté au mockup : 12 %, plancher 3,00 €** (paramètres serveur, révisables) |
| **D17** | **Snapshot de prix immuable dans le Booking** : photographie complète du breakdown (base, modificateurs, commission, **prime de protection séparée**, net) au moment de la réservation. Jamais de prix recalculé depuis le Trip | Si le Voyageur change son €/kg, les deals existants ne bougent pas ; contestations impossibles ; comptabilité auditable — et la prime d'assurance (D22) doit être un flux distinct dès le jour 1 | Duplication contrôlée de données | 🚪→ |
| **D18** | **Tous les montants en centimes entiers + champ `currency`** partout, même en mono-devise EUR | Jamais de float pour l'argent ; le jour du XAF/Mobile Money, le schéma est prêt | Néant | 🚪→ |
| **D25** | **Devise de transaction ≠ devise d'affichage** : transaction 100 % EUR en v1 (un seul monde comptable), affichage localisé via `Intl.NumberFormat(locale)` | La vraie multi-devise (conversion, taux) n'arrive qu'avec Mobile Money ; le formatage localisé est gratuit | Les utilisateurs hors zone euro voient des EUR | 🚪↔ |

## 2.3 Pricing & catégories

| ID | Décision | Pourquoi | Compromis assumé | Porte |
|---|---|---|---|---|
| **D13** | **Le prix = €/kg × poids** : le Voyageur fixe UN nombre (son €/kg) + sa capacité en kg. **Classes de taille visuelles** S/M/L (boîte à chaussures / sac cabine / demi-valise) avec léger modificateur — jamais de dimensions L×l×H demandées. **Bagages entiers (23 kg soute / 12 kg cabine) = produits à part, prix forfaitaire** | La catégorie ne reflète pas le coût du transport (poids + volume + risque) ; le marché informel « GP » du corridor price au kilo (8-15 €/kg) = standard mental des utilisateurs ; personne ne mesure un colis (adaptation UX du poids volumétrique DHL sans la règle) | Refonte des étapes « Catégories & tarifs » (create-trip) et « Colis » (booking wizard) | 🚪→ (schéma Booking) — **✅ acté au mockup** : coefs S/M/L ×1,0 / ×1,1 / ×1,25 ; forfait bagage suggéré = médiane_corridor × franchise × 0,9 (remise de gros) |
| **D14** | **La catégorie garde ses vrais métiers : conformité, risque, protection** — plus jamais le prix. **✅ Acté : 8 familles de risque** (liste finale et mapping en CAT-02 v1.2), dont **Alimentaire sec & scellé** — le volume réel du corridor GP — strictement encadrée par les règles CNF. Surcharges/exclusions optionnelles par famille (« électronique : +20 % ou refusé ») | Deux taxonomies incompatibles cohabitaient (types d'objets + formats de contenants) ; la catégorie répond à « qu'est-ce que c'est ? », pas « combien ça coûte ? » | Migration de l'enum fraîchement aligné (mapping ancien→nouveau, JSON i18n, front) — PR mécanique rendue facile par les fondations propres | 🚪→ |
| **D15** | **Moteur de suggestion de prix — deux temps.** V1 **déterministe** : `prixSuggéré(€/kg) = base_corridor × modificateurs` (vol direct/escales, proximité départ, saison, réputation, **demande latente = SavedRoutes actives sur le corridor**). Affichage en fourchette basse-médiane-haute + ancre « les trajets similaires partent à Y €/kg » ; badge « prix juste » côté Expéditeur. **Ajouts actés (v1.2)** : les tarifs express (DHL/La Poste) ne sont PAS une entrée de l'algo (le marché de référence est le GP) mais servent ① d'**ancre marketing** côté Expéditeur (« Total 28 € — vs ~85 € chez DHL ») et ② de **plafond de sécurité** de la suggestion. V2 **apprenante** post-launch (taux d'acceptation/expiration par niveau de prix, données collectées par Kafka + PostHog) | Livrable sans données historiques ; la table base_corridor est seedée depuis l'étude du marché GP ; le signal SavedRoutes est un avantage propriétaire | La V1 sera imparfaite — c'est son rôle | 🚪↔ (l'algo) / 🚪→ (l'existence de la suggestion dans l'UX) |

## 2.4 Modèle du deal

| ID | Décision | Pourquoi | Compromis assumé | Porte |
|---|---|---|---|---|
| **D19** | **La capacité (kg) est réservée dès PENDING** et **libérée** sur DECLINED / EXPIRED / CANCELLED — transaction MongoDB pour la concurrence | Sinon le Voyageur accepte un deal devenu physiquement impossible (3 demandes de 5 kg sur 8 kg restants) ; chaque transition de la state machine a désormais un effet capacité | Des kg « gelés » pendant la fenêtre 24 h d'une demande qui sera peut-être refusée | 🚪→ |
| **D20** | **v1 : tout deal passe par PENDING** — `instantBooking` ne court-circuite pas l'acceptation. Le flag devient un badge « réponse rapide » ou est retiré du wizard | L'acceptation EST le mécanisme de confiance ET d'inspection du colis (conformité D9) ; court-circuiter PENDING changerait états, paiement, notifications | Pas de réservation instantanée v1 | 🚪→ — **✅ acté** : badge informatif « Réponse sous 24 h », toute demande passe par l'acceptation |
| **D21** | **Matrice d'annulation/remboursement** (détail dans RG métier) : PENDING → 100 % ; ACCEPTED jusqu'à J-2 → 100 % ; ACCEPTED < 48 h du départ → retenue partielle ; après PICKED_UP → litige uniquement. Annulation par le **Voyageur** après acceptation → remboursement intégral + impact réputation | La spec disait « politique à préciser » — chaque case = un flux d'argent en B2/B4, il faut la figer avant | Les seuils (J-2, % de retenue) sont des curseurs ajustables | 🚪→ (l'existence des cases) / 🚪↔ (les seuils) |
| **D22** *(révisée v1.1)* | **Protection du colis — stratégie à deux étages.** **Cible : une véritable assurance en partenariat avec un assureur**, via le modèle *embedded insurance* (acteurs type Wakam, Owen, Qover, bsurance : ils portent l'agrément et fournissent le produit en API — Yamba distribue, ne devient pas assureur). **Transitoire au lancement : « Garantie Yamba »** (engagement commercial de remboursement plafonné, légal immédiatement) si le contrat n'est pas signé à temps. **Le schéma naît prêt pour la bascule** : champ `protectionPlan` avec `provider` (`YAMBA_GUARANTEE` → demain l'assureur), et la **prime est un flux comptable séparé de la commission dès le jour 1** (cf. D17). L'UI n'emploie le mot « assurance » qu'après signature du contrat | Une vraie assurance portée par un assureur reconnu est un actif de confiance supérieur — et la confiance est LE sujet du marché. Le montage (négociation produit, exclusions, sinistres, obligations de distribution DIC/IPID) prend des semaines à mois hors de notre contrôle : il ne doit bloquer ni le mockup ni B1. Bonus : le questionnaire de souscription de l'assureur muscle mécaniquement la conformité D9 | Double vocabulaire transitoire ; démarches partenaire à mener en parallèle (flux « Telama seul », §3.2) | 🚪→ (séparation prime/commission dans le schéma, `protectionPlan.provider`) / 🚪↔ (le partenaire choisi) |
| **D23** | **Un booking = un colis** en v1. Deux colis = deux réservations | Simplifie code de livraison, litige, notation ; extensible plus tard sans casse si le schéma nomme bien les choses | Friction pour l'Expéditeur multi-colis | 🚪↔ |
| **D24** | **Fuseaux horaires** : chaque horaire est **local à son aéroport** (convention aérienne). Schéma : + `originTimezone` / `destinationTimezone` (IANA, dérivés des lat/lng via Google Time Zone API) + `departureAtUtc` / `arrivalAtUtc` calculés. **Tout ce qui compare au temps réel (crons, countdowns, isTripPastDeparture) utilise l'UTC** ; tout affichage utilise le local + mention du fuseau | Sans ça, le cron complete-trips ferme un trajet Paris→New York avec 6 h d'erreur ; les countdowns 24 h / J+4 seraient faux | Deux champs de plus, un appel API à la création | 🚪→ |

## 2.5 Sécurité, conformité, confiance

| ID | Décision | Pourquoi | Compromis assumé | Porte |
|---|---|---|---|---|
| **D4** | **Sécurité by design (B1)** : toutes les limites revalidées **côté serveur** (tentatives, régénérations, fenêtres — le front n'est qu'indicatif) ; code bcrypt jamais dans les payloads carrier ; autorisations **par rôle et par champ** dans les DTOs ; idempotence des transitions ; horodatage serveur | De l'argent séquestré + un code de livraison = surface d'attaque réelle | Néant — c'est le métier | 🚪→ |
| **D7** | **Admin de niveau pro dès le chantier C** : app séparée (`apps/admin-ui`), **2FA TOTP obligatoire**, sessions courtes, **audit log de chaque action admin** (consommateur Kafka) | La surface la plus sensible du système (qui tranche les litiges, qui voit tout) ; « qui a fait quoi » est exigible en cas de contestation | Friction de login pour l'admin | 🚪→ |
| **D9** | **Politique de conformité colis illicite** — le risque existentiel du métier : inspection au pickup érigée en rituel (« j'ai vu le contenu ouvert, il correspond » ; colis non scellé jusqu'au pickup, scellé devant le Voyageur) ; **liste des interdits en dur dans le wizard** (stupéfiants, batteries lithium seules, liquides, espèces, médicaments — règles IATA) + attestation Expéditeur horodatée ; **Stripe Identity pour l'Expéditeur** (pas seulement le KYC Connect du Voyageur) ; **plafonds** (valeur déclarée, poids, envois/mois compte neuf). Document dédié : `POLITIQUE-CONFORMITE-YAMBA.md` (à rédiger, tâche de fond) | Un colis de stupéfiants transporté par un Voyageur de bonne foi = prison pour lui, une du journal pour Yamba. Aucun tuto e-commerce ne couvre ce risque ; il est propre au métier. L'assureur (D22) exigera ces contrôles | Friction à l'inscription Expéditeur (Identity) ; à doser | 🚪→ |
| **D12** | **RGPD by design (schéma B1)** : rétention définie par type de donnée, champ de consentement, procédure d'effacement. Attention particulière : le téléphone du **destinataire** (personne qui n'a jamais consenti), les photos, les CNI via Stripe Identity | Rattraper le RGPD après coup = refonte de schéma ; le destinataire est un tiers, cas juridiquement sensible | Un peu de modélisation en plus | 🚪→ |
| **D26** | **Modèle `Report` générique dès B1** : `targetType` (TRIP / USER / futur MESSAGE), `reason` (enum), description, reporter, statut de traitement. Front : bouton discret + modal sur page trajet publique et profil membre. File de traitement dans l'admin | Brique modération manquante ; alimente l'admin (C) et la conformité (D9) ; un seul endpoint pour tous les signalements | Néant | 🚪→ (modèle) |
| **D27** | **Politique de session** : (a) timeout d'**inactivité** côté serveur — `lastActivityAt` en Redis, `/auth/refresh` refuse au-delà du seuil (configurable par env, cible 30-60 min) ; (b) **durée de vie absolue** de session (30 jours max) ; (c) **ré-authentification pour les actions sensibles** (« sudo mode » : IBAN/Stripe, email, mot de passe). Front : déconnexion propre + modal d'avertissement avant expiration | Bug actuel : rotation du refresh token = session infinie. Le curseur inactivité est un arbitrage sécurité/conversion réglable | Reconnexions plus fréquentes | 🚪→ (le mécanisme) / 🚪↔ (les seuils) |

## 2.6 Produit & expérience

| ID | Décision | Pourquoi | Compromis assumé | Porte |
|---|---|---|---|---|
| **D5** | **Analytics = PostHog** (self-hostable, souveraineté des données) + **`viewsCount` réel via Redis `SETNX`** (dédup user/IP + jour) dans GET /trips/:id public | Kafka n'est pas l'outil de l'analytics produit ; PostHog donne funnels/conversion sans backend ; viewsCount = micro-PR d'une demi-journée | Un outil de plus | 🚪↔ |
| **D28** | **Wording des statuts trajet** : `PUBLISHED` → **« En ligne »** (EN « Online »), `PAUSED` → **« Masqué »** (EN « Hidden »). Les enums DB ne bougent pas | Les statuts doivent décrire l'effet pour l'utilisateur (« visible des Expéditeurs »), pas la mécanique. Grâce au chantier i18n : **éditer 2 JSON, zéro code** — premier dividende concret de la migration | Néant | 🚪↔ |
| **D29** | **Réputation & score — deux objets séparés.** ① **Réputation visible** : faits explicables que l'utilisateur contrôle (note B5, deals complétés, taux d'annulation post-acceptation, délai de réponse, ancienneté, identité vérifiée), présentés en **badges + statistiques** — jamais de note globale opaque. Niveaux publics : Nouveau / Confirmé / Top Yamber (critères affichés) ; miroir Expéditeur (« Expéditeur fiable »). ② **TrustScore interne** (invisible) : litiges perdus, écarts de poids répétés (PRC-07 = signal fraude), annulations tardives, vélocité anormale, signalements — usages : plafonds progressifs CNF-06, priorisation de la file de revue, aide à la décision admin. **Garde-fous** : humain dans la boucle pour toute sanction (SIG-03 + RGPD décisions automatisées), traçabilité par événements Kafka. **Signaux exclus des deux objets** : fréquence de connexion, volume brut de trajets (présence ≠ fiabilité ; ne récompenser que des issues coûteuses à falsifier) | Le score opaque à la Uber génère anxiété et contestation ; le signal comportemental doit servir le contrôle du risque, pas l'engagement artificiel. Matière première gratuite via D2 dès B1 | Deux systèmes à maintenir au lieu d'un | 🚪→ (la séparation) / 🚪↔ (les pondérations) |
| **D30** | **Stratégie de tests proportionnée, dès B1.** ① **Unitaires sur la logique pure** (Jest via @nx/jest, déjà configuré) : state machines (la table §2.2 = le plan de test), moteur de pricing (PRC), matrice d'annulation (ANN-01), compteurs/fenêtres. ② **Intégration sur les endpoints critiques** (supertest + mongodb-memory-server ; Stripe remplacé par un fake via l'interface PaymentProvider — dividende D11) : accept/decline, deliver (bcrypt + verrou), vues par rôle, concurrence capacité (CAP-01). ③ **Exclusions assumées v1** : tests de composants React (couverts par tsc + i18n CI), E2E Playwright (jalon pré-launch uniquement). ④ **Definition of Done** : toute PR touchant state machine / argent / règles ANN-CAP-PRC livre ses tests dans la même PR. ⑤ Job `tests` ajouté à la CI (checks requis). Rétrofit minimal : PR `test/trip-state-machine` (logique pure existante, ~1 session courte) | De l'argent séquestré sans tests = roulette ; les tests naissent avec le code (coût minimal) au lieu d'être rétrofités (coût maximal) | +10-15 % sur les chantiers B (absorbé dans les fourchettes réalistes) | 🚪↔ (l'outillage) / 🚪→ (la DoD) |
| **D31** | **Le gate Stripe/profil migre de la PUBLICATION vers l'ACCEPTATION.** Le KYC (~5 min, incompressible légalement) se demande au moment où l'argent est réel (« 66 € t'attendent, finalise pour accepter »), jamais avant la première preuve de valeur, jamais après la capture (on ne capture pas vers qui on ne sait pas payer). Pattern Airbnb/BlaBlaCar ; RIB + virements manuels = exercice illégal de services de paiement (DSP2/ACPR) → Stripe Connect Express confirmé (D11), `PaymentProvider` abstrait en B2 ; seule variable : le MOMENT. Compromis : trajets publiés par des Yambers non finalisés (mitigé : expiration 24 h + OnboardingBanner + cron relance + PostHog jalon 2). Implémentation : micro-PR dédiée (déplacer les 2 checks du controller) + l'accept B2 — HORS refonte pricing | Conversion supply : la démonstration involontaire de PR-A (une offre tarifaire complète refusée à la publication pour cause de KYC) est la munition | Fenêtre de trajets « non finalisés » à surveiller | 🚪→ |
| **D32** | **Plancher de facturation par colis (PRC-06).** Le coût réel du Voyageur pour un colis léger n'est pas le poids mais le **temps** (rendez-vous de remise, remise à l'arrivée, responsabilité). Deux paramètres serveur (§13) : **poids facturable minimum = 0,5 kg** (standard courrier/express) et **prix minimum par colis = 8 €** ; le plus élevé s'applique : `transport = max(max(poidsDéclaré, 0,5) × €/kg, plancher)`. Ex. à 12 €/kg : passeport 0,1 kg → 0,5 × 12 = 6 € → **8 €** ; livre 1,2 kg → 14,40 €. Affiché honnêtement : Expéditeur « Colis léger : minimum 8 € », Voyageur « Aucun colis ne te rapporte moins de 8 € ». Le plancher est du **transport** (net Voyageur) — la commission D16 s'applique dessus, pas à la place. Implémentation : **PR-C** (calcul côté Expéditeur) + snapshot de prix deal-service (D17) ; valeurs ajustables sans redéploiement | Sans plancher, une enveloppe à 12 €/kg vaut 1,20 € : aucun Yamber ne se déplace, l'offre se vide sur les petits objets — pourtant les plus rentables au kilo (arbitrage S/M/L sans XS, A28) | Un colis très léger paie « plus que son poids » : assumé, expliqué à l'écran | 🚪→ (existence du plancher) / 🚪↔ (les valeurs) |
| **D33** | **Comparabilité des offres en recherche (colis de référence 2 kg).** Deux moteurs coexistent (A28) et sont incomparables tels quels (15 € le colis vs 12 €/kg). On dénormalise sur le Trip `comparablePriceCents` = **coût de transport d'un colis de référence de 2 kg** : PER_KG → `max(2 × pricePerKgCents, plancher D32 800)` ; legacy → `minPriceCents`. Le tri « Prix le plus bas » trie sur ce champ (libellé « pour un colis de 2 kg ») et n'exclut plus les trajets au kilo. Le **filtre catégorie legacy est remplacé par un filtre FAMILLE** (D14) : un trajet est exclu s'il **refuse** la famille demandée ; les trajets legacy (sans familles) passent. Les facettes exposent le compte par famille ; les filtres à compte 0 sont masqués. Recalcul à chaque écriture (create/update/publish) via un helper pur ; backfill unique des trajets existants. **V2 (même PR)** : si l'Expéditeur saisit le poids de son colis, le prix affiché, le tri et l'exclusion par capacité se font **pour ce poids** (tri en mémoire, fenêtre bornée) — la référence 2 kg n'est que le défaut | Sans ça, la recherche cache les trajets au kilo dès qu'on filtre ou trie par prix — le nouveau moteur serait invisible | Le « 2 kg » est un choix d'ancre (mockup : colis 2,5 kg S) — paramètre serveur §13 candidat | 🚪↔ (le poids de référence) / 🚪→ (l'existence d'un prix comparable) |
| **D34** | **Un seul moteur de prix Expéditeur, pur, partagé front/serveur : `@packages/pricing`.** `quoteShipperPrice({ product, pricePerKgCents, weightKg, sizeClass, familySurchargePct, protection })` → devis en cents : `billable = max(poids, 0,5)` · `transport = max(€/kg × billable × coef S/M/L × (1 + supplément), 8 €)` · `commission = max(12 %, 3 €)` · `prime = 6 €` si Garantie 500 · **service = commission + prime** (COM-03 : 2 lignes) · **net Voyageur = transport**. Bagage entier = forfait, consomme sa franchise. Chaque champ du devis est conçu pour être **figé tel quel** dans `BookingPricingSnapshot` (D17) — le contrat s'enrichit de `billableWeightKg`, `sizeCoef`, `familySurchargePct`, `minimumApplied`, `rawTransportCents`, `serviceCents`. Les paramètres §13 vivent dans `PRICING_PARAMS` (une source ; l'endpoint `GET /pricing/params` les servira sans changer les formules) ; le seed passe à 12 %/3 € (il était à 15 %/2 €, obsolète). Le front utilise le même code pour le devis en direct : **ce que l'Expéditeur voit = ce que le serveur figera** | Deux implémentations (front devis / serveur snapshot) divergent toujours un jour — et une divergence de prix est un litige | Le front embarque une lib TS partagée (alias `@packages/pricing`, résolue par Next et Jest via tsconfig paths) | 🚪→ (l'unicité du moteur) / 🚪↔ (les paramètres) |
| **D36** *(gravée le 29/08/2026)* | **Jalons 4 et 5 : applications Android et iOS sur UNE base React Native + Expo (TypeScript).** Jalon 4 = socle mobile + Android (Google Play) ; Jalon 5 = iOS (App Store). Réutilisation obligatoire : `@packages/pricing` (D34 : même devis que le web et le serveur), `@packages/api-contracts` + client généré depuis l'OpenAPI (D3), messages i18n JSON. Auth par tokens (refresh) — pas de cookies ; push via notification-service ; paiement Stripe Payment Sheet (Apple Pay / Google Pay natifs) ; caméra pour les photos de pickup. Pas de deuxième moteur, pas de deuxième contrat : le mobile est un client de plus des mêmes API et des mêmes règles. **Exigences « natif, qualité, performance » (constitutives de D36)** : ① React Native **nouvelle architecture** (JSI + Fabric + TurboModules, moteur Hermes) — pas de bridge legacy ; ② navigation et écrans **natifs** (`react-native-screens`, Expo Router), animations et gestes sur le thread UI (Reanimated 3, Gesture Handler), listes virtualisées (FlashList) ; ③ **UI idiomatique par OS** : composants, typographie et gestes iOS sur iOS (SF Symbols, feuilles modales, retour par balayage) et Material 3 sur Android — pas d'UI « clone » identique sur les deux ; ④ **modules natifs** écrits (Swift/Kotlin via Expo Modules) dès qu'une capacité l'exige, jamais de WebView pour un écran produit ; ⑤ **budgets de performance mesurés en CI** (Maestro + Flashlight/Perfetto) : démarrage à froid < 1,5 s sur un milieu de gamme Android, 60 fps sur les listes recherche/deals, aucune frame > 100 ms sur les 2 parcours critiques, poids initial < 40 Mo ; ⑥ `expo-dev-client` + EAS Build (pas Expo Go en prod), OTA réservé au JS non structurel. **Alternative écartée : Flutter** (Dart) — excellent framework, mais il imposerait un second langage et la **réécriture** du moteur de prix, des contrats Zod et de l'i18n (ou un pont de génération à maintenir) : la règle « un prix, une règle, trois clients » ne tiendrait plus. Vrai natif Swift/Kotlin écarté (×2–2,5 sur les jalons 4–5, dérive entre clients) | Deux bases natives = deux fois les bugs et les règles ; Expo donne les deux OS, l'OTA et EAS Build avec une équipe d'une personne ; le code métier est déjà en TypeScript partagé | Performances/animations natives limitées sur quelques écrans (acceptable pour un marketplace) ; dépendance à Expo | 🚪↔ (Expo vs bare RN) / 🚪→ (une seule base, mêmes API) |
| **D37** *(gravée le 29/08/2026)* | **Naissance du deal en deux appels : autoriser, puis créer.** ① `POST /deals/payment-intents` : le serveur recalcule le devis avec le moteur unique (D34), compare au total VU par l'Expéditeur (`expectedTotalCents`, sinon 409 `QUOTE_DIVERGENCE`) et pose une **autorisation** (empreinte, capture manuelle) chez le `PaymentProvider` (D11) ; rien n'est écrit en base. ② L'Expéditeur confirme dans le Payment Element. ③ `POST /deals` : le serveur re-vérifie tout (trajet réservable, devis identique, autorisation `AUTHORIZED` au bon montant / trajet / expéditeur, intent jamais réutilisé) puis, dans **UNE transaction Mongo** : `trip.updateMany` conditionnel sur `reservedKg` (CAP-01 atomique), `booking.create` PENDING avec 5 snapshots figés (D17), 2 événements outbox (`booking.requested`, `booking.payment_authorized`) validés au contrat avant écriture. Capacité perdue entre ① et ③ → transaction annulée + autorisation libérée. Le Booking naît PENDING = « paiement autorisé », exactement la ligne 1 de la table §2.2 — aucun statut intermédiaire | Un deal sans argent bloqué n'engage personne ; une empreinte sans deal n'engage rien (elle expire seule). Le serveur ne fait confiance à aucun montant venu du front | Deux allers-retours ; un intent orphelin par abandon (sans effet : jamais capturé) | 🚪→ (séquence) / 🚪↔ (fournisseur) |
| **D38** *(gravée le 29/08/2026)* | **Pas de `payment-service` :6008 en B2 : le `PaymentProvider` (D11) est une lib `@packages/payments`** (interface + `StripePaymentProvider` capture manuelle + `FakePaymentProvider` mémoire, factory par environnement — le Fake est **refusé en production**). Consommée par deal-service ; l'extraction en service reste possible (la frontière est l'interface, pas le processus) | Un microservice de plus pour UN appel Stripe = une transaction distribuée (deal + paiement) à inventer, de la latence et un déploiement — sans bénéfice tant qu'un seul service paie. D30 y gagne : les tests injectent le Fake | Le secret Stripe vit dans deal-service (comme déjà dans auth-service pour Connect) | 🚪↔ |
| **D39** *(gravée le 31/08/2026)* | **Capture à l'ACCEPTATION (pas à J-1 du départ) + barème d'annulation post-acceptation.** Le `provider.capture()` est exécuté au moment où le Voyageur accepte (charte cochée, gate D31 passé). Raison technique dure : une empreinte carte expire **~7 jours** chez Stripe — capturer à J-1 casserait tout deal accepté plus d'une semaine avant le départ (ré-autorisations périodiques à inventer, échecs de ré-autorisation sur un deal déjà accepté). Conséquence assumée : toute annulation post-acceptation est un **REMBOURSEMENT** (ANN-01/02/04), plus une libération d'empreinte. Barème ANN-01 paramétré (§13) : `CANCEL_FULL_REFUND_UNTIL_HOURS = 48` ; **`CANCEL_LATE_RETENTION_PCT = 50`** — la retenue est destinée au Voyageur (versée avec l'infrastructure payout B4 ; en B2 elle est tracée dans `refundAmountCents` + l'événement `booking.refund_issued`). Ordre des opérations partout : **l'argent d'abord, la base ensuite** — capture/refund/cancel chez le fournisseur AVANT la transaction Mongo conditionnelle (updateMany sur le statut attendu) ; échec de la transaction après capture → compensation best-effort + filet webhook D40 | Un remboursement coûte moins cher qu'un système de ré-autorisation ; le Voyageur voit « l'argent est bloqué » dès son OUI — c'est la promesse produit | Frais Stripe non restitués sur les remboursements ; fenêtre théorique capture-réussie/transaction-échouée (compensée) | 🚪↔ (le barème) / 🚪→ (le moment de capture) |
| **D40** *(gravée le 31/08/2026)* | **Le webhook Stripe est la source de vérité de l'état du paiement.** `POST /webhooks/stripe` sur le deal-service, corps **BRUT** vérifié par signature (`STRIPE_WEBHOOK_SECRET`), route montée AVANT `express.json` (un corps re-sérialisé casse la signature — même raison : en dev `stripe listen --forward-to localhost:6003/webhooks/stripe`, jamais via le gateway). Événements traités : `payment_intent.canceled` → si un Booking **PENDING** porte cet intent, transition **SYSTEM cancel → CANCELLED** (kg restitués CAP-02, outbox `booking.cancelled`) — nouvelle transition `PENDING —cancel/SYSTEM→ CANCELLED` ajoutée à la machine et à la table §2.2, car une empreinte peut mourir SEULE (expiration 7 jours, annulation côté fournisseur) et un deal dont l'argent a disparu ne doit plus être acceptable ; `payment_intent.amount_capturable_updated` → accusé simple (l'autorisation est posée ; la création du deal reste pilotée par `POST /deals`, D37). Idempotence : un booking déjà terminal est ignoré (200) ; un intent inconnu aussi | Entre notre base et Stripe, c'est Stripe qui a l'argent : notre état doit converger vers le sien, jamais l'inverse | Un endpoint public de plus (signé) ; la transition SYSTEM-cancel n'existait pas dans la spec initiale (répercutée §2.2) | 🚪→ |
| **D41** *(gravée le 01/09/2026)* | **Emails transactionnels `booking.*` : un CANAL de plus de la matrice A15, dans le notification-service, sur une lib partagée `@packages/email`.** L'email n'est pas un nouveau système : c'est la deuxième colonne de la matrice A15, matérialisée par le MÊME consumer Kafka que l'in-app (pas de service email dédié, pas de 2e consumer group en v1 — un groupe de plus = des offsets de plus à opérer sans bénéfice tant qu'un seul handler envoie). La couche d'envoi (Nodemailer + EJS, SMTP par env, transport paresseux, port 465/587 auto) devient **`@packages/email`** — le 3e clone annoncé (auth-service, trip-service) est évité, la lib naît « au 1er email B2 » exactement comme prévu au handoff PR-A ; la migration des 2 clones existants reste au backlog (§7.2). Le provider transactionnel dédié (Resend/Postmark/SES — candidat D35) se branchera DERRIÈRE la même interface, sans toucher ni aux gabarits ni au dispatcher. Jointure `User` au moment de l'envoi (les événements ne portent ni email ni prénom — RGPD : un user effacé = envoi silencieusement sauté, tracé) ; locale FR par défaut (pas de `preferredLocale` sur User — même repli que trip-notifications) ; montants formatés depuis les CENTS du payload (A2). Le code de livraison ne voyage JAMAIS dans un email (règle existante, re-vérifiée sur les 17 payloads) | L'infrastructure (contrats riches, consumer idempotent, dédup event-id) a été construite pour ça : ajouter le canal = une matrice en data + des gabarits, zéro architecture nouvelle. Un email transactionnel est une preuve écrite (reçu, remboursement) que l'in-app seul ne donne pas | SMTP Gmail en dev/staging (provider sérieux avant lancement = D35) ; gabarits EN inline dans les templates (pas de next-intl côté serveur) | 🚪↔ (le transport) / 🚪→ (le canal dans le notification-service) |
| **D42** *(gravée le 02/09/2026)* | **Photos de prise en charge : upload DIRECT signé vers ImageKit (infrastructure existante), pas de media-service :6009 ni de R2 en B3.** Le Voyageur téléverse ses 1 à 5 photos depuis le navigateur avec la signature courte servie par `GET /api/uploads/imagekit-auth` (trip-service, déjà utilisé par les justificatifs de trajet — hook `useImageKitUpload`, dossier `/deals/pickup`) ; le deal-service ne reçoit que des **URLs https** (contrat `ConfirmPickupRequest.photoUrls`, min 1 / max 5, même forme que `parcel.photoUrls` de la demande) et les fige dans `Booking.pickup.photoUrls`. Aucun octet d'image ne transite par le gateway ni par le deal-service. La **checklist 5/5 est FIGÉE** avec les photos (`pickup.checklist`) : c'est l'attestation d'inspection CNF-04, horodatée serveur (RGP-03) | Le stockage d'images existe et fonctionne (justificatifs) ; monter un service de médias + un bucket R2 pour 5 photos par deal serait de l'architecture avant le besoin — et le schéma ne connaît que des URLs : changer de fournisseur demain = changer l'URL, pas le modèle | Les fichiers ImageKit sont lisibles par quiconque connaît l'URL (pas de signature de lecture) — acceptable pour des photos de colis en v1, à durcir avant lancement (fichiers privés + URLs signées, ou media-service) ; le serveur ne vérifie pas que l'URL pointe chez notre compte ImageKit (clés absentes du deal-service) | 🚪↔ (le fournisseur) / 🚪→ (URLs dans le snapshot, checklist figée) |
| **D43** *(gravée le 02/09/2026)* | **Code de livraison : DOUBLE stockage — bcrypt pour la validation, AES-256-GCM pour le ré-affichage.** Généré par le serveur à la transition ACCEPTED → PICKED_UP (`crypto.randomInt`, 6 chiffres 100000–999999, jamais avant — spec §3.1), il est écrit sous deux formes dans la MÊME transaction : `deliveryCodeHash` (bcrypt, coût 10 — le seul lu par `deliver`) et `deliveryCodeEncrypted` (AES-256-GCM, format versionné `v1.<iv>.<tag>.<chiffré>` en base64url — le seul lu par `GET /deals/:id` vue **Shipper**, en statut PICKED_UP uniquement, **jamais dans les listes**, jamais dans un événement ni un email, jamais dans une vue Carrier — A13). Clé `DELIVERY_CODE_ENCRYPTION_KEY` (32 octets base64, `.env` racine) ; **hors production sans clé → clé de développement dérivée d'une constante + avertissement au premier usage ; en production sans clé → refus** (miroir du Fake paiement refusé en production, D38). Le champ `deliveryCode: null` du contrat B1 devient le code en clair pour l'Expéditeur. Lib partagée **`@packages/delivery-code`** (node:crypto + bcryptjs, zéro dépendance d'infrastructure) : consommée par le deal-service ET par le seed (import relatif) | bcrypt seul (B1) rendait le ré-affichage impossible (« un code perdu = régénérer ») ; le clair en base serait lisible par toute fuite de dump ; l'AES avec clé d'environnement sépare la base de la clé — le compromis standard pour un secret qu'on DOIT pouvoir relire | Une clé unique pour toute la plateforme (rotation = ré-encryption, prévue par le préfixe de version) ; le code reste lisible par quiconque a la base ET l'environnement (l'admin C devra en tenir compte : jamais d'affichage admin du code) | 🚪→ (double stockage, périmètre d'exposition) / 🚪↔ (algorithme, longueur) |
| **D44** *(gravée le 03/09/2026, implémentée le jour même — PR `feat/email-locale`, A55–A57)* | **La langue d'un email est la langue de l'utilisateur, et le système est conçu pour N langues, pas pour deux.** ① `User.preferredLocale` (code court BCP-47 : `fr`, `en`, demain `pt`, `wo`…) — la liste des locales supportées vit à UN endroit (`SUPPORTED_LOCALES` dans `@packages/api-contracts`, consommée par next-intl ET par les services), jamais un booléen `fr ? … : …`. ② Le client API du front envoie `x-locale` (locale next-intl courante) sur CHAQUE requête. ③ Flux sans compte (OTP d'inscription, mot de passe oublié) : locale de la requête ; flux avec compte : `preferredLocale`, initialisée à l'inscription depuis `x-locale` et mise à jour immédiatement quand un utilisateur connecté bascule la langue dans le header (pas d'écran de profil). ④ Rendu : UN gabarit partagé dans `packages/libs/email` (préheader, code en gros caractères, durée d'expiration injectée, ligne « si ce n'est pas toi », pied « pourquoi tu reçois cet email »), dictionnaires par locale (un fichier par langue, repli `fr` clé par clé), sujets sans emoji ; les trois mailers (auth-service, trip-service, notification-service) migrent dessus | Recette 03/09 : sujets anglais sur corps français, trois mailers indépendants, aucune langue stockée, le front ne transmettait jamais sa locale ; notification-service est déjà bilingue mais figé sur `fr` — il ne manque que la donnée | Une locale par utilisateur (pas par appareil) ; un utilisateur EN qui reçoit un email déclenché par un utilisateur FR reçoit SA langue (la locale est celle du destinataire, pas de l'acteur) ; les templates `.ejs` par langue disparaissent au profit de dictionnaires — coût de migration des 7 templates auth | 🚪→ (locale = donnée utilisateur, liste centralisée) / 🚪↔ (format du dictionnaire, lib de rendu) |
| **D45** *(gravée le 03/09/2026)* | **Registre de marque des textes : Yamba TUTOIE partout** (interface, emails, messages d'erreur, notifications) **et nomme les gens par leur prénom réel.** Un email qui parle d'une autre personne dit « Thomas » quand le prénom est connu — jamais « le Voyageur » ni « l'Expéditrice » comme substitut ; le rôle n'apparaît qu'en complément (« Thomas, ton Voyageur »). Les pages d'authentification (login, inscription, OTP, mot de passe) passent au tutoiement dans la PR `feat/auth-pages-ux` avec la suppression des chiffres inventés (12k+, 4.8) et du témoignage fictif du panneau gauche. **Ouvert** : le nom du rôle transporteur (« Yamber » sur l'inscription, « Tripper » dans les emails carrier, « Voyageur » dans le produit) — une seule décision à prendre, puis appliquer partout | Trois registres coexistaient (pages auth « vous », porte de réservation « tu », emails « tu ») ; les faux chiffres et le faux témoignage sont un risque de crédibilité et un risque juridique sur une bêta | Le tutoiement engage un ton : les textes juridiques (CGU, confidentialité) restent au vouvoiement, seuls les textes produit tutoient | 🚪→ (tutoiement, prénom réel) / ⏳ (vocabulaire du rôle) |
| **D46** *(gravée le 03/09/2026)* | **Favoris de trajets = un signet PRIVÉ de l'Expéditeur.** Modèle `TripFavorite` (utilisateur + trajet, unique), `POST` / `DELETE /trips/:id/favorite` idempotents, `GET /trips/favorites`, champ `isFavorite` sur les cartes de recherche et la fiche publique (authent OPTIONNELLE : visiteur → `false`). Règles serveur : trajet inexistant ou supprimé → 404 (jamais 403), son propre trajet → 403 `OWN_TRIP`, trajet non PUBLISHED → 409 `TRIP_NOT_FAVORITABLE` à l'ajout ; le retrait est toujours possible. Un favori **survit à la fin du trajet** (la page « Mes favoris » le montre comme passé) ; il n'informe **jamais** le Voyageur et n'est **jamais compté publiquement** (pas de « 12 personnes ont mis ce trajet en favori » — ce serait un signal social non consenti). Visiteur qui clique le cœur → porte de connexion avec retour sur la page courante | Demande de recette 03/09 ; « mettre de côté » est le geste le plus fréquent d'un comparateur, et il doit exister avant les alertes (SavedRoute) parce qu'il ne demande aucune saisie | Pas de dossiers ni de notes (v1) ; pas de notification « ton favori part bientôt » (candidat avec les alertes de route) ; pas de compteur côté Voyageur | 🚪→ (privé, jamais compté) / 🚪↔ (rappels, dossiers) |
| **D47** *(gravée le 03/09/2026)* | **Connexion Google par jeton d'identité OpenID, vérifié côté serveur, avec consentement explicite à la création.** Le front rend le bouton officiel Google Identity Services et envoie l'`id_token` à `POST /auth/google` ; l'auth-service le VÉRIFIE (`google-auth-library`, audience = `GOOGLE_CLIENT_ID`) et exige `email_verified`. Modèle **`AuthIdentity`** (`provider` + `providerSub` uniques, séparé de `User` — pas de `googleSub` nullable unique sur Mongo) : identité connue → connexion ; sinon compte existant avec le même email vérifié → rattachement automatique puis connexion (l'email vérifié par Google vaut l'OTP) ; sinon nouveau compte SEULEMENT avec `consent { termsVersion, privacyVersion }` — sans lui, `CONSENT_REQUIRED` et rien n'est créé ; avec lui, User (sans mot de passe) + identité + `ConsentLog` dans UNE transaction, email de bienvenue. Une session D27 identique au login mot de passe (`issueSession` partagé). Facebook : non branché (bouton conservé) | Demande de recette 03/09 ; le jeton d'identité vérifié serveur est le seul schéma qui ne fait jamais confiance au navigateur ; le rattachement par email vérifié évite les doublons de compte ; le consentement reste une obligation RGPD quel que soit le canal | Sans `GOOGLE_CLIENT_ID` (serveur) et `NEXT_PUBLIC_GOOGLE_CLIENT_ID` (front), le bouton est inerte « bientôt disponible » et l'API répond 503 `GOOGLE_NOT_CONFIGURED` — la configuration Google Cloud est un geste de l'utilisateur ; un compte Google seul n'a pas de mot de passe (« mot de passe oublié » en crée un) | 🚪→ (jeton vérifié serveur, consentement obligatoire, AuthIdentity) / 🚪↔ (One Tap, autres fournisseurs) |
| **D48** *(gravée le 03/09/2026)* | **En développement et en recette, l'API est servie en MÊME ORIGINE que le front** : `next.config.js` proxie `/api/*` vers le gateway quand `API_PROXY_TARGET` est posé, et le navigateur appelle `NEXT_PUBLIC_API_BASE_URL=/api` (relatif). Les cookies d'authentification sont first-party sur n'importe quel hôte (`localhost`, IP LAN, téléphone) ; CORS et `SameSite` ne sont plus des paramètres de recette. Opt-in : sans la variable, comportement historique (URL absolue). Aucun appel API côté serveur Next aujourd'hui (vérifié) — si un jour un Server Component appelle l'API, il utilisera une URL interne absolue, jamais le chemin relatif | Piège vécu le 03/09 (front sur `localhost`, API sur l'IP LAN → login 200 puis `/me` 401, « impossible de se reconnecter ») ; un proxy de dev coûte trois lignes et supprime toute une classe de faux bugs | En production, l'architecture réelle (domaine API dédié ou reverse proxy) reste à graver avec le déploiement ; le proxy Next n'est pas un composant de prod | 🚪↔ (mécanisme du proxy) / 🚪→ (même origine en dev/recette) |
| **D49** *(gravée le 03/09/2026 — B4)* | **Versement au Voyageur : COMPLETED D'ABORD, transfert ENSUITE, idempotent et rejouable.** La transition DELIVERED → COMPLETED (confirmation anticipée de l'Expéditeur, ou cron J+4) est UNE transaction Mongo conditionnelle + outbox `booking.completed`, qui pose `payoutStatus = PENDING`. Le transfert (`PaymentProvider.transfer`, Stripe `transfers.create` vers le compte Connect du Voyageur, clé d'idempotence = id du booking) est exécuté juste après, hors transaction ; succès → `payoutStatus = SENT`, `transferId`, `payoutSentAt` + outbox `booking.payout_sent` ; échec → `payoutStatus = FAILED` + raison, et le cron de versement REJOUE les FAILED à chaque passage (fournées de 50, `payoutAttempts` compté). Inverse de D39 (capture/refund : l'argent d'abord) parce qu'ici COMPLETED est la CONDITION LÉGALE du versement (INV-2 : aucun versement avant COMPLETED) — un transfert avant la transaction pourrait payer un deal qui vient de passer DISPUTED | INV-2 et INV-5 se lisent sur un seul champ (`payoutStatus`) ; un échec Stripe (compte Connect incomplet, solde) ne bloque jamais la complétion ni la notation ; le rejeu est mécanique, pas manuel | Fenêtre où un deal est COMPLETED mais pas encore versé (visible : « versement en cours »). **Le transfert est rattaché à la charge** (`source_transaction` = `chargeId` stocké à la capture, décision utilisateur 03/09) : Stripe attend que les fonds de CETTE charge soient disponibles au lieu d'échouer sur le solde plateforme ; les deals capturés avant B4 (sans `chargeId`) transfèrent sans rattachement | 🚪→ (ordre, rattachement) / 🚪↔ (rejeu) |
| **D50** *(gravée le 03/09/2026 — B4)* | **Montant versé = `pricing.transportCents` du snapshot (net Voyageur, D16/D17), en `currencyCode` du snapshot.** Jamais recalculé, jamais de commission déduite à ce stade (elle est déjà hors du net). **Retenue ANN-01** (annulation tardive, D39) : le Voyageur touche la retenue **au prorata de sa part nette** — `retenue × transportCents / totalShipperCents` — Yamba garde la part de commission (décision utilisateur 03/09, option A). Même exécuteur de versement (D49), **PR dédiée juste après B4-PR1** (`feat/b4-late-cancel-payout`) : le Booking CANCELLED tardif porte `payoutStatus = PENDING` + `payoutAmountCents` calculé au cancel, versé par le cron | Un seul montant lu dans le snapshot immuable, auditable ; le Voyageur qui a bloqué sa capacité est indemnisé comme sur les marketplaces de référence (BlaBlaCar) | Un arrondi au centime par deal ; la retenue reste « en attente » jusqu'à la PR dédiée | 🚪→ |
| **D51** *(gravée le 03/09/2026 — B4)* | **Litige = un modèle `Dispute` (A10), un ticket `YAM-` + 4 chiffres CSPRNG unique, une seule ouverture par deal.** `POST /deals/:id/dispute` (Expéditeur) depuis **DELIVERED** (avant `payoutDueAt` — guard machine) **ou depuis PICKED_UP** (colis jamais livré : catégorie `NOT_DELIVERED` imposée, guard machine « départ du trajet dépassé de 48 h » — décision utilisateur 03/09, option A, 48 h) : catégorie parmi 6, description ≥ 50 caractères, `pledgeAccepted: true` obligatoire, photos ≤ 5 (URL ImageKit, upload direct D42, dossier `deals/dispute/`), solution souhaitée optionnelle. Transaction : DISPUTED + `payoutStatus = FROZEN` + `disputeTicket`/`disputedAt` sur le Booking + création du `Dispute` (`bookingId @unique`, `ticketNumber @unique`) + outbox `booking.disputed`. Collision de ticket (P2002 sur `ticketNumber`) → nouveau tirage (5 essais), au-delà 6 chiffres. DISPUTED reste terminal en v1 : la résolution (COMPLETED, remboursement partiel/total, décision) est le **chantier C (admin), lancé juste après B4** — aucune route admin jetable en B4 (décision utilisateur 03/09, option B) | INV-4 (irréversible, fenêtre J+4) et INV-5 (gel) tiennent dans le statut + `payoutStatus` ; le `Dispute` porte le dossier (photos, description, pledge horodaté) que la médiation lira ; l'unicité sur un modèle dédié évite le piège Mongo « unique nullable » sur le Booking | 4 chiffres = 9 000 tickets avant élargissement (assumé v1) ; aucune notification de l'équipe médiation en v1 (pas d'admin-ui) : le litige est visible en base et dans les emails des deux parties ; un deal PICKED_UP disputé reste sans versement (rien n'était programmé) | 🚪→ (modèle, fenêtre) / 🚪↔ (format du ticket, 48 h) |
| **D52** *(gravée le 03/09/2026 — B4)* | **Notifications B4 (complément de la matrice A35/A41)** : `booking.completed` → email **Expéditeur** (« paiement libéré, merci » — SANS bouton « Noter » tant que la notation n'existe pas, B5) ; `booking.payout_sent` → email **Voyageur** (montant net, copie honnête : « parti vers ton compte, sur ton compte bancaire sous 2 à 7 jours » — un transfert Connect n'est pas un virement) ; **`booking.verification_reminder`** (nouvelle clé, cron J+3) → email + in-app **Expéditeur** « dernier jour pour vérifier ou signaler » ; `booking.disputed` → email **Expéditeur** (accusé de réception, ticket, gel, 48 h ouvrées) ET **Voyageur** (ton calme : « un signalement a été ouvert (catégorie), ton paiement est mis en attente, nous te contacterons pour ta version »). In-app : `completed` et `disputed` aux deux parties, `payout_sent` au Voyageur (déjà dans `IN_APP_MATRIX`), `verification_reminder` à l'Expéditeur. Les emails B4 sont écrits en DONNÉES par langue via le gabarit partagé (D44 `sendTransactionalEmail`) et non en gabarits EJS par service — premier pas de la migration des emails `booking.*` | Chaque moment d'argent laisse un email (RG-N-01) ; le Voyageur reçoit UN email de versement, pas deux (completed est muet pour lui) ; D44 impose le dictionnaire | Deux mécanismes d'email cohabitent dans le notification-service jusqu'à la migration des 12 gabarits EJS (dette §7.2) | 🚪→ |
| **D53** *(gravée le 03/09/2026 — B5)* | **Notation mutuelle DOUBLE-AVEUGLE, une fois par rôle, fenêtre de 14 jours après COMPLETED.** Chaque partie note l'autre (1 à 5 obligatoire, critères en pouces optionnels propres au rôle noté, commentaire public attribué ≤ 280, non modifiable). Un avis naît caché (`Review.revealedAt = null`) et n'est révélé que quand l'AUTRE a noté (même transaction, `booking.rating_revealed` BOTH_RATED) ou à la fin de la fenêtre (cron, WINDOW_ELAPSED — même si un seul a noté). Seuls les avis révélés sont publics et comptent dans la réputation. Relances J+5 et J+7 aux rôles muets (in-app + email), puis silence. Pas de note sur un deal annulé ni en litige (la médiation tranche d'abord, chantier C). **Réputation visible (D29①)** calculée serveur et dénormalisée (`reputationLevel`, `completedDealsCount`, `lateCancellationsCount` sur CarrierPage / User) : NEW < 3 deals · CONFIRMED ≥ 3 · TOP (Voyageur : ≥ 10 deals, note révélée ≥ 4,8, 0 annulation après acceptation ; Expéditeur « fiable » : ≥ 5, 4,8, 0 annulation tardive) — seuils en `REPUTATION_PARAMS` ; `isSuperCarrier` = TOP. Niveaux INFORMATIFS : aucun effet sur le prix tant que l'historique n'existe pas (décision 6A) | Décisions utilisateur 03/09 (1A–6A) : la seconde note ne doit pas répondre à la première ; une note dit qui et quoi ; la réputation doit être explicable ; le prix ne bouge pas sur des chiffres qu'on n'a pas | Une note reste invisible jusqu'à 14 jours ; `bookingId` nullable sur Review → unicité par rôle garantie par le service (A9), pas par l'index | 🚪→ (double-aveugle, une fois) / 🚪↔ (seuils, fenêtre, effet prix) |
| **D54** *(gravée le 03/09/2026 — chantier C)* | **Chantier C = socle admin puis médiation, chez les propriétaires de domaine.** Découpage (1A) : C-PR1 socle (`apps/admin-ui` séparée D7, rôle ADMIN posé par script, **2FA TOTP obligatoire**, journal d'audit, file « à arbitrer » en lecture avec le dossier complet) → C-PR2 médiation (version du Voyageur dans l'app, décisions, argent, écrans des deux rôles) → C-PR3 signalements (endpoint `Report` réel, boutons in-app, file, masquage d'avis) → C-PR4 paramètres audités → C-PR5 vérification des billets ; TrustScore ② après. **Routes admin chez le propriétaire** (2A) : deal-service `/admin/disputes/*`, auth-service `/admin/*` + 2FA, trip-service `/admin/trips/*` ; gateway `/api/admin/*` par préfixe ; aucune écriture croisée entre services. **Issues d'une médiation** (3A) : rejet (Voyageur payé en entier, COMPLETED), remboursement partiel (montant libre en centimes, Voyageur = net − montant plancher 0, commission conservée, COMPLETED), remboursement total (commission comprise, Voyageur 0, CANCELLED fermé par ADMIN) — même exécuteur D49, remboursement d'abord, transfert ensuite. **Aucune notation publique après un litige** (4B) : le litige tranché est un fait interne (D29②). **Version du Voyageur dans l'app** (5A), une fois, ≥ 50 caractères + 5 photos ; chaque partie ne voit que son dossier et la décision motivée. **Décision = issue + montant + motif ≥ 50 caractères** (6A), transaction + outbox `booking.dispute_resolved`, email + notification aux deux, irréversible dans l'app. **Retenues à arbitrer dans la même file** (7A) : compensation au Voyageur (prorata A79) ou restitution à l'Expéditeur. **Accès admin** (8A) : connexion par le même auth-service puis TOTP (secret chiffré AES, codes de secours à usage unique), session admin courte (accès 15 min, inactivité 45 min, pas de « rester connecté »), **journal d'audit écrit dans la même transaction que chaque action** (qui, quoi, cible, avant, après) | Décisions utilisateur 03/09 (1A 2A 3A 4B 5A 6A 7A 8A) : l'admin voit les dossiers et déclenche des remboursements — il ne se livre pas sans 2FA ni journal ; la machine reste l'unique juge des transitions ; une note après litige est une note de vengeance | Cinq PR avant que l'admin soit complet ; les paramètres restent en dur jusqu'à C-PR4 | 🚪→ (découpage, propriétaires, 2FA, journal, pas de note après litige) / 🚪↔ (durées de session, plancher, format des codes) |
| **D55** *(gravée le 03/09/2026 — chantier C, PR2)* | **La médiation se joue dans l'app, avec un délai, un récapitulatif et une trace.** (1A) À l'ouverture d'un litige, le Voyageur donne sa version DANS l'app (`POST /deals/:id/dispute/response` : texte ≥ 50 caractères, ≤ 5 photos, une seule fois) ; l'admin peut trancher dès la réponse reçue, ou sans réponse après `DISPUTE_RESPONSE_DELAY_HOURS` = 72 h. (2A) Décision = issue + montant (partiel borné entre 1 centime et le total − 1) + motif ≥ 50 caractères, précédée d'un récapitulatif des flux (remboursé à l'Expéditeur, versé au Voyageur, conservé par Yamba) ; irréversible, le remboursement part immédiatement. (3A) Retenue à arbitrer : deux issues aux montants calculés serveur — compensation au Voyageur (prorata A79) ou restitution à l'Expéditeur — motif obligatoire. (4A) Un litige tranché incrémente un compteur INTERNE « litiges perdus » sur la partie condamnée (Expéditeur si rejet, Voyageur dès qu'il y a remboursement), servi au dossier admin, sans effet public. (5A) Les deux parties reçoivent « Décision rendue » (écran + email + notification) avec l'issue, le montant qui les concerne et le motif ; aucune ne voit la version de l'autre. (6A) Recours = médiation conventionnelle par email au support, rien dans l'app. (7A) Machine : DISPUTED → COMPLETED (`resolveDisputeKeep`, ADMIN : rejet ou partiel) et DISPUTED → CANCELLED (`resolveDisputeRefund`, ADMIN : total) ; retenue : statut inchangé, `retentionDisposition` → CARRIER ou SHIPPER ; pas de statut RESOLVED ; le `Dispute` porte la résolution ; une décision par dossier (verrou), outbox `booking.dispute_resolved` ; pas de fenêtre de notation après litige (D54 4B) | Décisions utilisateur 03/09 (1A–7A) : juger sans avoir entendu est injuste, trancher sans récapitulatif coûte de l'argent réel, un litige laisse une trace sans devenir une note de vengeance | 72 h d'attente possibles avant décision ; le Voyageur ne peut pas compléter sa version une fois envoyée | 🚪→ (délai, récapitulatif, compteur interne, machine) / 🚪↔ (72 h, bornes du partiel) |
| **D56** *(gravée le 04/09/2026 — chantier C, PR3)* | **Profils admin, comptes du back-office par invitation, sanctions à deux niveaux, sessions, observabilité.** (1A) Quatre profils sur `User.adminRole` — `SUPER_ADMIN` (tout), `MEDIATOR` (litiges, sanctions), `SUPPORT` (fiches, propose une sanction), `FINANCE` (lecture, journal) — matrice `ADMIN_PERMISSIONS` dans le contrat, lue par le middleware `requireAdminPermission` et par l'admin-ui ; posés par script ou par un SUPER_ADMIN ; jamais le dernier SUPER_ADMIN rétrogradé ou retiré, jamais soi-même. **Invitation** : un SUPER_ADMIN crée un compte admin qui naît SANS rôle client (ni publier ni réserver tant qu'il ne passe pas par le parcours client), mot de passe défini par un lien emailé (jeton Redis 48 h), 2FA à la première connexion ; un compte existant reçoit le profil et un email « accès accordé ». **Conflit d'intérêts** : un admin n'agit jamais sur un deal dont il est partie ni sur son propre compte (403 serveur). (2A) Sanction à deux niveaux : `RESTRICTED` (ni publier ni réserver, deals en cours continuent) et `SUSPENDED` (connexion refusée, sessions révoquées, trajets masqués de la recherche par filtre de lecture, deals en cours signalés au support), motif ≥ 20 caractères, durée optionnelle, email au membre sans le détail d'un signalement, levée motivée. (3A) SUPPORT propose, MEDIATOR / SUPER_ADMIN exécute ; un admin ne sanctionne un autre admin que s'il est SUPER_ADMIN. (4A) Fiche : identité, rôles, statut Voyageur et Stripe (identifiant masqué, encaissements / versements), réputation publique et compteurs internes, trajets, deals, litiges, journal le concernant, sessions actives — jamais un secret ni un code de livraison. (5A) Recherche par email, prénom, nom, téléphone, identifiant de deal ou ticket. (6A) Sessions admin listées et révocables, alerte email à chaque ouverture de session. (7A) Sentry : `initSentry(service)` dans chaque service, capture des 5xx et des erreurs non opérationnelles dans le middleware d'erreurs commun, taguées service + `x-correlation-id` ; instrumentation Next (client et serveur) sur user-ui et admin-ui ; inerte sans DSN | Décisions utilisateur 04/09 (1A–7A + invitation) : tout le monde ne peut pas être super admin ; un compte de staff ne doit pas pouvoir transporter ou expédier par défaut ; une sanction sans motif ni effet réel est un drapeau, pas une décision | Le gateway n'initialise pas Sentry (pas d'alias `@packages` dans son webpack) ; RGPD (export, effacement) et alertes de seuil restent à faire (C-PR6/C-PR8) | 🚪→ (profils, invitation sans rôle client, conflit d'intérêts, deux niveaux, effets) / 🚪↔ (matrice exacte, 48 h, durées) |
| **D57** *(gravée le 04/09/2026 — chantier C, PR4)* | **Trajets et billets côté admin : vérifier, masquer, compter — sans jamais annuler à la place du Voyageur.** (1A) File « billets à vérifier » = documents `TICKET_PROOF` en attente sur des trajets à venir, les plus anciens d'abord ; l'admin ouvre le document (consultation journalisée, 7A), valide ou rejette avec un motif fermé (`ILLEGIBLE`, `DATES_MISMATCH`, `NAME_MISMATCH`, `SUSPICIOUS`) ; vérifié → badge public, rejeté → email et nouveau dépôt possible ; un billet en attente sur un trajet parti passe `EXPIRED` à la lecture de la file (8A). (2A) Le billet reste informatif : rien n'est bloqué sans billet vérifié. (3A) « Masqué par Yamba » (`Trip.hiddenByAdminAt` + motif ≥ 20, réversible) est distinct de la pause du Voyageur : invisible en recherche et en page publique, réservations en cours préservées, email au Voyageur avec motif générique ; Yamba n'annule jamais un trajet. (4A) Fiche trajet admin : le trajet, le Voyageur (état du compte), les réservations avec statuts et montants, les documents, les dates clés, le journal. (5A) Liste filtrable (statut, corridor, Voyageur, départ, « billet à vérifier », « masqués »). (6A) Support vérifie les billets et propose un masquage ; Médiateur ou super admin masque et démasque. **KPI d'accueil** : compteurs opérationnels calculés serveur (`GET /admin/kpis`), chaque compteur servi selon la permission du profil, le super admin voit tout ; le pilotage complet (courbes, corridors, finances) reste C-PR5/C-PR6 | Décisions utilisateur 04/09 (1A–8A + KPI par profil) : un billet non vérifié ne doit pas bloquer une place avant d'avoir du volume ; masquer est un geste de modération, annuler est un geste d'argent | Pas de détection de doublons ni d'identité forte (backlog) ; la visibilité d'un trajet masqué passe par une lecture de plus (filtre `hiddenByAdminAt` absent OU null) | 🚪→ (file, motifs fermés, masquage réversible, jamais d'annulation admin) / 🚪↔ (liste des motifs, expiration) |
| **D58** *(gravée le 04/09/2026 — chantier C, PR5)* | **Finances côté admin : voir l'argent tel qu'il est, agir sur les exceptions, ne jamais recalculer.** (1A) Pas de grand livre : les files, la fiche argent et le rapport se calculent depuis le `Booking` (snapshot figé + champs posés par les transitions, RG-FIN-01) ; un grand livre viendra avec le volume ou l'expert-comptable. (2A) Trois files d'exception : « versements en échec » (motif lisible, tentatives, prochaine relance), « transferts renversés » (webhook `transfer.reversed`, jamais renvoyés automatiquement), « retenues conservées » (lien vers la médiation). (3A) Gestes : rejouer un versement maintenant (même exécuteur A65, idempotent) ; clore un renversement avec motif ≥ 20 — « re-versé » (nouvelle clé d'idempotence, même exécuteur) ou « abandonné » ; **remboursement manuel** hors litige (geste commercial) borné à total − déjà remboursé, motif ≥ 50, email à l'Expéditeur, proposé par FINANCE ou SUPPORT, appliqué par SUPER_ADMIN seul (C-PR5b). (4A) Fiche argent de TOUT deal : chronologie autorisé → capturé → remboursé → terminé → versé → renversé → retenue, identifiants fournisseur en clair pour la finance, et **rapprochement à la demande** (`PaymentProvider.inspect`, lecture seule : état réel de l'intent, remboursements, transfert) qui liste les divergences avec la base — seul moyen de voir un remboursement parti sans écriture (D39). (5A) Rapport par mois et par devise (encaissé, remboursé, versé, commission = revenu, retenues conservées = passif, en attente) + export CSV par deal avec les identifiants Stripe, journalisé (C-PR5b). (6A) Permissions : `finances.read` (FINANCE, MEDIATOR), `finances.export` (FINANCE), `payouts.retry` / `payouts.resolve` (FINANCE, MEDIATOR), `refunds.manual.propose` (FINANCE, SUPPORT), `refunds.manual.apply` (SUPER_ADMIN) ; conflit d'intérêts partout. (7A) Le digest email quotidien (A88) est conservé et pointe vers les files admin. (8A) Deux PR : C-PR5a (files, fiche argent, rejeu, renversements, rapprochement, identifiants stockés, rejeux espacés) puis C-PR5b (rapport, export, remboursement manuel) | Décisions utilisateur 04/09 (1A–8A) : l'argent réel est chez Stripe, la base ne fait que le refléter — l'admin doit pouvoir comparer les deux ; un geste d'argent sans motif ni journal n'existe pas | Les frais Stripe ne sont pas en base (le comptable rapproche avec l'export Stripe) ; un renversement « abandonné » reste un manque à gagner assumé | 🚪→ (pas de grand livre, trois files, rapprochement, SUPER_ADMIN pour le remboursement manuel) / 🚪↔ (espacement des rejeux, bornes) |
| **D59** *(gravée le 04/09/2026 — chantier C, PR6)* | **Pilotage : des courbes et des corridors calculés serveur, des alertes de seuil sans état, la chronologie complète d'un deal — et deux signaux de demande.** (1A) Les courbes se calculent depuis les modèles (inscriptions, trajets publiés, demandes, acceptations, livraisons, litiges, annulations, deals terminés, volume encaissé par devise), par semaine ou par mois UTC, sans dépendance externe ; PostHog (D5) reste une PR dédiée du jalon 2. (2A) Tableau par corridor (ville → ville) : trajets publiés, demandes, taux d'acceptation, prix moyen au kilo, litiges, vues et recherches. (3A) Alertes de seuil = règles serveur évaluées par un cron horaire du deal-service, seuils en constantes versionnées (versement en échec > 48 h, litige décidable sans décision > 72 h, retenue non arbitrée > 7 j, renversement ouvert > 48 h, outbox parqué ou relais en retard > 15 min, emails en échec, aucun trajet publié depuis 7 j, taux d'acceptation < 30 % sur 7 j) ; réglables en C-PR8. (4A) Alertes calculées à la lecture (accueil admin + digest email), sans stockage ni acquittement. (5A) « Tout ce qui est arrivé à ce deal » = chronologie en lecture seule fusionnant outbox (avec l'état de relais), journal admin, notifications et envois d'emails ; consultation journalisée ; jamais le code de livraison. (6A) Cache Redis 60 s sur courbes et corridors seulement. (7A) `pilotage.read` pour FINANCE et MEDIATOR ; la chronologie d'un deal (`deals.history.read`) pour MEDIATOR, SUPPORT, FINANCE. (8A→8B) Deux PR : C-PR6a courbes, corridors, chronologie, compteurs ; C-PR6b alertes et cron. **Axes retenus** : compteur de vues d'un trajet (D5 : Redis, dédoublonné par visiteur et par jour, affiché dans la recherche et le détail du trajet côté web) et compteur de recherches par corridor avec le nombre sans résultat (Redis, aucune donnée personnelle) | Décisions utilisateur 04/09 (« oui pour toutes les recommandations » + affichage web des vues) : un opérateur pilote avec les données réelles avant d'instrumenter le front ; un corridor demandé sans offre est le signal le plus précieux d'une marketplace | Les vues et recherches vivent dans Redis (pas de reprise si Redis est vidé) ; sans événements de trajet, l'historique d'un trajet reste muet (D-next) ; le chat Expéditeur ↔ Voyageur (chantier F) est à challenger après C | 🚪→ (calcul serveur, alertes sans état, chronologie lecture seule, compteurs Redis) / 🚪↔ (seuils, granularités, TTL) |
| **D60** *(gravée le 04/09/2026 — chantier C, retours de recette)* | **Profils cumulés, recherches et exports encadrés, pilotage lisible.** (1A) Un compte admin porte une LISTE de profils (`User.adminRoles`, union des permissions, SUPER_ADMIN = tout) ; l'invitation et la fiche admin cochent les profils ; `adminRole` reste le miroir du premier profil le temps de la reprise (script, listes absentes) ; gardes « dernier SUPER_ADMIN » et conflit d'intérêts inchangées. (2A) Chaque domaine admin (utilisateurs, trajets, billets, à arbitrer) a des filtres serveur, un tri, une pagination par curseur et un export CSV journalisé (période, filtres, nombre de lignes) ; **RGPD** : l'export de données personnelles (utilisateurs) est réservé au SUPER_ADMIN (`exports.personal`) avec un motif ≥ 20 caractères au journal ; les exports de trajets, billets et dossiers ne portent ni email ni téléphone (identifiants seulement) ; Finance garde l'export financier. (3A) Pilotage : deux courbes par ligne avec hauteur minimale, chaque courbe s'agrandit en pleine largeur avec le tableau des données sous la courbe ; un clic sur un point liste les éléments de la période (inscriptions, trajets, deals) avec lien vers leur fiche (« drill-down », un endpoint serveur par mesure, borné). (4A) Onglet « Finances » du pilotage : encaissé, remboursé, versé, revenu reconnu, retenues, par période et par devise, mêmes courbes agrandissables ; calcul par les mêmes règles que le rapport (A114). Ordre : C-PR6c pilotage v2 → C-PR3bis profils cumulés → C-PR7a recherches et exports → C-PR6b alertes → chantier F chat | Décisions utilisateur 04/09 après recette C-PR4 → C-PR6a (« OK pour toutes les recommandations ») : une personne cumule les casquettes au lancement ; un opérateur a besoin de filtrer et d'exporter ; une courbe qu'on ne peut ni agrandir ni ouvrir ne sert pas | Un export nominatif reste un risque assumé, tracé ; la reprise des profils est un script à jouer une fois | 🚪→ (liste de profils, export personnel SUPER_ADMIN + motif, drill-down borné) / 🚪↔ (filtres exacts, bornes) |
| **—** | **Espagnol (3e langue)** : l'ajout devient le **critère de fin** du chantier i18n. Une fois toutes les migrations faites : PR `feat/locale-es` = 22 namespaces traduits + `"es"` dans routing.locales, zéro code. (À mûrir : le portugais — Angola, diaspora lusophone — pourrait servir le corridor avant l'espagnol) | Ajouter l'espagnol maintenant produirait une expérience trouée (clusters non migrés en FR) ; l'objectif force à finir la dette | L'espagnol attend la fin des migrations | 🚪↔ |
| **—** | **SavedRoutes = arme anti-liquidité** : CTA « Créer une alerte » sur la page Alertes route ET sur l'état vide de la recherche (« Aucun trajet ? Sois prévenu dès qu'un Yamber en publie un »). **Page destinataire** (lien SMS sans compte, suivi + « vous aussi, envoyez ou transportez ») = canal d'acquisition dormant des deux côtés du corridor | Une marketplace meurt du frigo vide ; l'Expéditeur qui trouve zéro trajet ne revient jamais ; le destinataire vit l'expérience sans être converti | Petites PRs front | 🚪↔ |
| **—** | **Supply-seeding au lancement** (ops, pas code) : recruter à la main 20-30 voyageurs réguliers du corridor avant d'ouvrir la demande | La liquidité avant les features | Effort terrain | — |


---

# 2bis. Registre des arbitrages de chantier (A-series)

> Les décisions **D** fixent le cap (§2) ; les arbitrages **A** sont les choix d'implémentation gravés en cours de chantier, validés par Telama en session. Même règle : toute divergence future s'arbitre contre ce tableau, puis s'y répercute. Colonne « PR » = où l'arbitrage est né. Sources consolidées : handoffs B1-PR1-PR2 §3 (A1–A11), B1-PR3 §3 (A12–A22), B1-PR4 §3 (A23–A24) — exhaustivité vérifiée (11+11+2 = 24, sans trou).

## 2bis.1 — Session B1-PR1/PR2 (modèle & machine) — A1→A11

| # | Arbitrage | Pourquoi | Compromis | PR |
|---|---|---|---|---|
| A1 | Capacité kg : `Trip.capacityKg` + `reservedKg` compteur atomique ; `remainingKg` toujours DÉRIVÉ, jamais stocké | Invariant vérifiable (`reservedKg === Σ kg des bookings actifs`) ; un champ stocké de plus = une désynchronisation de plus | Un calcul à chaque lecture | #71 |
| A2 | Montants monétaires : `Int` centimes, JAMAIS de Float | Le flottant monétaire produit des erreurs d'arrondi ; les centimes entiers sont exacts par construction | Conversions d'affichage côté front | #71 |
| A3 | `PricingSnapshot` agnostique au moteur (discriminé PER_CATEGORY / PER_KG) | Un changement de moteur de prix (D13) ne migrera JAMAIS les snapshots existants — l'immuabilité D17 tient dans le temps | Deux formes à savoir afficher | #71 |
| A4 | **B1 sans création de deal** (aucun POST) | L'invariant « PENDING ⇒ paiement autorisé » ne doit jamais être violé, même en dev ; la création naît en B2 avec le PaymentIntent | Le front s'appuie sur le seed (A14) en attendant | #71 |
| A5 | Découpage B1 : 4 PRs backend + 1 PR front (PR5) | Chaque PR reste relisible et mergeable seule ; livraison = PR mergée | Plus de rituel git | — |
| A6 | Nom Nx scopé `@yamba-app/deal-service` | Cohérence de nommage du monorepo | Harmonisation des anciens noms au backlog | #70 |
| A7 | Tracking HORS state machine (séquenceur dédié `canConfirmTrackingStep`) | Les jalons AT_AIRPORT→FLIGHT_DEPARTED→FLIGHT_ARRIVED sont une séquence, pas des états du deal : les mélanger gonflerait la machine | Deux mécanismes à documenter | #71 |
| A8 | Machine à ACTEURS (from×action×acteur) + effets déclarés EN DATA | Un `cancel` SHIPPER ≠ un `cancel` CARRIER (ANN-01 vs ANN-02) ; les effets en data sont testables et exécutables par les writers B2 sans réécriture | Table de transitions plus grande (188 tests l'encadrent) | #71 |
| A9 | Réputation = EXTENSION du modèle `Review` existant (B5), pas de modèle parallèle | Une seule source d'avis ; D29 s'appuie dessus | Piège noté : index unique sur `bookingId` nullable (Mongo P2002) — index partiel ou unicité service | #71 |
| A10 | `Dispute` modèle en B4 ; `Report` livré dès B1 | Le signalement (SIG) est transverse et immédiat ; le litige exige la matrice de médiation (chantier C) | DISPUTED terminal en v1 | #71 |
| A11 | Divergence CONSTATÉE : `SiteConfig.commissionRate` 0.10 ≠ D16 (12 % + plancher 300 cents) → correction gravée pour B2 | On ne corrige pas un paramètre monétaire en passant ; la correction voyage avec les writers B2 qui le consomment | Divergence vivante jusqu'à B2 — documentée ici pour ne pas devenir invisible | #71 |

## 2bis.2 — Session B1-PR3 (lecture & contrats) — A12→A22

| # | Arbitrage | Pourquoi | Compromis | PR |
|---|---|---|---|---|
| A12 | Route carrier = `GET /deals?tripId=` côté deal-service ; ownership par lecture Prisma READ-ONLY du Trip (select minimal) | Le domaine deal reste chez le deal-service ; enrichir `GET /trips/:id` aurait fait écrire du deal dans le trip-service | Une frontière de lecture croisée (jamais d'écriture) | #72\* |
| A13 | DTOs par rôle en LISTE BLANCHE stricte (jamais spread+delete). Carrier : jamais code/hash/régénérations ; pricing = gains seulement. Code : bcrypt (validation) + AES-256-GCM `deliveryCodeEncrypted` (ré-affichage shipper, champ B2) ; d'ici là `deliveryCode = null` documenté au contrat | Un spread oublie d'exclure le champ ajouté demain ; la whitelist est spread-résistante (prouvé par test makeLeakyBooking) | Chaque nouveau champ exige un ajout explicite aux DTOs | #72\* |
| A14 | Seed international 6 corridors, idempotence hybride (users upsert / trips+bookings wipe&recreate), reservedKg CALCULÉ (CAP-02), `seed-output.json` gitignoré | Outil de démo/QA permanent + prérequis du branchement front PR5 ; l'international dès le seed = le positionnement universel dans les données de dev | Wipe destructif assumé sur trips/bookings de seed | #72\* |
| A15 | Matrice des 17 notifications GRAVÉE dès PR3 (handoff PR3 §4) ; event keys + payloads Zod dans les contrats | Les contrats d'événements sont une porte 🚪→ : un payload pauvre publié = donnée perdue à jamais | Contrats écrits avant leur premier consumer | #72\* |
| A16 | notification-service DÉDIÉ :6004, livré PR4bis | Premier consumer = frontière de domaine propre (D1), pas un module du deal-service | Un service de plus à opérer | #72\* |
| A17 | PAS de « kafka-service » : le broker est de l'infra ; ce qui s'écrit = le relay (deal-service) + un consumer par service | Un service-bus central recréerait le couplage que D2 supprime | La lib partagée (A24) porte le code commun | #72\* |
| A18 | Roadmap ports gravée (voir §0bis). REFUSÉ : créer des services vides en avance | Réserver les numéros coûte zéro ; des squelettes vides coûtent de la maintenance (pushback accepté) | — | #72\* |
| A19 | BOOKING_ACTIVE/TERMINAL/COMPLETION_BLOCKING_STATUSES vivent dans `@packages/api-contracts` (source unique) ; la machine importe + ré-exporte | trip-service et deal-service consomment les MÊMES ensembles — la recopie aurait divergé | Dépendance contrats ← machine | #72\* |
| A20 | DISPUTED conserve les kg (bloque edit/unpublish) mais NE bloque PAS la complétion du trip → 2e flag ctx `hasBookingsInProgress?` avec REPLI CONSERVATEUR (`?? hasActiveBookings`) | Le voyage physique est fini même en litige ; le repli fait qu'un appelant non recâblé retrouve l'ancien comportement — sans information, choisir l'erreur la moins grave | Deux flags proches à ne pas confondre (4 tests les gardent) | #72\* |
| A21 | Sémantique d'erreurs propre dès le jour 1 au deal-service : 400 validation seule, 403 ForbiddenError, 404 NotFoundError — « pas partie au deal » = 403, pas 404 | Le trip-service traîne un legacy 400-partout (PR `fix/error-semantics` au backlog §7.2) ; le nouveau service ne l'hérite pas | Incohérence temporaire entre services, documentée dans les deux OAS | #72\* |
| A22 | Registre OAS COMMUN assumé : les 2 specs embarquent les 93 mêmes schémas (un seul espace de noms plateforme) | `z.globalRegistry` est global par nature ; un espace de noms unique évite les collisions de `$id` et sert le client mobile généré (G) | Les deux openapi.json bougent ensemble (le diff CI le sait) | #72\* |

## 2bis.3 — Session B1-PR4 (le producteur) — A23→A24

| # | Arbitrage | Pourquoi | Compromis | PR |
|---|---|---|---|---|
| A23 | **Topologie événementielle** : UN topic par domaine (`booking-events`), jamais par eventType · clé = `aggregateId` · **12 partitions à la création** · rétention 7 j · replay depuis l'outbox MONGO (source de vérité, jamais de delete), jamais depuis Kafka · auto-création interdite côté cluster ET producer | Même clé = même partition = ordre par deal sans ordre global ; augmenter les partitions plus tard changerait le mapping clé→partition (quasi irréversible → pris large d'emblée) ; l'outbox éternel rend la rétention courte sans risque | Kafka n'est pas la source de replay (assumé) ; 12 partitions surdimensionnées au départ | #73 |
| A24 | **Architecture du relay** : lib `@packages/messaging` (interface `EventPublisher`, kafkajs isolé dans UN fichier) · un seul relay actif (RelayLease : CAS atomique, TTL 30 s, heartbeat 1 s) + `OUTBOX_RELAY_ENABLED` · payload = événement COMPLET validé au contrat AVANT publication · header unique `event-id` (= _id row) = clé d'idempotence consumers · at-least-once (publishedAt post-ack, par message) · poison (ZodError ou kafkajs non-retriable HORS connexion) : attempts++, parking à 10, jamais de delete · transitoire : JAMAIS d'attempts++, backoff 1→30 s · boucle setTimeout chaîné, connexion lazy, arrêt propre, timers unref | Le relay publie l'union sans énumérer les types (un 18e event ne le touche pas) ; changer de client Kafka = réécrire un seul fichier, zéro test à toucher ; une panne broker ne doit jamais parquer des événements sains (bug attrapé au smoke réel : kafkajs marque `retriable:false` ses pannes de connexion épuisées) | Polling 1 s (latence acceptée vs CDC/change streams) ; une table de bail de plus | #73 |

*\* #72 : numéro de la PR3 à confirmer (§7.2).*

## 2bis.4 — Session B2-PR1 (naissance du deal) — A29→A30

| # | Arbitrage | Pourquoi | Compromis | PR |
|---|---|---|---|---|
| A29 | **Snapshot `parcel.category` conservé (enum legacy) : dérivé de la famille D14** par une table figée `FAMILY_DEFAULT_CATEGORY` (`CLOTHES_TEXTILE → CLOTHES`, `ELECTRONICS_DEVICES → OTHER_ELECTRONICS`, `TOYS_CHILDCARE → SMALL_TOYS`, `DOCUMENTS_PAPERS → DOCUMENTS`, autres → `OTHER_ACCESSORIES`, bagages → `CHECKED_BAG_23KG`/`CABIN_BAG_12KG`) ; `categoryFamily` porte la famille réelle | Les événements et le consumer notification exigent `category` ; rendre le champ nullable aurait touché 3 services pour un intérêt nul | Une catégorie « approximative » sur les PER_KG — jamais utilisée pour le prix | B2-PR1 |
| A30 | **Un seul système de paiement à l'écran : le Payment Element** (carte + Apple Pay + Google Pay gérés par Stripe via `automatic_payment_methods`) — les radios maison Carte / Apple Pay / Google Pay sont supprimées, `paymentMethod` sort du brouillon (`DRAFT_VERSION` 5) | Moins de code, wallets natifs affichés automatiquement sur mobile, aucune promesse non tenue | Le look du formulaire de carte est celui de Stripe (thème mango appliqué) | B2-PR1 |

## 2bis.5 — Session B2-PR3 (front des transitions) — A31→A34

| # | Arbitrage | Pourquoi | Compromis | PR |
|---|---|---|---|---|
| A31 | **La prévisualisation d'annulation est SERVIE par la vue Shipper** (`cancellationPreview` : `refundCents`, `retentionCents`, `retentionPct`, `fullRefundUntil`, `currencyCode`), non nulle exactement quand `cancel` ∈ `allowedActions` — le front n'applique JAMAIS le barème ANN-01 lui-même (pas de moteur partagé, pas d'endpoint dédié) | « Le front reflète, ne décide jamais » ; le barème vit à UN endroit (`computeCancellationRefundCents`) ; zéro aller-retour supplémentaire avant la modale | La préviz est un instantané à la lecture — le montant réel est RECALCULÉ au cancel effectif (l'écart n'existe qu'à cheval sur le seuil J-2, assumé) | B2-PR3 |
| A32 | **Le textarea « détails » du refus disparaît** : `DeclineDealRequest` = la raison seule (5 valeurs É2, optionnelle) ; l'enum front s'aligne sur le contrat (`CATEGORY_NOT_CARRIED`, `TOO_HEAVY`, `PLACES_INCOMPATIBLE`, `TIMING`, `OTHER`) | Le contrat et la spec É2 ne connaissent que la raison ; une UI qui promet un champ jamais transmis est un mensonge | Le Voyageur ne peut pas nuancer son refus par du texte libre (le chat F le permettra) | B2-PR3 |
| A33 | **Le FakePaymentProvider ADOPTE les intents seedés** : tout id inconnu préfixé `pi_fake_seed_` est matérialisé `AUTHORIZED` à la première lecture ; le seed écrit `paymentProvider: FAKE` + `pi_fake_seed_<clé>` sur chaque booking et un `CarrierPage` COMPLETE/Stripe factice par Voyageur (gate D31 passant) | Le Fake est en mémoire : sans adoption, accept/decline/cancel sur un deal seedé finissaient en 409 `PAYMENT_STATE_CONFLICT` — les parcours B2 étaient injouables en dev | Un id non préfixé continue de jeter (les tests de conflit restent valides) ; montant adopté = 0 (les événements lisent le snapshot) | B2-PR3 |
| A34 | **Contrat `CreateBookingRequest` aligné sur le wizard** (diagnostic Stripe réel 01/09) : `recipient.email` devient **optionnel** (`nullish` ; spec É1 « pour notifications si renseigné » ; snapshot Prisma `String?`, jamais de chaîne vide figée) et `description` passe de min 10 à **min 5** (le plancher du wizard, spec « recommandée, pas bloquante ») — le front envoie `null` pour un email vide. En annexe : `backfill-reserved-kg.ts` matérialise `reservedKg: 0` sur les 27 Trips antérieurs à B2-PR1 (champ absent ⇒ le `updateMany` conditionnel CAP-01 ne matche pas ⇒ faux `CAPACITY_EXCEEDED`) | Un corps que le wizard laisse passer se faisait rejeter en 400 sans `details.code` → toast GENERIC après une carte AUTORISÉE (PI `requires_capture` orphelin, aucune écriture) — le contrat doit accepter tout ce que l'UI validée en recette produit | Défense runtime impossible pour le champ absent : Prisma refuse `isSet` sur un champ non-nullable et `NOT:{gt}` ne matche pas les documents sans le champ (vérifié) — le backfill idempotent est à rejouer sur tout environnement dont des Trips prédatent B2-PR1 | B2-PR3 |

## 2bis.6 — Session B2-PR4 (emails transactionnels) — A35→A36

| # | Arbitrage | Pourquoi | Compromis | PR |
|---|---|---|---|---|
| A35 | **Colonne email de la matrice A15 RECONSTITUÉE et gravée EN DATA** (`EMAIL_MATRIX`, totale sur les 17 clés comme `IN_APP_MATRIX` — tsc casse si une clé manque) : `requested` → CARRIER (deadline 24 h) · `payment_authorized` → SHIPPER (reçu) · `accepted` → SHIPPER · `declined` → SHIPPER · `expired` → SHIPPER · `cancelled` → SHIPPER toujours + CARRIER si `wasAccepted` · `refund_issued` → SHIPPER · `tracking_event` → JAMAIS (anti-spam, push seul) · `rating_revealed` → JAMAIS (in-app seul) · les 8 clés B3/B4/B5 (`picked_up`, `pickup_refused`, `code_regenerated`, `delivered`, `completed`, `payout_sent`, `disputed`, `rating_reminder`) = « à venir » : le gabarit s'implémente dans la MÊME PR que le premier writer de l'événement (miroir D30). Cette reconstitution VAUT gravure : le « handoff PR3 §4 » qui portait la colonne n'est pas versionné | La matrice était éclatée entre les `.meta` du contrat, la spec §10 et un document perdu — une matrice qu'on ne peut pas relire n'existe pas ; en data + totale, elle est exécutable et vérifiée par le compilateur | Un événement futur qui voudrait un email exigera d'éditer la matrice ET son gabarit (c'est voulu) | B2-PR4 |
| A36 | **Idempotence email : at-most-once PAR DESTINATAIRE.** Modèle `EmailDelivery` (unique `[eventId, userId]`), claim-first AVANT l'envoi (create PENDING ; P2002 = déjà claimé → jamais de renvoi, que le claim soit SENT, PENDING ou FAILED) ; échec d'envoi → `FAILED + lastError`, **jamais de throw** : l'email est best-effort, il ne bloque ni la partition ni la matérialisation in-app ni le PROCESSED du ConsumedEvent. SMTP non configuré (dev) = skip loggé sans claim | Le retraitement PENDING/FAILED du ConsumedEvent re-exécute tout le handler : sans marqueur par destinataire, un crash renverrait les emails. Perdre un email (SMTP down) se rattrape in-app ; un DOUBLON d'email « argent » (reçu, remboursement) érode la confiance — on choisit l'erreur la moins grave, comme A20 | Un email peut se perdre (crash entre claim et envoi, panne SMTP) — rows FAILED = rejeu manuel possible ; pas de retry automatique en v1 | B2-PR4 |

## 2bis.7 — Session B2-PR5 (tracker Expéditeur) — A37

| # | Arbitrage | Pourquoi | Compromis | PR |
|---|---|---|---|---|
| A37 | **Le tracker Expéditeur `/bookings/[id]` bascule sur `GET /deals/:id` dès B2, par un adapter qui PRODUIT le view-model EXISTANT** (`booking-tracker.adapter.ts`, patron du chap. 28) : les ~40 fichiers de vues É3/É4b/É6/É8/É9 ne bougent pas, le vocabulaire est absorbé à la frontière (statuts `PENDING→AWAITING_CARRIER`, `COMPLETED→VERIFIED` ; cents→euros d'affichage). Les champs que l'API ne sert pas ENCORE deviennent **optionnels et documentés** — carte bancaire (marque/4 derniers : un jour depuis Stripe), stats Voyageur (B5), code de livraison (B3, `deliveryCodeEncrypted`), ETA (durée de vol absente du snapshot) — avec dégradation explicite dans les 2 seuls composants concernés (`BookingCarrierCard`, `BookingPaymentBlock`). Le **fallback menteur** (statut inconnu → vue « accepté ») est remplacé par une vue d'état neutre (`BookingStatusNotice` : AWAITING_CARRIER, DECLINED, EXPIRED, CANCELLED, VERIFIED, DISPUTED) — la spec classait É3b/É10 hors périmètre v1, mais une URL directe ne doit jamais MENTIR. Les actions B3/B4 (régénérer le code, confirmer, litige) restent des mocks MARQUÉS qui basculeront avec leurs endpoints ; l'annulation Expéditrice reste dans Mes envois (A31). Les mocks de données (`booking-tracker.state.ts`, matrice d'URLs annexe A de la spec) sont SUPPRIMÉS | Brancher le réel sans réécrire l'UI : l'adapter est le seul point de vérité du mapping, et un écran qui affiche « accepté » pour un deal refusé est un mensonge pire qu'un écran sobre | Vues post-pickup atteignables seulement par seed tant que B3 n'écrit pas les statuts ; code affiché vide d'ici l'AES (assumé, seed seulement) | B2-PR5 |

---

## 2bis.8 — Sessions B3-PR1 (transport, serveur), B3-PR2 (front) B3-PR3 (boîte du Voyageur), B3-PR4 (page demande) B3-PR5 (typographie), fix ImageKit, B3-PR6 (visionneuse) et fix relay — A38→A49

| # | Arbitrage | Pourquoi | Compromis | PR |
|---|---|---|---|---|
| A38 | **Tentatives de code : le serveur compte, verrouille et REMET À ZÉRO.** `POST /deals/:id/deliver` compare en bcrypt ; mauvais code → `deliveryAttempts + 1`, écrit par `updateMany` CONDITIONNEL sur le compteur lu (deux saisies concurrentes = une seule comptée) ; au 3e échec → `deliveryLockedUntil = now + 15 min` ET `deliveryAttempts = 0` (fenêtre neuve après le verrou — le guard machine `deliveryAllowed` reste cohérent : lock d'abord, plafond ensuite). Réponses 409 : `DELIVERY_CODE_INVALID` (+ `attemptsLeft`), `DELIVERY_LOCKED` (+ `lockedUntil`), `DELIVERY_CODE_UNAVAILABLE` (PICKED_UP sans hash — seeds antérieurs à B3). Une tentative échouée n'écrit PAS d'événement outbox : ce n'est pas un changement d'état métier (le compteur est une donnée de garde, exposée dans la vue Carrier par `deliveryAttemptsLeft`/`deliveryLockedUntil`) | Le front n'est qu'indicatif (D4) : le compteur client du mock disparaît ; la remise à zéro évite un deal définitivement bloqué après un verrou | Un brute-force reste théoriquement possible à 3 essais / 15 min (10⁶ codes → ~35 ans) — suffisant ; la régénération par l'Expéditeur remet aussi le compteur à zéro | B3-PR1 (#95) |
| A39 | **L'undo 5 s des jalons de tracking est CLIENT SEUL** : le front n'appelle `POST /deals/:id/events` qu'À LA FIN de la fenêtre (le toast « Annuler » empêche l'appel) ; le serveur n'offre AUCUNE annulation d'un jalon confirmé. Séquence stricte (`canConfirmTrackingStep`), doublon ou saut ⇒ 409 `TRACKING_STEP_NOT_ALLOWED` ; écriture par `updateMany` conditionnel (`status: PICKED_UP` + `trackingEvents.none({ step })`) + `push` + outbox `booking.tracking_event` (in-app Expéditeur, jamais d'email — A35). Le « débounce serveur » de la spec §6 n'est PAS retenu | Un jalon confirmé notifie l'Expéditeur ; l'annuler côté serveur = un deuxième message (« finalement non ») pire que d'attendre 5 s avant d'envoyer ; l'API reste idempotente et sans état transitoire | Un onglet fermé pendant la fenêtre perd la confirmation (le Voyageur la refait — optionnelle par nature, A+B) | B3-PR1 (#95) |
| A40 | **Refus au pickup = annulation CARRIER sans pénalité + remboursement INTÉGRAL réel.** `POST /deals/:id/pickup/refuse` : raison parmi 5 (`PickupRefusalReason` : CONTENT_MISMATCH · SUSPICIOUS_CONTENT · OVERWEIGHT · BAD_PACKAGING · OTHER, optionnelle — le champ « détails » du mock disparaît, comme A32), champ `Booking.pickupRefusalReason`. L'argent est CAPTURÉ (D39) → `provider.refund(total)` AVANT la transaction (l'argent d'abord), puis ACCEPTED → CANCELLED, `closedBy: CARRIER`, kg restitués (CAP-02), outbox `booking.pickup_refused` + `booking.refund_issued`. Aucun `PENALIZE_CARRIER` (CNF-07 : le doute est un droit) — la transition `refusePickup` de la machine reste distincte de `cancel/CARRIER` (ANN-02) | La conformité D9 repose sur un Voyageur qui peut dire non sans coût ; le remboursement intégral est la contrepartie due à l'Expéditeur | Frais Stripe non restitués sur le remboursement (déjà assumé D39) ; un Voyageur de mauvaise foi peut refuser sans trace réputationnelle — le signalement SIG reste possible | B3-PR1 (#95) |
| A41 | **Matrice email A35 complétée pour les 4 clés B3** : `picked_up` → SHIPPER (« ton code est prêt dans ton suivi » + CTA — le code n'y est JAMAIS) · `pickup_refused` → SHIPPER (raison traduite, remboursement annoncé — `refund_issued` suit avec le montant) · `code_regenerated` → SHIPPER (email de sécurité : un nouveau code a été généré, compteur restant — sans le code) · `delivered` → SHIPPER (« 3 jours pour confirmer ou signaler », date J+4) ; le Voyageur livré = in-app seul (son email « versement » arrive avec `payout_sent`, B4). Garde-fou de rendu renforcé : aucun gabarit ne contient une suite de 6 chiffres ni la locution « code de livraison » | Miroir D30/A35 : chaque gabarit arrive avec le writer de son événement ; la règle « le code ne voyage jamais » devient testable sur le HTML (6 chiffres) et plus seulement sur un mot | 4 emails de plus à maintenir en 2 locales inline | B3-PR1 (#95) |
| A42 | **`CarrierPage.primaryAddressId` perd son `@unique` naïf** (annexe B3-PR1, découvert au rejeu du seed) : sur Mongo, l'index unique n'est pas sparse — deux pages Voyageur SANS adresse principale (`null`) collisionnent en P2002 (une vraie page `philanthrope…` à `null` bloquait la création de toute autre page sans adresse — le seed A33 n'avait donc jamais pu tourner sur cette base, et un DEUXIÈME vrai Voyageur sans adresse aurait été bloqué à l'onboarding). Schéma : `primaryAddressId String? @db.ObjectId` + `@@index([primaryAddressId])` simple ; côté `Address`, la relation devient une liste déclarative (`CarrierPage[]` — Prisma exige `@unique` pour un 1-1) ; l'unicité est garantie PAR CONSTRUCTION (le service crée l'adresse pour une page et une seule — `carrier.controller.ts`). `prisma db push` exécuté sur le dev Atlas (index unique remplacé par l'index simple). Pitfall CLAUDE.md « Nullable unique fields on Mongo collide on null » — 2e occurrence | Un index unique qui interdit le cas nominal (pas d'adresse) est un bug, pas une contrainte ; la règle métier (« une adresse appartient à une page ») ne se perd pas, elle change de gardien | Plus de garde base contre un partage d'adresse (impossible par les chemins d'écriture actuels) ; à rejouer : `npx prisma db push` sur tout environnement | B3-PR1 (#95) |
| A43 | **Front B3 (PR2) : le client porte l'attente, le serveur porte les compteurs.** ① Photos : `DealPickupClient` téléverse les `PickupPhotoDraft.file` UNE PAR UNE via `useImageKitUpload("/deals/pickup")` AVANT `POST /deals/:id/pickup` (premier échec = arrêt + toast, rien n'est envoyé au deal-service) — D42. ② Jalons : `TrackingSpotlight` gagne `onEventCommittedAction`, appelée À LA FIN de la fenêtre d'undo ; c'est là (et seulement là) que `POST /deals/:id/events` part ; échec → rollback de l'état optimiste + toast — A39. ③ Livraison : plus aucun compteur client — `attemptsUsed`/`lockedUntil` s'initialisent depuis la vue Carrier (`deliveryAttemptsLeft`, `deliveryLockedUntil`) et se mettent à jour depuis les `details` des 409 (`attemptsLeft`, `lockedUntil`) — A38. ④ Le textarea « détails » du refus au pickup disparaît (le contrat ne porte que la raison — miroir A32). ⑤ Régénération Expéditeur : `POST /deals/:id/code/regenerate` puis `invalidateQueries` (le code affiché vient TOUJOURS de `GET /deals/:id`, jamais du cache local) ; `deliveryCode.status` devient `VALIDATED` après livraison | « Le front reflète, ne décide jamais » (D4) appliqué aux trois mocks qui décidaient (compteur d'essais, code aléatoire local, undo simulé) | Vue DELIVERED persistante côté Voyageur non faite (spec §11 : hors périmètre v1 — `DealClosed` « Livraison validée » reste) ; pas de Jest user-ui : preuve = tsc + build + parcours seed | B3-PR2 (#96) |
| A44 | **Les demandes du Voyageur vivent dans « Mes trajets », pas dans un 3e onglet — et chaque notification est un lien.** Constat recette 02/09 : le Voyageur n'avait AUCUN chemin vers `/carrier/deals/:id` (la liste réelle « Mes trajets » ignorait les deals, la vitrine à deals vivait en mock sur `/dashboard/trips/preview`, la boîte de notifications n'était pas cliquable, le champ `pendingDemandsCount` attendu par le front n'était servi par personne). Décision : ① un endpoint `GET /me/deals` (vue Carrier, tous trajets, miroir de `/me/bookings`, filtre `?status`) — UNE lecture pour la liste, l'accueil et le badge de la sidebar, jamais un appel par trajet ; ② « Mes trajets » gagne la bande **« À traiter »** trans-trajets (répondre avant expiration · prise en charge · livraison — `deriveCarrierActions` du mock, sur données réelles) et, sous chaque trajet, ses deals réels (ligne cliquable : Expéditeur, colis, gains nets, statut) ; ③ l'accueil du dashboard montre la même bande ; ④ chaque notification devient un lien vers le deal (`/carrier/deals/:id` si le lecteur est le Voyageur du payload, `/bookings/:id` sinon) — le clic marque lu ET navigue ; ⑤ la page trajet propriétaire liste ses demandes. Le compteur `pendingDemandsCount` (jamais servi) disparaît au profit d'une dérivation front depuis `/me/deals`. Le mock `components/dashboard/trips` reste la vitrine `/preview` ; `TripDealRow` en est extrait pour servir les deux | Une demande est un fait du trajet (« mon Paris → Brazzaville a deux demandes »), pas un objet à ranger ailleurs ; un onglet de plus disperse ce que le Voyageur cherche au même endroit ; la notification qui ne mène nulle part est une impasse | `hasRated` (B5) inconnu → l'action « Noter » n'est pas dérivée (jamais inventée) ; RDV de pickup (`pickupMeetingAt`) absent du snapshot → la ligne « prise en charge » n'affiche pas d'heure | B3-PR3 (#98) |
| A45 | **Page « demande » du Voyageur (É2) : la recette réelle corrige quatre mensonges.** ① Les **photos déclarées** n'étaient JAMAIS envoyées (`photoUrls: []` figé dans `createDeal` depuis B2-PR1, en attendant un media-service) → le wizard les téléverse vers ImageKit (même mécanique que le pickup, D42, dossier `/bookings/declared`) **AVANT la confirmation carte** — un upload qui échoue arrête tout sans empreinte orpheline (leçon A34) — et le Voyageur les voit enfin sur la demande (`DealParcelPhotos`, déjà câblé). ② La colonne d'action (gains, couverture, Accepter/Refuser) était `hidden lg:block` alors que la bascule mobile se fait à 768 px : entre 768 et 1023 px, AUCUN bouton → grille et colonne visibles dès `md` (300 px, 340 px en `lg`). ③ **GAR-02** : « Assurance basique » / « Assurance 500 € » → « Garantie Yamba incluse » / « Protection étendue 500 € » — le mot « assurance » n'apparaît qu'après signature avec un assureur ; corrigé aussi dans le tracker Expéditeur, le wizard et la home. ④ **RGP-02** : « Téléphone communiqué après acceptation » → « à la prise en charge » (le destinataire n'est révélé qu'après PICKED_UP). En annexe : « Voir profil » loguait dans la console → `BookingCounterpart.publicSlug` (contrat + mapper) et lien réel vers `/u/[slug]`, bouton masqué s'il n'y a pas de slug | Ce que l'écran promet doit être ce que le serveur fait ; un CTA invisible sur tablette est une demande qui expire ; un mot juridiquement faux (« assurance ») engage Yamba | Les photos passent par le même compte ImageKit public (dette D42 inchangée) ; `publicSlug` peut être null (anciens comptes) → bouton absent, jamais un lien mort | B3-PR4 (#100) |
| A46 | **Échelle typographique unique des pages Deal (Voyageur) et zone morte 768–1023 px fermée partout.** Les cinq pages (`demande`, `accepté`, `pickup`, `tracking`, `deliver`) empilaient un H1 `text-3xl font-black` puis un H2 `text-2xl font-black` (« Mon Deal accepté » puis « Et maintenant ? ») — deux titres criards pour une page d'action. Échelle alignée sur le dashboard (« Mes trajets » : `text-xl font-medium`, page trajet : `22px semibold`) : **H1 = `text-[22px] font-semibold sm:text-2xl`, H2 de contenu = `text-[17px] font-semibold sm:text-lg`, H2 mobile = `17px semibold`, succès livraison = `22/26px bold`** ; les libellés de blocs restent en 11 px capitales ; les MONTANTS gardent leur graisse `black` (c'est l'information). Même correction A45 (colonne dès `md`) appliquée aux quatre autres grilles desktop et au squelette | La hiérarchie visuelle doit dire « voici la page, voici l'action » — pas « voici deux affiches » ; un seul seuil de bascule pour tout le module | Aucun | B3-PR5 (#102) |
| A47 | **Le paquet `imagekit@1.5.0` était un FOSSILE de 2016** (constructeur « ImageKit Id / API Key / API secret », dépôt gitlab `imagekit-sdks`) alors que `lib/imagekit.ts` et `upload.controller.ts` ciblent le SDK Node moderne (`publicKey/privateKey/urlEndpoint`, `getAuthenticationParameters`, `deleteFile`) — tout `GET /uploads/imagekit-auth` répondait 500 « Something went wrong », donc AUCUN upload (justificatifs, photos déclarées, photos de pickup) n'a jamais pu fonctionner dans cet environnement ; la déclaration maison `types/imagekit.d.ts` masquait l'écart au compilateur. Fix : `imagekit@6.0.0` (exact, racine + `apps/trip-service`), typage officiel du SDK (d.ts maison supprimée), preuve par script (`getAuthenticationParameters` OK). Piège annexe : une copie **imbriquée** `apps/trip-service/node_modules/imagekit` (versions divergentes entre le package du service et la racine) masquait la racine — `npm install` après alignement des deux `package.json` la déduplique (`npm ls imagekit` doit montrer « deduped »). Dans la même PR : `useImageKitUpload(folder, { maxSizeBytes, allowedMimeTypes })` + `uploadDetailed()` (code d'erreur rendu à l'appelant), photos de colis à **10 Mo** et WebP acceptés (spec §3.4), messages explicites (trop lourd / format) côté pickup et wizard | Un 500 traduit en « vérifie ta connexion » est un écran de fumée (chap. 32) ; une déclaration de types maison sur un paquet inconnu est un mensonge au compilateur | Version exacte (pas de caret) pour ce paquet ; la limite 10 Mo reste côté client (le serveur ne voit jamais l'octet — D42) | fix #104 |
| A48 | **Une visionneuse de photos partagée, des vignettes RÉELLES partout.** Les vues héritées des mocks affichaient les photos de colis comme des carrés de couleur à pictogramme (violet = déclarées, amber = pickup) sans image ni clic — dix blocs dupliqués des deux côtés (Voyageur : timeline, sidebar tracking, récap accepté, carte « déclaré » du pickup, sidebar livraison ; Expéditrice : récap accepté, sidebar en transit, timeline miroir, récap livraison, dossier litige, carte « photos de la prise en charge »). Nouveau : `components/shared/photos/PhotoThumbs` (image `object-cover`, liseré de la couleur du moment — le langage visuel §3.4 est conservé —, `max` avec « +N », dégradé + pictogramme SEULEMENT si l'image ne charge pas) et `PhotoLightbox` (plein écran, Échap/flèches, balayage tactile, compteur, scroll du fond verrouillé) extraite de `DealParcelPhotos`, i18n `common.lightbox.*`. Les dix blocs sont remplacés par une ligne | Une preuve photographique qu'on ne peut pas regarder n'est pas une preuve ; dix copies d'un même bloc = dix endroits à oublier | `<img>` natif (pas `next/image` : domaines ImageKit non configurés, pas d'optimisation serveur) ; pas de zoom/pinch dans la visionneuse | B3-PR6 (#106) |
| A49 | **Le relay outbox n'a JAMAIS relayé un événement écrit par l'application** (recette 02/09 : aucun email, aucune notification, 38 événements `attempts: 0`). Cause : `drainBatch` filtrait `publishedAt: null` alors que les writers réels (`applyBookingTransition`, `createBooking`) n'écrivaient pas le champ — sur Mongo, Prisma ne matche pas un champ ABSENT avec `null` (pitfall CLAUDE.md « readAt », **3e occurrence** après `reservedKg` A34). Les preuves B1-PR4/B2 passaient parce que `seed-outbox.ts` posait `publishedAt: null` explicitement. Fix double : le relay filtre `OR: [{ publishedAt: null }, { publishedAt: { isSet: false } }]` ET les deux writers posent `publishedAt: null` explicitement ; les 38 lignes orphelines (bookings effacés par les rejeux du seed) sont PARQUÉES (`publishedAt` posé + `lastError` « PARKED ») pour ne pas rejouer des emails vers des comptes seed ; les événements du deal réel de la recette (`requested`, `payment_authorized`, `accepted`, `picked_up`) partiront au redémarrage du deal-service. Deuxième cause, indépendante : le `.env` racine (lu par notification-service) n'a AUCUNE variable `SMTP_*` (elles ne vivent que dans `apps/trip-service/.env`) → `isEmailConfigured()` false → emails sautés silencieusement même une fois le relay réparé | Un pipeline « prouvé » sur des fixtures qui posent le champ n'a jamais été prouvé sur le chemin réel ; la règle « champ nullable : `OR null / isSet false` » vaut pour TOUT filtre, pas seulement `readAt` | Rejeu manuel des 38 orphelins impossible (parqués) — voulu | fix #109 |

## 2bis.9 — Session recette auth (03/09, `fix/auth-recette`) — A50→A54

| # | Arbitrage | Pourquoi | Compromis | PR |
|---|---|---|---|---|
| A50 | **Échecs OTP : barème par PALIERS de 5, le code est invalidé à chaque palier** (`otp-policy.ts`, pur et testé) : échecs 1→4 = essais restants annoncés, sans verrou ; 5e = code supprimé de Redis + verrou 1 min ; 10e = code supprimé + 30 min + email « activité suspecte » ; 15e et chaque suivant = 24 h. Le compteur cumulé vit 24 h et n'est JAMAIS remis à zéro par un renvoi. Remplace l'ancien barème (3e = 1 min, 4e = 5 min, 5e = 30 min, 6e = 24 h) | Un code à 6 chiffres se tape mal sur un téléphone : 24 h au 6e échec punissait la faute de frappe, pas l'attaque. Invalider le code rend le brute-force inutile (jamais plus de 5 essais sur un même code) tout en laissant l'utilisateur légitime redemander un code après 1 minute | Un utilisateur qui accumule 15 échecs sur 24 h est bloqué un jour : voulu (alerte envoyée dès le 10e) | #116 |
| A51 | **L'API parle anglais, le front ne montre JAMAIS son `message`** : chaque règle a un code stable dans `details` (types « safe » du middleware : `password` → `PASSWORD_TOO_SHORT`, `PASSWORD_CONTAINS_PERSONAL_INFO`… (`password-rules.ts`) ; `register` → `EMAIL_ALREADY_USED` ; `otp` → `OTP_INCORRECT` / `OTP_INVALIDATED` / `OTP_LOCKED` / `OTP_EXPIRED` + `attemptsLeft` / `lockUntilSeconds`). Le front (`lib/auth/auth-error-codes.ts`) construit la phrase dans la langue de l'interface et la pose sur LE champ concerné ; au submit local, il nomme le PREMIER critère non satisfait (`firstFailingCheck`) au lieu de « ne respecte pas tous les critères » | Recette 03/09 : un message générique ne dit pas quoi corriger, et les formulaires OTP affichaient le texte anglais brut de l'API sur une interface française | Les listes de règles front (`password-strength.ts`) et serveur (`password-rules.ts`) sont un miroir à maintenir à la main (même ordre, mêmes motifs) — un test serveur par règle le verrouille | #116 |
| A52 | **Aucune durée en dur dans un email** : les templates OTP reçoivent `expiresInMinutes` dérivé de `OTP_TTL_SECONDS` (10 min réelles alors que les templates disaient « 5 minutes » depuis le passage à 10). Sujets FR provisoires sans emoji (« Ton code d'activation Yamba », « Ton code de réinitialisation Yamba ») en attendant D44 | Le compte à rebours de l'écran (10:00) et l'email (5 min) se contredisaient : l'utilisateur ne sait plus à qui croire | Les sujets restent monolingues jusqu'à D44 | #116 |
| A53 | **La fenêtre d'inscription en attente passe de 15 à 30 min et est PROLONGÉE à chaque renvoi de code** (`refreshPendingRegistration` → `EXPIRE`) | Renvoi à la 10e minute : code valable jusqu'à la 20e, inscription morte à la 15e → « session expirée » avec un code valide en main | Un brouillon d'inscription (hash de mot de passe inclus) vit jusqu'à 30 min de plus en Redis — acceptable, la clé n'est adressable que par le token de vérification | #116 |
| A54 | **Un pool de visuels ne référence que des fichiers qui existent** : les deux photos JPG du panneau gauche (jamais livrées) sont retirées de `hero-visuals.ts` (2 chargements sur 5 affichaient le texte alternatif) ; l'icône œil des champs mot de passe se centre sur le champ, pas sur le conteneur qui inclut la marge (`top-1.5 bottom-0` au lieu de `inset-y-0`) | Recette au pixel : une image cassée au premier écran d'un nouvel utilisateur coûte plus qu'une photo en moins | Le pool est 100 % illustrations tant que les photos ne sont pas fournies dans la même PR que leur référence | #116 |

## 2bis.10 — Session `feat/email-locale` (D44 + D45 dans les emails) — A55→A57

| # | Arbitrage | Pourquoi | Compromis | PR |
|---|---|---|---|---|
| A55 | **Les emails sont des DONNÉES, le gabarit est UNE chaîne.** `packages/libs/email` gagne `EmailContent` (préheader, titre, salutation, paragraphes, bloc code, encadré, CTA, notes, raison, aide) et `LAYOUT_EJS`, une source EJS embarquée dans le bundle — rendue par `ejs.render`, jamais `renderFile` : plus aucun chemin dépendant du `cwd` (nx serve à la racine, `node dist/main.js` depuis `apps/<service>`, jest). Les 7 emails auth deviennent `AUTH_EMAILS: Record<SupportedLocale, AuthEmailDictionary>` — tsc CASSE si une langue oublie un email ; un test « miroir » le double. La liste des langues vit dans `packages/libs/api-contracts/src/locale.ts` (sans zod, alias front dédié `@packages/api-contracts/locale`) et next-intl la CONSOMME (`routing.ts`) au lieu de la redéclarer | Trois mailers, sept templates HTML de 100 lignes copiés-collés, « 5 minutes » en dur dans deux d'entre eux : la duplication est la cause de chaque incohérence de recette | Le gabarit est unique (un email « spécial » devra enrichir `EmailContent`, pas contourner) ; les templates trip/notification, déjà bilingues par ternaires, restent tels quels (dette : les migrer sur le gabarit quand une 3e langue arrive — leurs ternaires `fr ? :` ne passent pas à l'échelle) | `feat/email-locale` |
| A56 | **Quelle langue pour quel email** : sans compte (OTP d'inscription, mot de passe oublié, alerte sécurité) → la langue de la REQUÊTE (`x-locale` posé par le client API depuis le premier segment d'URL, sinon `Accept-Language` — `localeFromHeaders`) ; avec compte → `User.preferredLocale` du DESTINATAIRE, initialisée à l'inscription depuis la requête et enregistrée IMMÉDIATEMENT par `PATCH /auth/me/locale` quand un utilisateur connecté bascule FR/EN dans le header (best-effort : la navigation ne dépend pas de l'API). Un `PATCH` avec une locale hors liste → 400 `LOCALE_UNSUPPORTED` | La personne qui reçoit l'email n'est pas forcément celle qui a agi : l'acceptation d'un Voyageur EN doit produire un email FR pour une Expéditrice FR | Une locale par compte, pas par appareil ; le rendu serveur (SSR) n'envoie pas `x-locale` (pas de `window`) — les appels SSR n'envoient pas d'email, acceptable | `feat/email-locale` |
| A57 | **La contrepartie a un prénom (D45).** Le dispatcher notification-service charge les DEUX parties de l'événement (destinataire : email + locale ; contrepartie : prénom) en une seule requête et passe `counterpartFirstName` aux gabarits ; « le Voyageur a accepté » devient « Thomas a accepté ». Compte effacé (RGPD) → `null` → les gabarits replient sur le mot de rôle (`${counterpartFirstName \|\| "ton Voyageur"}`), jamais « null » à l'écran | Un email qui dit « le Voyageur » alors que l'app affiche « Thomas » partout est une rupture de confiance (D45) | Le prénom vient d'une jointure à l'envoi (D41), pas du payload : un événement rejoué après renommage montre le prénom actuel — voulu | `feat/email-locale` |

## 2bis.11 — Session `feat/booking-auth-modal` (porte de réservation en modale, retour après connexion) — A58

| # | Arbitrage | Pourquoi | Compromis | PR |
|---|---|---|---|---|
| A58 | **La porte « Connecte-toi pour réserver » s'ouvre SUR la page du trajet** (`BookingAuthGateModal` : dialogue centré sur desktop, feuille du bas sur mobile, ESC / fond / « Plus tard » ferment, focus sur l'action principale, scroll verrouillé) quand un visiteur non connecté clique « Réserver » (carte desktop ET barre mobile) ; la page `/trips/:id/book` GARDE sa porte pleine page pour l'accès direct par URL — le serveur reste seul juge (CNF-05). Utilisateur en cours de chargement : on pousse vers `/book` et la page tranche. **Retour après connexion = le wizard** (`/trips/:id/book`), l'intention était de réserver. **Le header porte le retour** : « Connexion » et « Créer un compte » (barre desktop, palette de commandes, feuille mobile) transmettent la page courante en `?redirect=` (`loginHrefFor` / `registerHrefFor`), sauf depuis les pages d'authentification (boucle) et l'accueil (retour par défaut) | Une page blanche « connecte-toi » fait perdre le contexte (prix, trajet) au moment exact où l'utilisateur est décidé ; un lien « Connexion » qui ramène à l'accueil fait perdre la page en cours | La modale n'est pas un composant générique du design system (pas de `ui/Dialog` aujourd'hui) — troisième dialogue maison après le lightbox et la feuille tarifs : candidat à factoriser à la 4e occurrence | `feat/booking-auth-modal` |

## 2bis.12 — Session `feat/trip-favorites` (D46) — A59

| # | Arbitrage | Pourquoi | Compromis | PR |
|---|---|---|---|---|
| A59 | **Le cœur reflète, le serveur décide, le cache bascule d'abord.** ① `isFavorite` est posé par les CONTROLLERS après mapping (`markFavorites`), jamais par `mapTripToYambaResult` (qui reste pur et sans utilisateur) ; `/trips/search` et `/trips/:id/public` passent sous `isOptionallyAuthenticated` (même réponse pour un visiteur, `false` partout, zéro requête en plus). ② `useToggleFavorite` bascule de façon optimiste les TROIS caches qui montrent un trajet (`["public-trip", id]`, toutes les pages infinies `["trips-search"]`, la liste `["favorites"]`) et remet TOUT en place en erreur ; les refus serveur (`OWN_TRIP`, `TRIP_NOT_FAVORITABLE`) sont traduits depuis `details.code`, jamais depuis le message. ③ La liste « Mes favoris » réutilise les cartes de recherche (`YambaTripResult`, `isFavorite = true`) : une seule mise en page à maintenir. ④ Sur la fiche, le cœur est masqué pour le créateur ; sur les cartes (pas d'identité du créateur dans le DTO), c'est le 403 serveur qui tranche, le cœur revient et un toast explique. ⑤ Le générateur OpenAPI partage un registre Zod global : ajouter `TripFavoriteState` fait bouger les TROIS `openapi.json` (deal, notification, trip) — attendu, commités ensemble | « Le front reflète, ne décide jamais » (D4) : une règle métier en double (front + serveur) finit par diverger ; l'optimisme sur plusieurs caches évite le cœur qui « clignote » entre la carte et la fiche | Sur les cartes, un clic sur son propre trajet fait un aller-retour serveur (rare : ses propres trajets apparaissent peu dans SA recherche) ; `isFavorite` optionnel dans les contrats pour ne pas casser les consommateurs existants | `feat/trip-favorites` |

## 2bis.13 — Session `feat/auth-pages-ux` (D45 sur les pages auth, porte « Partager un trajet ») — A60

| # | Arbitrage | Pourquoi | Compromis | PR |
|---|---|---|---|---|
| A60 | **Une seule porte d'identité, habillée par l'action.** `AuthGateModal` (générique : `title`, `subtitle`, `redirect`, boutons communs `common.authGate`) remplace la modale dédiée à la réservation, qui n'en est plus qu'un habillage (`BookingAuthGateModal`). « Partager un trajet » (header desktop, mobile, compact) l'ouvre pour un visiteur avec le message « Connecte-toi pour partager un trajet » et retour sur `/trips/create` ; le **cœur favori** (cartes de recherche, fiche) l'ouvre avec « Connecte-toi pour enregistrer un favori » et retour sur la page courante — plus aucune redirection sèche vers `/login`. La modale est rendue par PORTAIL dans `<body>` : déclenchée depuis une carte-lien, rendue dans l'ancre, chaque clic aurait navigué. Même règle que A58 : utilisateur inconnu (chargement) → la page cible tranche. **D45 appliqué aux pages auth** : les six formulaires (connexion, inscription, deux OTP, mot de passe oublié, réinitialisation) et `auth.json` passent au tutoiement ; « compte verrouillé » devient « saisie bloquée » (miroir A50) ; le panneau gauche remplace les chiffres inventés (12k+ / 48h / 4.8) et le témoignage fictif par trois promesses PRODUIT vraies (compte vérifié — CNF-05 ; débité seulement à l'acceptation — capture manuelle D11 ; Garantie Yamba : code de remise, photos, suivi). Mobile : champs de saisie à 16 px sous `sm` (fin du zoom iOS au focus) | Deux modales quasi identiques auraient divergé ; une porte doit dire POURQUOI on demande l'identité, avec les mots de l'action ; des chiffres faux sur une bêta sont un risque de crédibilité et un risque juridique | Le vocabulaire du rôle (Yamber / Tripper / Voyageur) reste OUVERT (D45) : « Deviens Yamber » conservé ; CTA collant au-dessus du clavier non fait (les formulaires sont courts, le bouton reste visible) | `feat/auth-pages-ux` |

## 2bis.14 — Session `feat/auth-google` (D47) — A61

| # | Arbitrage | Pourquoi | Compromis | PR |
|---|---|---|---|---|
| A61 | **La logique Google est PURE et injectée.** `google-auth.service.ts` reçoit ses dépendances (`verify`, `prisma`, `generatePublicSlug`, `recordConsents`, `normalizeEmail`) : six tests couvrent 503 / 401 / 403, identité connue, rattachement par email normalisé, `CONSENT_REQUIRED` sans création, création avec consentement (User sans mot de passe, `preferredLocale` depuis la requête — D44, `ConsentLog` dans la transaction). `google-token.verifier.ts` est le SEUL fichier qui parle à Google (`null` sans client ID). Côté front, `useGoogleIdentity` charge le script GIS une fois et rend le bouton OFFICIEL (seul chemin fiable vers un `id_token` au clic : `prompt()` One Tap est soumis à des cooldowns) ; `GoogleSignInButton` porte l'écran « Finalise ton compte » (CGU + confidentialité, mêmes versions `LEGAL_VERSIONS` que l'inscription) et rejoue le MÊME jeton avec le consentement. Les erreurs sont traduites depuis `details.code` (`oauth` ajouté aux types safe). `issueSession` est extrait de `loginUser` pour que Google et le mot de passe ouvrent la même session D27 | Tester un flux OAuth sans réseau impose l'injection ; un `id_token` reste valable quelques minutes, assez pour l'aller-retour du consentement, sans stocker de brouillon serveur | Le bouton officiel ne suit pas exactement la charte (thème `outline`, forme pilule) ; « Rester connecté » n'est pas proposé sur le chemin Google (session standard) | `feat/auth-google` |

## 2bis.15 — Session `fix/session-remember-default` (D27 vécu en recette) — A62

| # | Arbitrage | Pourquoi | Compromis | PR |
|---|---|---|---|---|
| A62 | **« Rester connecté » est DÉCOCHÉ par défaut, et la case dit ce qu'elle fait.** Recette 03/09 : « l'utilisateur reste indéfiniment connecté ». Le serveur applique bien D27 (standard : 60 min d'inactivité, 7 j max ; rememberMe : 7 j d'inactivité, 30 j max ; rotation qui ne repousse jamais le plafond ; cookie de session sans `maxAge` hors rememberMe) — mais la case était cochée par défaut, donc chaque connexion de recette était une session « 7 jours ». Correctif : `remember: false` par défaut, libellé « Rester connecté sur cet appareil » + aide « Coché : 7 jours sans activité. Sinon : déconnexion après 60 minutes sans activité. » Aucune valeur de D27 ne change | Un défaut coché transforme un choix de confort en comportement par défaut invisible ; l'utilisateur doit comprendre la durée qu'il accepte | Les testeurs devront cocher la case pour les recettes longues ; le chemin Google ouvre une session standard (pas de case) | `fix/session-remember-default` |

## 2bis.16 — Session `feat/auth-gate-inline-login` (connexion DANS la porte) — A63

| # | Arbitrage | Pourquoi | Compromis | PR |
|---|---|---|---|---|
| A63 | **La porte d'identité contient le formulaire de connexion, pas des boutons vers lui.** `AuthGateModal` embarque `LoginForm` en variante `modal` (mêmes champs, mêmes erreurs traduites, même bouton Google officiel, Facebook inerte, lien « Inscris-toi » avec retour) : ZÉRO redirection pour se connecter au moment d'un geste. Après connexion (mot de passe ou Google), `onSignedInAction` reprend le geste sans quitter la page (le cœur favori met le trajet en favori immédiatement) ; sans reprise fournie, navigation vers `redirect` (réserver → wizard, partager → création). Le formulaire de connexion reste UN composant (`LoginForm`, variante `page` | `modal`) — pas de copie | Un visiteur décidé ne doit pas changer de page pour prouver qui il est ; deux formulaires de connexion divergeraient (A60 : généraliser à la 2e occurrence) | L'inscription reste une page (formulaire long, OTP) ; les `id` des champs (`email`, `password`) sont partagés page/modale — jamais les deux à la fois | `feat/auth-gate-inline-login` |

---

## 2bis.17 — Session `feat/follow-auth-gate` (porte d'identité sur « Suivre ») — A64

| # | Arbitrage | Pourquoi | Compromis | PR |
|---|---|---|---|---|
| A64 | **Le bouton « Suivre » du profil public passe par la porte d'identité en modale, comme le cœur favori.** Le visiteur non connecté qui clique « Suivre » sur `/u/:slug` voit `AuthGateModal` « Connecte-toi pour suivre {prénom} » (formulaire de connexion embarqué, A63) ; après connexion, `onSignedInAction` applique le suivi immédiatement (`useFollowUser`, optimiste, `notifyNextTrip: true`) et le profil est rechargé côté connecté (`isFollowedByMe`, `isOwnProfile`). La redirection `/login?redirect=/u/:slug` disparaît — c'était la dernière porte d'identité par redirection sur un geste. Le serveur reste seul juge (401 sans session, refus de se suivre soi-même) | Troisième occurrence du même geste (réserver, favori, suivre) : la règle A60 « généraliser à la 2e occurrence » s'applique ; un visiteur qui suit un Voyageur veut rester sur son profil | La modale n'est montée que hors « propre profil » ; le libellé porte le prénom (variable i18n `{firstName}`) — à revoir si la porte devient un composant sans contexte | `feat/follow-auth-gate` |

---

## 2bis.18 — Session B4-PR1 `feat/b4-payout-server` (argent sortant, serveur) — A65→A68

| # | Arbitrage | Pourquoi | Compromis | PR |
|---|---|---|---|---|
| A65 | **Le versement est un exécuteur à part (`deal-payout.service.ts`), appelé par `confirmEarly`, par le cron J+4 et par le rejeu des FAILED** — jamais dans la machine, jamais dans la transaction de transition (D49). Sans compte Connect (`CarrierPage.stripeAccountId` absent ou `stripePayoutsEnabled` faux) → `FAILED` avec raison `CARRIER_ACCOUNT_NOT_READY`, rejoué au cron suivant. Le Fake transfère toujours (id `tr_fake_…`) | Un seul chemin pour tous les déclencheurs ; l'échec est un état, pas une exception | Le rejeu est borné par `payoutAttempts` (10) puis reste FAILED, visible en base (admin C) | `feat/b4-payout-server` |
| A66 | **Le cron de versement (`*/5`, `BOOKING_PAYOUT_CRON_ENABLED`) fait deux passes** : (1) `DELIVERED` + `payoutDueAt <= now` → `autoComplete` (SYSTEM) puis versement ; (2) `COMPLETED` + `payoutStatus = FAILED` + `payoutAttempts < 10` → rejeu du versement. Même patron anti-chevauchement que l'expiration 24 h | Le J+4 et le rejeu partagent l'horloge, la fournée et le journal | Deux requêtes par tick | `feat/b4-payout-server` |
| A67 | **`POST /deals/:id/confirm` répond après le versement tenté** (`payoutStatus` dans la réponse) — l'Expéditeur voit « libéré » ou « en cours » dès son clic ; Stripe est appelé en ligne (≈ 1 s), jamais différé à un job | Le geste « tout va bien » doit rendre un état lisible ; un job asynchrone rendrait un « en cours » systématique sans raison | Latence Stripe sur la requête (bornée par le SDK) | `feat/b4-payout-server` |
| A69 | **`chargeId` est stocké à la capture** (`PaymentAuthorization.chargeId`, Stripe `latest_charge`, Fake `ch_fake_…`) et passé en `source_transaction` du transfert (D49). Sans `chargeId` (deals capturés avant B4) : transfert sans rattachement, comportement historique | Un transfert rattaché à sa charge ne dépend pas du solde disponible de la plateforme | Un champ de plus sur le Booking | `feat/b4-payout-server` |
| A70 | **Rappel J+3 = troisième passe du cron de versement** : `DELIVERED`, `payoutDueAt - 24 h <= now`, `verificationReminderSentAt` absent → outbox `booking.verification_reminder` + marquage (jamais deux rappels). Même fournée, même horloge UTC (FUS-03) | Un rappel la veille de l'échéance évite le « je n'ai pas vu passer les 4 jours » ; la clé d'événement rend le rappel observable (in-app + email) | Une clé d'événement de plus (18) ; le rappel n'existe pas pour les deals déjà DELIVERED avant B4 dont l'échéance est passée | `feat/b4-payout-server` |
| A68 | **Les DTO role-scoped exposent `payoutStatus` aux deux rôles** (Expéditeur : « le paiement du Voyageur est libéré / en cours / gelé », Voyageur : « versement envoyé le … / en cours / gelé ») et `transferId` à personne. Le `Dispute` (description, photos, catégorie) est servi à l'Expéditeur seul via `GET /deals/:id` (`dispute` présent uniquement dans `ShipperBookingView`) ; le Voyageur voit le ticket, la date et la **catégorie** (décision utilisateur 03/09 : ne pas le laisser dans le noir), jamais la description ni les photos (il donnera sa version en médiation, chantier C) | Whitelists strictes (règle non négociable) ; le dossier de litige n'est pas une pièce contradictoire avant la médiation | Le Voyageur ne connaît pas le détail du signalement en v1 | `feat/b4-payout-server` |

---

## 2bis.19 — Session B4-PR2 `feat/b4-shipper-front` (argent sortant, tracker Expéditeur) — A71→A74

| # | Arbitrage | Pourquoi | Compromis | PR |
|---|---|---|---|---|
| A71 | **La confirmation anticipée est un bouton SECONDAIRE** (contour, pas de couleur pleine) avec confirmation en ligne « définitif » et le conseil « demande au destinataire d'ouvrir le colis avant de confirmer » ; « Signaler » reste visible à côté. Après confirmation, le suivi bascule sur la vraie vue « Envoi terminé » (COMPLETED), pas sur une carte d'état | Décisions utilisateur 03/09 (1A, 2A) : un clic trop rapide retire un droit ; l'écran de fin doit dire le sort de l'argent (RG-T-03) sans jamais exposer un échec de versement du Voyageur à l'Expéditeur | La carte « paiement libéré » de la maquette disparaît au profit de la vue COMPLETED | `feat/b4-shipper-front` |
| A72 | **`disputeOpensAt` est SERVI par l'API** (vue Expéditeur : `trip.departureAt + 48 h` en PICKED_UP, null sinon) — le front l'affiche (« possible à partir du … ») et désactive le bouton tant que `dispute ∉ allowedActions` ; il ne calcule jamais la date | « Le front reflète, ne décide jamais » ; la constante 48 h n'a qu'un propriétaire (la machine) | Un champ de plus dans le DTO Expéditeur | `feat/b4-shipper-front` |
| A73 | **Un seul formulaire de signalement** : depuis le transit, le motif est verrouillé sur « non livré » avec un bandeau explicatif (5A) ; les photos partent à la sélection vers `deals/dispute/` (upload direct signé D42) et l'envoi n'est possible que quand toutes sont en ligne (6A) ; un accès direct à `/report` sans droit (`dispute ∉ allowedActions`) renvoie au suivi avec un message (7A) | Une copie du formulaire divergerait (A60) ; une photo qui ne part pas doit se voir AVANT l'envoi (RG-P-13) ; une page « impossible » n'apprend rien de plus qu'un retour au suivi | Le formulaire porte une variante d'état de plus | `feat/b4-shipper-front` |
| A74 | **La vue DISPUTED est un récapitulatif du dossier** (ticket, motif, description, photos, solution souhaitée, date), les 4 étapes du processus et l'adresse support avec le numéro à rappeler (3A) | L'Expéditeur doit retrouver ce qu'il a déclaré et savoir ce qui suit ; la médiation (chantier C) lira le même dossier | Aucun échange dans l'app avant le chantier C | `feat/b4-shipper-front` |

---

## 2bis.20 — Session B4-PR3 `feat/b4-carrier-front` (argent sortant, côté Voyageur) — A75→A78

| # | Arbitrage | Pourquoi | Compromis | PR |
|---|---|---|---|---|
| A75 | **La cause d'un versement bloqué est servie GROSSIÈREMENT au Voyageur** : `CarrierBookingView.payoutBlocker ∈ { ACCOUNT_NOT_READY, RETRYING, null }` dérivé de `payoutStatus = FAILED` + préfixe de `payoutFailureReason` — jamais le message Stripe. Compte non prêt → « finalise ton compte Stripe » + bouton vers l'onboarding, sur le deal ET en bandeau « Mes trajets » ; erreur fournisseur → « en cours de traitement, rien à faire » (décision utilisateur 2A) | Un CTA doit être juste (une erreur de solde ne s'arrange pas dans l'onboarding) ; le détail technique reste à Yamba | Un champ de plus, dérivé (pas stocké) | `feat/b4-carrier-front` |
| A76 | **Photo optionnelle à la remise** (décision 3A) : `POST /deals/:id/deliver` accepte `photoUrls` (≤ 2, upload direct D42 vers `deals/delivery/`), stockées dans `Booking.deliveryPhotoUrls`, servies aux DEUX vues (récap Expéditeur, dossier médiation). Jamais obligatoire : le code prouve la remise, la photo prouve l'état — c'est l'assurance du Voyageur | Un litige « endommagé » ne se juge aujourd'hui que sur les photos de prise en charge ; une photo colis fermé dans les mains du destinataire protège le Voyageur | Friction facultative au moment du code ; pas de photo = comportement actuel | `feat/b4-carrier-front` |
| A77 | **Le portefeuille Voyageur reste une maquette dans cette PR** (décision 4B) : l'état réel du versement est porté par la page du deal et les lignes « Mes trajets » (`payoutStatus`, `payoutSentAt`, `payoutBlocker`, `disputeTicket` ajoutés à la liste) ; le vrai portefeuille (à venir / en attente / envoyés) est une PR dédiée | Livrer d'abord l'état là où le Voyageur agit ; un portefeuille exige des agrégats qui méritent leur propre lot | La section Finances reste visuelle | `feat/b4-carrier-front` |
| A78 | **Le Voyageur d'un deal disputé peut « donner sa version » par email** (bouton `mailto:` support, ticket en objet — décision 5A) en attendant la médiation dans l'app (chantier C) ; l'écran lui montre le ticket, la catégorie, « paiement en attente — ce n'est pas une décision » et les étapes. Copie honnête partout : « parti vers ton compte, sur ton compte bancaire sous 2 à 7 jours » remplace « virement Stripe automatique » / « virés le {date} » ; le bouton « Noter » de l'écran de succès disparaît jusqu'à B5 | Symétrie avec l'Expéditeur ; ne pas laisser le Voyageur sans voix ni sans repère temporel honnête | Un échange par email, hors app, jusqu'au chantier C | `feat/b4-carrier-front` |

---

## 2bis.21 — Session `feat/b4-late-cancel-payout` (retenue ANN-01 au Voyageur, D50) — A79→A82

| # | Arbitrage | Pourquoi | Compromis | PR |
|---|---|---|---|---|
| A79 | **Compensation = `round(retenue × transportCents / totalShipperCents)`**, retenue = total − remboursement ANN-01 (50 %), arrondi au centime, sans minimum, reste d'arrondi à Yamba (décisions 1A, 5A). La prime de protection vaut 0 aujourd'hui ; **quand D22 sera réel, la prime sera remboursée à 100 % et le prorata calculé hors prime** (gravé ici, à appliquer avec D22) | Un seul calcul, lisible, sur des cents entiers ; ne pas inventer un traitement de prime pour un flux qui n'existe pas encore | Tant que D22 n'est pas là, le prorata inclut une prime nulle | `feat/b4-late-cancel-payout` |
| A80 | **La compensation part IMMÉDIATEMENT à l'annulation** (décision 2A), après le remboursement de l'Expéditeur : la transition CANCELLED pose `payoutStatus = PENDING` + `payoutAmountCents` + `retentionCents` + `retentionDisposition = CARRIER`, puis l'exécuteur unique de versement (A65) transfère en ligne ; échec → FAILED rejoué par le cron (passe 2 étendue aux CANCELLED). Rien de rétroactif (4A) : les deals annulés avant cette PR n'ont pas de `payoutStatus`, le cron les ignore | Un seul chemin d'argent sortant (D49) ; l'argent est capturé, rien n'attend de vérification | Un transfert `source_transaction` sur une charge partiellement remboursée : Stripe accepte tant que le montant ≤ charge − remboursements (50 % ≥ part nette) | `feat/b4-late-cancel-payout` |
| A81 | **Annulation APRÈS le départ sans prise en charge : pas de compensation automatique** (décision 3A) — `retentionDisposition = HELD_FOR_MEDIATION`, `retentionCents` tracé, `payoutStatus` absent ; le chantier C tranchera (Voyageur absent ou Expéditeur absent ?) | Compenser automatiquement récompenserait un Voyageur qui ne s'est pas présenté ; la machine ne sait pas qui a fait défaut | Le Voyageur d'une annulation post-départ attend la médiation | `feat/b4-late-cancel-payout` |
| A82 | **Même événement `booking.payout_sent` avec `reason = LATE_CANCELLATION`** (défaut `DELIVERY`), email Voyageur en variante « compensation d'annulation », ligne « Mes trajets » et page du deal annulé avec l'état du versement (montant de la compensation, pas le net) ; côté Expéditeur, l'aperçu d'annulation et l'email de remboursement disent que la retenue revient au Voyageur (décision 6A) | Un moment d'argent laisse un email (RG-N-01) ; l'Expéditeur doit savoir où va sa retenue ; une clé d'événement de plus n'apporterait rien | L'email `refund_issued` (gabarit EJS historique) reçoit une ligne conditionnelle, pas une migration | `feat/b4-late-cancel-payout` |

---

## 2bis.22 — Session `feat/wallet` (Finances : portefeuille Voyageur + paiements Expéditeur) — A83→A84

| # | Arbitrage | Pourquoi | Compromis | PR |
|---|---|---|---|---|
| A83 | **Les totaux d'argent sont calculés SERVEUR** : `GET /me/wallet` (deal-service, `wallet.service.ts` pur et testé) renvoie, pour les DEUX rôles de l'appelant (décision 1A), des agrégats en cents et des lignes à état de vocabulaire produit — Voyageur : UPCOMING · PENDING · BLOCKED · FROZEN · SENT · HELD ; Expéditeur : AUTHORIZED · HELD · RELEASED · RELEASED_NO_CHARGE · REFUNDED · PARTIALLY_REFUNDED. Le front affiche, ne recalcule jamais (décision 2A). Trois cartes + une liste unique triée par date, badge par état (4A). `ShipperBookingView` sert désormais `capturedAt`, `refundedAt`, `refundAmountCents` (jamais au Voyageur, A13). Section passée sous next-intl (`finances`, 5A) | Un total d'argent doit être auditable et identique partout ; c'est le point d'entrée du solde Stripe plus tard ; deux écrans calculant chacun divergeraient | Un endpoint de plus, un proxy gateway de plus ; pas de pagination (volumes v1) | `feat/wallet` |
| A84 | **« Voir mes virements sur Stripe »** : `POST /carrier/stripe/dashboard-link` (auth-service, `accounts.createLoginLink`, lien à usage unique jamais stocké) ouvre le tableau de bord Stripe Express du Voyageur — dates d'arrivée, RIB, historique (décision 3A). Sans compte Stripe : 409 `STRIPE_ACCOUNT_MISSING`, le front dit « finalise d'abord ton compte » | La date d'arrivée sur le compte bancaire n'est connue que de Stripe ; ne pas la promettre, y renvoyer | Le Voyageur quitte l'app pour ce détail ; contrôleur mince sans test unitaire (appel Stripe direct) | `feat/wallet` |

---

## 2bis.23 — Session `fix/tracking-absent-composite` (recette réelle 03/09) — A85

| # | Arbitrage | Pourquoi | Compromis | PR |
|---|---|---|---|---|
| A85 | **Les listes d'un Booking EXISTENT dès la création, et les gardes de concurrence ne portent jamais sur une liste.** Pitfall Prisma+Mongo, 4e occurrence : `trackingEvents: { none: { step } }` ne matchait AUCUN document réel (le champ était absent — le seed le posait, `booking-request.ts` non) → chaque jalon de voyage répondait 409 en recette réelle. Correctif : le writer crée `trackingEvents: []` et `deliveryPhotoUrls: []` ; la garde du jalon devient un **verrou optimiste sur `updatedAt`** (`BOOKING_WRITE_SELECT` l'inclut) ; `repair-absent-lists.ts` pose `[]` sur les documents existants via `$exists` (Prisma ne sait pas exprimer « liste absente ») — joué le 03/09 : 3 + 23 documents | Aucun filtre Prisma (`none`, `some`, `isEmpty`, `equals: []`) ne matche une liste absente ; une règle de concurrence doit tenir quel que soit l'état du document | Le verrou `updatedAt` refuse aussi une écriture concurrente sans rapport (relecture) — acceptable sur un geste rare | `fix/tracking-absent-composite` |

---

## 2bis.24 — Session `chore/b4-hardening` (durcissement de l'argent sortant) — A86→A89

| # | Arbitrage | Pourquoi | Compromis | PR |
|---|---|---|---|---|
| A86 | **Plafond de rejeu porté à 100 essais** (décision utilisateur 03/09, 1B), toutes causes confondues ; au-delà, le versement reste FAILED, visible dans le récapitulatif support (A88). Le rejeu déclenché par `account.updated` (A87) IGNORE le plafond | 100 × 5 min ≈ 8 h de rejeu automatique ; le cas « compte Stripe finalisé le lendemain » est rattrapé par le webhook, pas par le compteur | Une erreur fournisseur persistante n'est plus rejouée après 8 h : le support la voit le lendemain | `chore/b4-hardening` |
| A87 | **Un seul URL de webhook, deux endpoints Stripe** (plateforme + comptes connectés, `STRIPE_WEBHOOK_SECRET` + `STRIPE_CONNECT_WEBHOOK_SECRET`, chaque signature essayée) — décision 2A. `account.updated` → drapeaux `CarrierPage` mis à jour sans repasser par l'onboarding + rejeu immédiat des versements FAILED du Voyageur si `payouts_enabled` · `transfer.reversed` → `payoutStatus = REVERSED` (`PROVIDER_REVERSED`), JAMAIS renvoyé automatiquement (arbitrage admin) · `payout.failed` (compte connecté) → le Voyageur est prévenu (in-app `carrier.payout_failed` + email « vérifie ton RIB »), jamais le message brut de la banque | Un compte prêt doit payer sans que le Voyageur clique ; un renversement est une anomalie à examiner, pas à réparer en boucle ; un virement bancaire refusé est invisible sans ce webhook | Deux exceptions au patron outbox (événement de COMPTE, pas de deal) : le deal-service écrit lui-même la notification et l'email (`ops-notify.service.ts`) | `chore/b4-hardening` |
| A88 | **Récapitulatif quotidien au support** (décision 3A) : cron `0 8 * * *` (`OPS_DIGEST_CRON_ENABLED`) → email `SUPPORT_EMAIL` avec les versements FAILED depuis > 24 h, les transferts renversés et les retenues « à arbitrer » (liens vers les deals) ; rien à dire → pas d'email | Filet de sécurité avant l'admin (chantier C) : aucun argent ne reste bloqué en silence | Un email de plus par jour ouvré ; le support agit à la main jusqu'au chantier C | `chore/b4-hardening` |
| A89 | **Session expirée = UNE fenêtre de connexion, pas un toast par écran** (décision 4A) : le client API émet `yamba:session-expired` quand le rafraîchissement échoue, `SessionExpiredGate` (monté dans les providers) ouvre `AuthGateModal` « Ta session a expiré » sur la page courante ; après connexion, le circuit breaker est réarmé et le cache invalidé — l'utilisateur refait son geste. Cause probable de l'« erreur » de régénération du code en recette (A62 : 60 min sans activité) | Un 401 silencieux transformé en « Erreur, réessaye » fait croire à un bug ; la porte d'identité existe déjà (A63) | Sur une page publique, la fenêtre s'ouvre aussi si une requête authentifiée échoue (rare : `requireAuth` seulement) | `chore/b4-hardening` |

---

## 2bis.25 — Session `chore/vocabulaire-voyageur` (D45 : le nom du rôle) — A90
| # | Arbitrage | Pourquoi | Compromis | PR |
|---|---|---|---|---|
| A90 | **« Voyageur » partout dans l'interface FR (décision utilisateur 03/09), « Traveler » en EN** — 57 valeurs de traduction dans 11 fichiers + 8 chaînes visibles dans le code (copie du dashboard, inscription, badge d'avatar, bannière d'onboarding, carte des lieux). Les IDENTIFIANTS (`superTripper`, `TripperBlock`, `becomeYamber`, route `/become-yamber`, `PublicTripper`) ne changent pas : ils sont du code, pas de la copie | Un seul mot pour un seul rôle, en FR comme dans les emails (D45) ; renommer des identifiants et une route publique n'apporte rien au membre et casserait des liens | Le code garde son ancien vocabulaire interne (`carrier` en API, `tripper` dans quelques noms de composants) ; « Traveler » en EN est un choix d'alignement, révisable | `chore/vocabulaire-voyageur` |

## 2bis.26 — Session `feat/notifications-vivantes` (les notifications parlent et se rafraîchissent) — A91
| # | Arbitrage | Pourquoi | Compromis | PR |
|---|---|---|---|---|
| A91 | **Notifications vivantes** (décisions utilisateur 03/09, 1A–4A) : (1) rafraîchissement par polling 30 s onglet visible + au retour sur l'onglet + invalidation après les gestes (pas de flux temps réel) ; (2) **copie par événement ET par rôle** (`notifications.copy.<event>.<ROLE>`, un jalon par étape), avec prénom de la contrepartie SERVI par l'API (`NotificationView.counterpartFirstName`, jointure au listing), corridor, montant, dossier, date — le front formate, ne recalcule rien ; (3) cloche desktop = menu des 5 dernières + « Tout marquer lu » (`PATCH /me/notifications/read-all`) + « Voir tout » ; (4) matrice A35 amendée : l'ATTERRISSAGE écrit à l'Expéditeur (« préviens le destinataire »), les autres jalons restent in-app. Le contrat `NotificationView.type` accepte les notifications SYSTÈME (`carrier.payout_failed`, A87) — le mapper strict les rejetait (bug latent de #139, corrigé ici) | Une notification qui ne dit ni qui ni quoi n'est pas lue ; une cloche figée ment ; l'atterrissage est le seul jalon où l'Expéditeur doit AGIR (prévenir le destinataire) | Polling = jusqu'à 30 s de retard ; un menu de plus dans le header | `feat/notifications-vivantes` |

---

## 2bis.27 — Session B5-PR1 `feat/b5-rating-server` (notation double-aveugle, serveur) — A92→A94

| # | Arbitrage | Pourquoi | Compromis | PR |
|---|---|---|---|---|
| A92 | **La notation est une opération gardée SANS transition** (`canRate(booking, role, now)` dans la machine, comme `canRegenerateCode`) : COMPLETED, fenêtre ouverte (`ratingWindowEndsAt` posé à la fin de transaction), pas encore noté par CE rôle. Écriture en UNE transaction (Review + marquage du Booking) sous **verrou optimiste `updatedAt`** (A85), jamais une garde sur un champ peut-être absent | La règle vit dans la machine et se teste seule ; le double-aveugle se décide dans la même transaction que la note (l'autre a-t-il déjà noté ?) | Un `updatedAt` concurrent (autre écriture sur le deal) refuse la note : l'utilisateur recharge | `feat/b5-rating-server` |
| A93 | **La réputation est dénormalisée par le deal-service** (`reputation.service`, fonctions pures testées) à chaque fait qui change : révélation d'avis, COMPLETED, annulation fautive. Le profil public (auth-service) et la recherche (trip-service, `isSuperCarrier`) LISENT, ne calculent pas ; le profil ne sert que les avis révélés (`revealedAt: { not: null }`) ; les avis historiques sont révélés une fois (`repair-legacy-reviews.ts`) | Un seul calcul, un seul propriétaire, des lectures rapides ; le double-aveugle ne peut pas fuiter par le profil | Recalcul best-effort (jamais bloquant) : un échec laisse la valeur précédente jusqu'au prochain fait | `feat/b5-rating-server` |
| A94 | **Relances et révélation dans UN cron horaire** (`rating.cron.ts`, `RATING_CRON_ENABLED`) : J+5 puis J+7 aux rôles muets (`ratingRemindersSent` 0 → 1 → 2, garde conditionnelle sur le compteur), révélation à 14 j même avec une seule note, fermeture silencieuse sans note. Emails : `rating_reminder` → rôle cible (dictionnaire D44, « dernier rappel » à J+7) ; l'email de fin de transaction retrouve son bouton « Noter » (fin de la décision 10 de B4) | Deux rappels et on se tait (spec §3.7) ; le compteur est la garde de concurrence | Un rappel peut partir jusqu'à 59 min après J+5 | `feat/b5-rating-server` |

## 2bis.28 — Session B5-PR2 `feat/b5-rating-front` (notation et réputation à l'écran) — A95→A97

Décisions utilisateur du 03/09 après challenge métier/UX : 1A (carte « Noter » sur l'écran terminé des deux rôles + mention sur les listes + « à traiter » à l'accueil, jamais de fenêtre bloquante) · 2A (l'écran de notation ne montre NI la moyenne NI le nombre de deals de la personne notée — biais d'ancrage) · 3A (« Plus tard » = retour au deal sans question) · 4A (badge de niveau dans l'en-tête du profil + ligne de faits + critère du niveau suivant) · 5A (cartes de recherche inchangées, le badge suit le niveau serveur) · 6A (avis publics avec pouces des critères et « Signaler cet avis » en mailto jusqu'au chantier C).

| # | Arbitrage | Pourquoi | Compromis | PR |
|---|---|---|---|---|
| A95 | **L'état de notation est SERVI avec le deal** (`rating` sur `ShipperBookingView` et `CarrierBookingView` : `windowEndsAt`, `ratedByMe`, `counterpartHasRated`, `revealedAt`, `canRate` calculé par la machine pour LE rôle qui lit). Le front ne déduit jamais « à noter » d'une date ou d'un statut : la carte, les listes et l'accueil lisent `canRate` ; `hasRated` des listes = `!canRate` | Règle non négociable : le front reflète, il ne décide pas. Une seule source (`canRate`) pour trois surfaces | Une liste montre « pense à noter » jusqu'à la fermeture de fenêtre même si l'utilisateur ne notera jamais : c'est voulu (deux relances, puis silence, mais le bouton reste) | `feat/b5-rating-front` |
| A96 | **Trois écrans, zéro 409 visible** : le formulaire n'est rendu que si `canRate` ; sinon l'écran « fait » (note envoyée / révélée / fermée) — et un `TRANSITION_NOT_ALLOWED` au POST (course avec le cron ou double onglet) recharge le contexte au lieu d'afficher une erreur. La notation ne montre pas la moyenne de la personne notée (2A) ; le succès dit « révélée quand {prénom} aura noté, ou le {date} », ou « {prénom} t'avait déjà noté : vos deux avis sont visibles » quand la révélation est immédiate | Le double-aveugle se comprend à l'écran : on sait TOUJOURS pourquoi on ne voit pas encore la note de l'autre | Un rechargement de contexte après un 409 masque la cause exacte (déjà noté vs fenêtre fermée) : le message le dit en toast | `feat/b5-rating-front` |
| A97 | **Les seuils de niveau ne sont écrits qu'une fois** (`REPUTATION_PARAMS`, serveur) ; la copie du profil (info-bulle du badge, « prochain niveau ») les recopie en clair FR/EN et DOIT suivre toute modification du paramètre (Confirmé = 3 deals ; Top = 10 deals Voyageur / 5 envois Expéditeur, moyenne ≥ 4,8, 0 annulation tardive). Pouces des critères servis avec chaque avis révélé (`criteria`) ; « Signaler cet avis » = mailto support avec l'identifiant de l'avis (6A), en attendant la file Reports du chantier C | La réputation explicable (D29①) n'a de sens que si l'utilisateur lit les critères réels ; un libellé décalé serait un mensonge | Copie dupliquée (contrat ↔ messages) jusqu'à ce que l'admin serve les paramètres au front | `feat/b5-rating-front` |

## 2bis.29 — Session C-PR1 `feat/c1-admin-socle` (socle admin : application séparée, 2FA, journal, file en lecture) — A98→A101

| # | Arbitrage | Pourquoi | Compromis | PR |
|---|---|---|---|---|
| A98 | **TOTP maison, zéro dépendance** (`@packages/totp` : HMAC-SHA1 RFC 6238, base32, fenêtre ±1 pas, anti-rejeu par `totpLastUsedStep`, codes de secours 10 caractères sans ambiguïté hashés SHA-256, secret chiffré AES-256-GCM sous `TOTP_ENCRYPTION_KEY` avec le patron D43 — clé de dev hors production, refus en production). Testé sur les vecteurs officiels de la RFC | 40 lignes de node:crypto contre une chaîne de dépendances ; les vecteurs de la RFC valent mieux qu'une confiance dans un paquet ; même discipline que le code de livraison | Pas de SHA-256/SHA-512 TOTP (SHA-1 est ce que les applications d'authentification supportent toutes) ; 5 échecs → 15 min d'attente (compteur Redis) | `feat/c1-admin-socle` |
| A99 | **Deux paires de cookies, deux sessions** : `admin_access_token` / `admin_refresh_token` (+ `admin_preauth` 5 min entre mot de passe et TOTP), record Redis `admin_jti:` séparé, JWT porteur de `adm: true` + `amr: ["pwd","totp"]`. `isAdminAuthenticated` ne lit JAMAIS `access_token` ; une session utilisateur d'un compte ADMIN n'ouvre aucune route admin ; `POST /auth/login` reste inchangé pour user-ui | Les cookies ignorent le port : user-ui (3000) et admin-ui (3001) partagent l'hôte en dev ; sans noms distincts, une connexion admin écraserait la session utilisateur — et inversement. La 2FA doit être prouvée par le jeton lui-même, pas déduite du rôle | Plomberie doublée (login, refresh, logout) ; en production l'admin vit sur son propre hôte et le proxy D48 garde les cookies first-party | `feat/c1-admin-socle` |
| A100 | **Journal d'audit dans la transaction, pas dans un consommateur Kafka** (`@packages/admin-audit`, modèle `AdminAction`) : `ADMIN_LOGIN`, `ADMIN_TOTP_ENABLED`, `ADMIN_BACKUP_CODE_USED`, `ADMIN_LOGOUT`, `DISPUTE_VIEWED` (ouvrir un dossier montre des identités et des photos : ça se journalise). D7 prévoyait un consommateur Kafka : l'événement viendra EN PLUS (C-PR2, `booking.dispute_resolved`), jamais à la place | Un geste dont le journal échoue n'a pas eu lieu ; un consommateur peut être en retard ou en panne | Pas de flux temps réel du journal avant C-PR2 | `feat/c1-admin-socle` |
| A101 | **Une seule file « à arbitrer »** (`GET /admin/disputes`) mêlant litiges OPEN et retenues `HELD_FOR_MEDIATION` (7A), les plus anciens d'abord, et un dossier (`GET /admin/disputes/:id`) qui sert TOUTES les preuves (déclaration, prise en charge + checklist, jalons, remise, litige) et l'état de l'argent — jamais le code de livraison (D43), jamais les identifiants Stripe. Le rôle ADMIN ne se pose que par script (`grant-admin.ts`) | L'admin doit voir en une page ce que les deux parties ont déclaré et prouvé ; le code de livraison reste un secret des parties même pour Yamba | admin-ui recopie les types du contrat (pas de zod dans le bundle admin) : à garder alignés | `feat/c1-admin-socle` |

## 2bis.30 — Session C-PR2 `feat/c2-mediation` (médiation : version du Voyageur, décisions, argent, écrans) — A102→A104

| # | Arbitrage | Pourquoi | Compromis | PR |
|---|---|---|---|---|
| A102 | **La décision suit le patron « l'argent d'abord » de l'annulation (D39)** : `provider.refund` AVANT la transaction, puis UNE transaction (transition ADMIN par la machine, résolution sur le `Dispute` avec garde `resolvedAt` en forme `null OU isSet:false`, compteur interne, `AdminAction`, outbox `booking.dispute_resolved`), puis le transfert par l'exécuteur unique (`payoutAmountCents` posé = net − remboursement ; `executePayout` lit désormais `payoutAmountCents` d'abord, à COMPLETED comme à CANCELLED). Aucun événement `booking.completed` n'est émis (il déclencherait « paiement libéré ») : le seul signal est `dispute_resolved`, aux deux parties | Un seul chemin d'argent, un seul ordre ; le récapitulatif admin recopie exactement la règle serveur (`computeResolutionMoney`) | Un remboursement parti puis une transaction échouée laisse un dossier à réparer à la main (même risque accepté que l'annulation ; visible dans les logs et le journal) | `feat/c2-mediation` |
| A103 | **La version du Voyageur est un état du `Dispute` (OPEN → CARRIER_RESPONDED), pas une transition du Booking** ; l'événement `booking.dispute_carrier_responded` est écrit dans la même transaction (règle « pas d'état sans outbox ») mais ne notifie personne : l'admin le lit dans la file (« version reçue · à trancher », « attend le Voyageur · N h », « sans réponse · à trancher »). L'Expéditeur voit seulement « {prénom} a donné sa version », jamais son contenu | Le dossier a sa propre vie ; l'Expéditeur ne doit pas répondre à la version de l'autre (pas de ping-pong) | Pas de relance au Voyageur muet en v1 (l'email d'ouverture dit 72 h ; le cron viendra si nécessaire) | `feat/c2-mediation` |
| A104 | **« Clos par médiation » = `completedBy: "ADMIN"`** : la machine refuse `canRate` (D54 4B), le mapper sert `rating: null` (aucune carte), le portefeuille montre le montant réellement versé (`payoutAmountCents`), les listes lisent l'état comme un COMPLETED ordinaire. Le seed porte désormais un cas « retenue à arbitrer » (`bzv-held`) pour la recette | Un marqueur déjà présent (qui a clos) vaut mieux qu'un statut de plus ; la notation après litige serait une note de vengeance | Les COMPLETED d'avant C-PR2 clos par admin n'existent pas (aucune migration) | `feat/c2-mediation` |

## 2bis.31 — Session C-PR3 `feat/c3-admin-users` (profils, utilisateurs, sanctions, sessions, Sentry) — A105→A107

| # | Arbitrage | Pourquoi | Compromis | PR |
|---|---|---|---|---|
| A105 | **La matrice de permissions vit dans le contrat** (`ADMIN_PERMISSIONS` + `adminRoleAllows`, testés) et est recopiée en miroir dans l'admin-ui (`permissions.ts`) pour masquer les entrées de menu et les boutons ; le serveur reste le seul juge (403 `ADMIN_PERMISSION_DENIED`, jamais un 404 : la route existe, c'est le profil qui manque) | Une seule source, deux lecteurs ; l'écran ne promet rien que l'API refuserait | Miroir à maintenir à la main (comme pour les types admin) | `feat/c3-admin-users` |
| A106 | **Les effets d'une suspension sont des LECTURES, pas des écritures croisées** : la connexion est refusée par `isAuthenticated` (relit `accountStatus`), la publication et la réservation par `requireActiveAccount` (trip-service, deal-service), les trajets disparaissent par un filtre de recherche sur la relation `user.accountStatus` ; l'auth-service, propriétaire du User, ne touche ni Trip ni Booking. Les sessions sont révoquées et les emails partent hors transaction (idempotents) | D54 2A : aucun service n'écrit dans le domaine d'un autre ; la sanction reste réversible sans rien recalculer | Une requête de recherche fait une jointure de plus ; un `$ne` matche aussi les comptes sans le champ (voulu) | `feat/c3-admin-users` |
| A107 | **Sentry par le middleware d'erreurs commun** (`packages/error-handler/sentry.ts`) : une seule ligne par service (`initSentry`), capture des 5xx et des erreurs non opérationnelles avec `service` et `correlationId` en tags, `tracesSampleRate: 0` (erreurs seulement, pas de traçage) ; front : `instrumentation-client.ts` + `instrumentation.ts` sans `withSentryConfig` (pas d'upload de source maps en v1) | Le même identifiant dans l'alerte et dans les logs ; zéro coût quand le DSN est vide | Pas de performance ni de replay ; le gateway est hors périmètre (pas d'alias) | `feat/c3-admin-users` |

## 2bis.32 — Session C-PR4 `feat/c4-admin-trips` (trajets, masquage, billets, KPI d'accueil) — A108→A110

| # | Arbitrage | Pourquoi | Compromis | PR |
|---|---|---|---|---|
| A108 | **« Masqué par Yamba » est un champ du Trip lu à trois endroits, pas un statut** : `hiddenByAdminAt` (+ motif, + admin) est posé par le trip-service (propriétaire) dans la même transaction que le journal ; la recherche (`notHiddenFilter()` : null OU absent), la page publique (404) et la création de réservation côté deal-service (`checkTripBookable`, lecture seule) le lisent. Distinct de PAUSED pour que le Voyageur ne puisse pas le lever lui-même ; la machine d'états du trajet n'est pas touchée. La proposition du Support vit sur le trajet (`hideProposed*`), effacée à l'exécution. L'email au Voyageur porte un motif GÉNÉRIQUE ; le motif interne reste dans le journal | Un statut de plus aurait imposé une transition admin dans une machine faite pour le Voyageur ; une lecture de plus coûte moins qu'une écriture croisée à défaire | Trois lectures à maintenir (comme A106) ; un trajet masqué reste visible du Voyageur, qui peut le mettre en pause ou l'annuler lui-même | `feat/c4-admin-trips` |
| A109 | **La file des billets expire à la lecture, sans cron** : `GET /admin/tickets` marque `EXPIRED` (nouveau statut de `TripDocument`) tout billet PENDING d'un trajet déjà parti avant de servir la liste (un `updateMany`, `expiredNow` renvoyé). Une décision = document + `Trip.ticketVerificationStatus` + journal dans une transaction, verrou optimiste sur `status: PENDING` ; motif fermé au rejet ; un billet rejeté redéposé repasse PENDING (`addDocuments`) | Pas de tâche planifiée à opérer pour un cas rare ; celui qui ouvre la file la nettoie | Les billets expirés ne le sont qu'à la prochaine ouverture (les compteurs KPI excluent déjà les trajets partis) | `feat/c4-admin-trips` |
| A110 | **KPI d'accueil = compteurs par permission** (`GET /admin/kpis`, auth-service, lectures croisées de la base partagée, aucune écriture) : chaque compteur n'est servi que si le profil a la permission de la file correspondante (`null` sinon), l'admin-ui n'affiche que les tuiles non nulles. Le pilotage complet (courbes, corridors, finances) reste C-PR6 | Un chiffre que l'on ne peut pas ouvrir est une frustration ; les permissions existent déjà, autant les réutiliser | 14 `count` à chaque ouverture de l'accueil (acceptable au volume actuel ; cache Redis si besoin) | `feat/c4-admin-trips` |

## 2bis.33 — Session C-PR5a `feat/c5a-admin-finances` (files d'exception, fiche argent, rapprochement, rejeu, renversements) — A111→A113

| # | Arbitrage | Pourquoi | Compromis | PR |
|---|---|---|---|---|
| A111 | **Rejeux espacés sans plafond** : à chaque échec l'exécuteur pose `payoutNextRetryAt` (5 min ×6, 30 min ×6, 2 h ×12, puis 1 fois par jour — `payoutRetryDelayMs`, pur, testé) et `payoutLastAttemptAt` ; le cron rejoue « absent OU null OU échu » (pitfall Mongo) ; le compteur de tentatives est lu puis écrit (jamais `increment`, qui rend null sur un champ absent). Le rejeu par `account.updated` (A87) et le rejeu manuel admin ignorent l'échéance. Remplace le plafond de 100 (décision 03/09 1B) après lequel un versement dû se taisait | Un versement dû ne doit jamais devenir silencieux ; espacer coûte moins que borner | Un versement définitivement impossible est rejoué une fois par jour jusqu'à décision humaine (file admin) | `feat/c5a-admin-finances` |
| A112 | **Rapprochement en lecture seule** : `PaymentProvider.inspect({ intentId, transferId })` (Stripe : intent + `refunds.list` + `transfers.retrieve` ; Fake : mémoire) → `reconcile(db, live)` pur, qui LISTE des divergences codées (`REFUND_NOT_RECORDED` = remboursement parti sans écriture, D39 ; capture, transfert manquant, montant, renversement non marqué…) sans jamais écrire en base ; journal `DEAL_RECONCILED` avec les codes. `refundId` est désormais stocké aux quatre sites de remboursement (refus au pickup, annulation, décision de litige, restitution de retenue) | La base reflète Stripe, elle ne le remplace pas ; l'admin doit pouvoir voir l'écart avant de le corriger | Une correction reste un geste manuel documenté (pas d'auto-réparation) ; les remboursements antérieurs n'ont pas d'identifiant (rapprochement par montant) | `feat/c5a-admin-finances` |
| A113 | **Un renversement se clôt par une décision, jamais par un rejeu** : `transfer.reversed` pose `REVERSED` et met la clôture à null explicitement ; les files lisent « absent OU null ». `RESENT` = `PENDING` + **nouvelle clé d'idempotence explicite** (`payoutIdempotencyKey`, sinon `payout:<id>` renverrait le transfert renversé, RG-PAY-04) + clôture + journal dans UNE transaction, puis l'exécuteur unique ; `WRITTEN_OFF` = clôture seule, manque à gagner assumé. Rejeu et clôture refusés à une partie du deal (403) et hors état (400) | RG-PAY-04 reste vrai (même clé ⇒ même transfert) tout en permettant un second transfert voulu | Un re-versement en échec redevient un versement FAILED ordinaire (file, espacement) | `feat/c5a-admin-finances` |

## 2bis.34 — Session C-PR5b `feat/c5b-admin-finances-report` (rapport mensuel, export CSV, remboursement manuel) — A114→A116

| # | Arbitrage | Pourquoi | Compromis | PR |
|---|---|---|---|---|
| A114 | **Le rapport date chaque fait par SON champ** : capture → `capturedAt`, remboursement → `refundedAt`, versement → `payoutSentAt`, revenu (commission + prime des COMPLETED) → `completedAt`, retenue → `closedAt` ; mois UTC, par devise (`buildFinanceReport`, pur, testé). Un deal capturé en mars et terminé en avril compte dans les deux mois. Le `snapshot` du jour (dû, gelé, renversé ouvert, retenues à arbitrer, remboursements proposés) est un passif, jamais un revenu. Aucun frais Stripe (pas en base) | Sans grand livre (D58 1A), la seule vérité datée est le champ posé par la transition ; un revenu se reconnaît à la fin du deal, pas à l'encaissement | Les remboursements d'avant C-PR5 n'ont pas d'identifiant ; le comptable rapproche les frais avec l'export Stripe | `feat/c5b-admin-finances-report` |
| A115 | **L'export CSV est un téléchargement direct, journalisé, borné** : `GET /admin/finances/export?from&to` (≤ 366 jours), `finances.export` = FINANCE seul, une ligne par deal ayant un fait d'argent dans la période, en-tête stable (`FINANCE_CSV_COLUMNS`), cellules échappées, préfixes de formule neutralisés (injection tableur), BOM UTF-8 pour Excel, journal `FINANCE_EXPORTED` (période, nombre de lignes). L'admin-ui ouvre l'URL : le cookie admin suit par le proxy `/api` | Le fichier sort de la plateforme avec des identifiants : chaque sortie doit laisser une trace ; la borne évite l'export « tout depuis le début » | Pas de filtre par corridor ni par statut en v1 (le tableur fait le tri) | `feat/c5b-admin-finances-report` |
| A116 | **Le remboursement manuel suit D39 et le verrou du cumul** : proposition sans argent (`REFUND_MANUAL_PROPOSED`, FINANCE / SUPPORT), application par SUPER_ADMIN seul : bornes pures (`manualRefundBounds` : deal COMPLETED ou CANCELLED, capturé, plafond = total − déjà remboursé), `provider.refund` D'ABORD, puis UNE transaction conditionnelle sur `refundAmountCents` (absent OU null OU valeur lue) qui cumule, pose `refundId`, efface la proposition, écrit l'outbox `booking.refund_issued` (acteur ADMIN → email standard à l'Expéditeur) et le journal. Le versement du Voyageur n'est pas touché : Yamba porte le geste. Le portefeuille de l'Expéditeur lit désormais un remboursement après COMPLETED comme une annulation partielle (corrige aussi le partiel de médiation C-PR2) | Un geste commercial n'est pas une décision de litige (DISPUTED exclu) ; deux admins ne doivent jamais rembourser deux fois | Un remboursement parti puis une base non écrite reste possible (D39) — visible au rapprochement `REFUND_NOT_RECORDED` | `feat/c5b-admin-finances-report` |

## 2bis.35 — Session C-PR6a `feat/c6a-admin-pilotage` (courbes, corridors, chronologie d'un deal, compteurs de demande) — A117→A119

| # | Arbitrage | Pourquoi | Compromis | PR |
|---|---|---|---|---|
| A117 | **Les courbes sont des petits multiples à une série** : chaque mesure (inscriptions, trajets publiés, demandes, acceptations, livraisons, deals terminés, annulations, litiges, encaissé par devise) a sa propre courbe, une seule couleur, un seul axe, survol avec repère, dernier point étiqueté, vue tableau — jamais un double axe ni huit séries superposées. Calcul serveur pur (`buildSeries`) : chaque fait compte dans la période (semaine ISO ou mois UTC) de SA date, périodes vides incluses ; cache Redis 60 s par requête (`yamba:pilotage:*`) | Huit mesures sur un même graphe ne se lisent pas ; une courbe par mesure se lit d'un coup d'œil et reste accessible (une série = pas de légende, une table à côté) | Pas de comparaison superposée (à faire en petits multiples alignés si besoin) ; les comptes Voyageurs « prêts » sont un total, pas une courbe (pas de date d'activation Stripe en base) | `feat/c6a-admin-pilotage` |
| A118 | **La demande vit dans Redis, sans donnée personnelle** (`packages/libs/redis/trip-stats.ts`, clés pures testées) : une vue de page = `SET NX` de dédoublonnage par visiteur (id utilisateur, sinon empreinte tronquée IP + agent) et par jour, puis `INCR` du total du trajet et du corridor du jour ; une recherche = `INCR` par corridor (`ville>ville` normalisées : minuscules, sans accents) et par jour, plus un compteur « sans résultat » ; les corridors demandés sont mémorisés dans un SET (pas de SCAN). Clés de jour expirées à 400 j. Le trip-service écrit et sert `viewsCount` (cartes, détail) ; l'auth-service lit pour le tableau des corridors, qui inclut les corridors cherchés SANS trajet (« demande sans offre »). Toute erreur Redis est avalée : jamais un 500 pour un compteur | D5 (vues via Redis) et le signal de liquidité de la roadmap ; un compteur ne doit ni identifier ni casser | Pas de reprise si Redis est vidé ; les vues d'un visiteur non connecté changent avec son adresse | `feat/c6a-admin-pilotage` |
| A119 | **La chronologie d'un deal est une fusion pure et whitelistée** (`mergeDealHistory`) : outbox (avec `publishedAt`, tentatives, parqué ≥ 10), journal admin (`targetId`), notifications (`bookingId`), emails (`eventId` des événements) ; le payload outbox est réduit à `HISTORY_PAYLOAD_WHITELIST` (jamais un code, une photo, une adresse) ; consultation journalisée `DEAL_HISTORY_VIEWED` ; permission `deals.history.read` (MEDIATOR, SUPPORT, FINANCE). Au passage : les chemins admin C-PR5a/5b du deal-service étaient rangés hors de `paths` dans l'OpenAPI (siblings du document) — replacés, 34 chemins dont 14 admin | Un événement parqué est la seule trace d'un relais cassé ; un opérateur doit le voir sur le deal, pas dans les logs | Bornes : 500 événements, 200 actions, 500 notifications ; aucune action de réparation depuis l'écran (backlog) | `feat/c6a-admin-pilotage` |

## 2bis.36 — Session C-PR6c `feat/c6c-admin-pilotage-v2` (courbes agrandies, drill-down, finances, popularité web) — A120→A122

| # | Arbitrage | Pourquoi | Compromis | PR |
|---|---|---|---|---|
| A120 | **Une courbe agrandie = la courbe, son tableau, et la liste derrière chaque point** : deux courbes par ligne (hauteur 220), « Agrandir » ouvre la mesure seule (hauteur 360, libellés de période espacés), le tableau période / valeur / variation dessous, et un clic (courbe ou ligne) charge `GET /admin/pilotage/drilldown?metric&granularity&period` — `periodBounds` (pur, inverse de `periodKey`, testé) borne [début, fin) de la période, une requête par famille (comptes, trajets, deals), 200 éléments maximum avec `truncated`, lien vers la fiche. La liste des inscriptions est journalisée (`PILOTAGE_DRILLDOWN_VIEWED`) : ce sont des personnes | Une courbe qu'on ne peut pas ouvrir ne sert pas ; « quels deals ? » est la première question d'un opérateur | Pas de sélection de plage libre (semaine ISO ou mois seulement) ; bornes fixes | `feat/c6c-admin-pilotage-v2` |
| A121 | **Les finances du pilotage sont calculées par la même fonction que l'activité** : `buildSeries` porte `finance[]` par devise (encaissé, remboursé, versé, revenu reconnu, retenues nées), chaque fait à sa date (A114) ; l'onglet « Finances » réutilise les mêmes courbes agrandissables avec sélecteur de devise | Une seule lecture, une seule règle de datation, deux onglets | Le rapport mensuel (`/finances/report`) reste la référence comptable ; le pilotage est une lecture rapide | `feat/c6c-admin-pilotage-v2` |
| A122 | **La popularité d'un trajet se montre comme un badge, pas comme un chiffre gris** : pastille « n vues » (icône œil) dans la rangée des badges des cartes desktop et mobile et sous le titre du détail public, « Populaire » à partir de `POPULAR_VIEWS` = 20 vues (`lib/trip-signals.ts`, seuil de présentation) ; billet vérifié rappelé dans la même rangée sur le détail ; zéro vue n'affiche rien | Un signal social doit être lisible en un coup d'œil et cohérent entre la liste et le détail | Le seuil est arbitraire tant qu'il n'y a pas de volume ; à ajuster avec les données | `feat/c6c-admin-pilotage-v2` |

## 2bis.37 — Session C-PR3bis `feat/c3bis-admin-roles` (profils cumulés) — A123→A125

| # | Arbitrage | Pourquoi | Compromis | PR |
|---|---|---|---|---|
| A123 | **Liste + miroir, jamais liste seule** : `User.adminRoles` (liste, `@default([])`) porte les profils cumulés ; `User.adminRole` reste le profil PRINCIPAL (SUPER_ADMIN d'abord, sinon le premier dans l'ordre canonique — `primaryAdminRole`) : la connexion admin, l'affichage et les comptes d'avant la reprise le lisent toujours. Tout writer passe par `adminRolesData(roles)` (liste normalisée + miroir) ou `NO_ADMIN_ROLES` — la liste n'est jamais absente (pitfall Mongo) ; `adminRolesOf(u)` lit de façon tolérante (liste vide → `[adminRole]`) ; `backfill-admin-roles.ts` reprend les comptes existants | Aucune route ni écran n'a à changer de sémantique d'un coup ; un compte d'avant la PR fonctionne sans script (le script rend la liste exacte) | Deux champs à garder cohérents (une seule fonction d'écriture) | `feat/c3bis-admin-roles` |
| A124 | **L'union se calcule au dernier maillon** : `adminRolesAllow(roles, permission)` = l'un des profils suffit ; `isAdminAuthenticated` pose `req.adminRoles`, `requireAdminPermission` et les KPI lisent la liste ; JWT et `/admin/me` portent `adminRoles` ; l'admin-ui `can(roles | role, permission)` accepte encore un profil seul. La garde « dernier SUPER_ADMIN » compte liste OU miroir ; un admin cumulant plusieurs profils n'est sanctionné que par un SUPER_ADMIN | La matrice D56 ne bouge pas : cumuler = lire plusieurs lignes | Une permission refusée à tous les profils d'un compte reste refusée (pas d'héritage implicite) | `feat/c3bis-admin-roles` |
| A125 | **Pas de `transform` Zod dans un contrat servi en OpenAPI** : la normalisation (ordre canonique, doublons) est une fonction (`normalizeAdminRoles`), le schéma reste un tableau borné 1..4 — un `transform` ne se représente pas en JSON Schema et cassait la génération des trois documents | Les contrats sont la source de l'OpenAPI (D3) : tout ce qu'ils portent doit se sérialiser | La normalisation est un appel de plus côté serveur (`adminRolesData`) | `feat/c3bis-admin-roles` |

## 2bis.38 — Session C-PR7a `feat/c7a-admin-search-exports` (recherches poussées, exports encadrés) — A126→A128

| # | Arbitrage | Pourquoi | Compromis | PR |
|---|---|---|---|---|
| A126 | **Les filtres sont des contrats, traduits par des fonctions pures** : `AdminUsersQuery`, `AdminTripsQuery`, `TicketQueueQuery`, `ArbitrationQueueQuery` (Zod, bornes : limite ≤ 100, texte ≤ 120) ; `buildUsersWhere` / `buildTripsWhere` / `buildTicketsWhere` produisent le `where` Prisma (testés sans base : combinaison d'un OR texte et d'un OR « non masqué » par AND, identifiant ou ticket court-circuitant le texte) ; la file d'arbitrage est filtrée en mémoire sur les items calculés (`filterQueueItems`, pur) car ses dates et sa « décidabilité » sont dérivées. Pagination par curseur (id du dernier, tri [champ, id]) ; les compteurs restent ceux de la liste entière | Un filtre serveur est une règle : il se teste, il se documente dans l'OpenAPI, il vaut pour l'export | La recherche par deal / ticket ignore les autres filtres (les deux parties, comme avant) | `feat/c7a-admin-search-exports` |
| A127 | **Deux classes d'export, une lib** : `@packages/libs/csv` (cellules RFC 4180, préfixes de formule neutralisés, BOM) ; `exports.operational` (FINANCE, MEDIATOR) pour trajets, billets, arbitrage — identifiants seulement, jamais un email ni un téléphone ; `exports.personal` (SUPER_ADMIN seul) pour les utilisateurs — motif ≥ 20 caractères obligatoire. Chaque export écrit `EXPORTED` au journal avec le domaine, les filtres, le nombre de lignes et le motif ; 5 000 lignes maximum ; l'admin-ui ouvre l'URL (cookie par le proxy) et demande le motif dans un panneau | Un fichier qui sort est une donnée qui sort : le journal dit qui, quoi, combien, pourquoi (RGPD, D9) | Pas d'export planifié ni d'envoi par email ; le comptable télécharge | `feat/c7a-admin-search-exports` |
| A128 | **Les routes d'export se déclarent AVANT les routes `/:id`** (`/admin/users/export`, `/admin/trips/export`, `/admin/tickets/export`, `/admin/disputes/export`) — sinon Express prend « export » pour un identifiant et répond 400 « Invalid id ». Au passage : plugin Tailwind `line-clamp` retiré (intégré depuis 3.3) | Un ordre de routes est une règle de code, pas un détail | — | `feat/c7a-admin-search-exports` |

---

# 3. Roadmap maîtresse

## 3.0 Les trois jalons

| Jalon | Nom | Critère de sortie | Contenu |
|---|---|---|---|
| **1** | **Boucle transactionnelle** | Un Expéditeur réserve, paie, fait livrer, note — de bout en bout, en conditions réelles Stripe test | B1 ✅ (fondations) → PR4bis (notification-service) → PR5 front (listes réelles) → refonte pricing front → B2 (argent entrant) → B3 (transport) → B4 (argent sortant) → B5 (confiance) |
| **2** | **Plateforme opérable** — *constitutif du lancement public* | Telama peut opérer seul : arbitrer un litige, vérifier un billet, ajuster un paramètre, voir une erreur avant l'utilisateur | **C admin-ui** (médiation YAM-XXXX, file Reports, paramètres audités, TrustScore D29②, 2FA TOTP) · E profil public Voyageur · solde session auth (SES-03/04/05) · micro-PRs confiance (Signaler, wording D28, CTA alertes, page destinataire) · Sentry front+back · PostHog · vérification backups Atlas |
| **3** | **Expansion** | Le playbook corridor est réplicable et l'expérience s'étend | F chat (message-service :6005) · locales ES puis PT (critère de fin i18n) · G mobile (client généré depuis l'OpenAPI, D3) · H recommandations (l'outbox D2 capture l'historique depuis B1 précisément pour ce replay) |

Le lancement public = **fin du jalon 2**. L'admin-ui n'est pas un confort d'après-lancement : c'est l'organe de conformité (D9), de médiation (D21/D26) et de pilotage sans lequel le jalon 1 est une boucle qu'on ne sait pas tenir.

## 3.1 Chantiers séquencés

| # | Jalon | Chantier | Contenu | Dépend de | Décisions consommées |
|---|---|---|---|---|---|
| **-1** ✅ | — | **CI** (PR #63) | GitHub Actions : tsc, JSON parse + miroir FR/EN, anti-fuite — devenue 11 checks (tests, contrats OAS) | — | D10 |
| **-1bis** ✅ | — | **PR session auth** | Inactivité serveur + durée absolue livrées ; solde SES-03/04/05 au jalon 2 | — | D27 |
| **0** ✅ | — | **OpenAPI** | OAS 3.1 depuis Zod — trip-service fait ; conversion auth-service au backlog (§7.4) | — | D3 |
| **0bis** ✅ | — | **Mockup pricing** | Arbitrages D13/D14/D16/D20 rendus sur pièce | — | D13-D16, D20-D22 |
| **B1** ✅ | 1 | **deal-service — fondations** (PR #70, #71, #72\*, #73+#74) | Modèle Prisma Booking (snapshots, fuseaux, RGPD, capacité, protectionPlan), state machine (188 tests), GET par rôle (DTOs whitelist A13), `hasActiveBookings()` branché (A20), contrats + 17 événements (A15), outbox + relay Redpanda (A23/A24, 218 tests), modèle Report, template service complet | 0, 0bis | D1-D4, D6, D8, D12, D17-D19, D22-D24, D26 |
| **B1-solde** | 1 | **PR4bis notification-service :6004 + PR5 front** | Premier consumer (dédup event-id, matrice des 17), listes réelles Mes envois / deals Mes trajets (seed A14) | B1 | D2, D8 (A15, A16) |
| **B2** | 1 | **Naissance du deal + argent entrant** | Création depuis wizard, PaymentIntent (autorisation → capture à l'acceptation), accept/decline, cron expiration 24h, remboursements, `PaymentProvider` abstrait, writers outbox EN TRANSACTION, payment-service :6008, media-service :6009 | B1-solde + transactions Atlas prouvées (§7.2) | D11, D16, D20, D21 |
| **B3** | 1 | **Transport** | Pickup (upload R2 + code bcrypt + checklist conformité), refuse, tracking events, deliver (compare + lock serveur), régénération | B2 | D4, D9 |
| **B4** | 1 | **Argent sortant** | **PR1 serveur, PR2 Expéditeur, PR3 Voyageur faites** (D49–D52, A65–A78) : confirmation anticipée, cron J+4 → COMPLETED + `transfer`, dispute avec gel (aussi « non livré » depuis PICKED_UP), rappel J+3, écrans des deux côtés, photo de remise, **retenue ANN-01 au Voyageur (A79–A82)** · reste portefeuille (A77) ; matrice remboursements = chantier C | B3 | D21, D22, D49–D52 |
| **B5** | 1 | **Confiance** | **PR1 serveur faite** (D53, A92–A94) : rating double-aveugle, relances J+5/J+7, réputation dénormalisée (D29①) · reste PR2 front (écrans de notation réels, boutons « Noter », niveaux sur profils et cartes) | B4 | D2, D29, D53 |
| **C** | 2 | **admin-ui** | Médiation litiges (tickets YAM-XXXX), vérification billets, file des Reports, gestion users, **paramètres plateforme** (curseurs du mockup, audités), **TrustScore interne + plafonds progressifs** (D29②). Login séparé, 2FA TOTP, audit log | B4 | D6, D7, D26, D29 |
| **E** | 2 | **Profil public Voyageur** | Page publique (stats réelles, trajets, avis) + Shop-preview équivalent | B1 (stats), B5 (avis) | — |
| **F** | 3 | **message-service** (chat) | Socket.io + persistance Mongo + événements → notifications. Coordination pickup Expéditeur↔Voyageur | B (utile dès ACCEPTED) | D2 |
| **G** | 3 | **Mobile** | App consommant le gateway, client généré depuis OpenAPI | B, D3 | D3 |
| **H** | 3 | **Recommandations ML** | « Trajets pour toi », matching SavedRoutes intelligent — sur les données collectées (Kafka + PostHog = collecte gratuite) | post-lancement, volume | D2, D5, D15-V2 |

## 3.2 En continu (au fil de l'eau, entre les lots)

- **PRs i18n restantes** : dashboard sections (dissolution dashboard.copy.ts), booking, trips/create, page publique (LocationsCard → namespace tripDetail), divers, suppression UiPreferencesProvider → **critère de fin : PR `feat/locale-es`** (jalon 3), puis PT.
- **Micro-PRs front (jalon 2)** : wording statuts (D28 — 2 JSON), bouton Signaler (trajet + membre), CTA alertes (page + empty state recherche), page destinataire.
- **Micro-PR backend** : viewsCount Redis (D5).
- **Intégrations (jalon 2, constitutives du lancement)** : PostHog, Sentry (front + back), vérification backups MongoDB Atlas.

## 3.3 Flux « Telama seul » (chemin critique potentiel — échéances réelles)

- 📅 **Septembre 2026 — le plus urgent** : dossiers **Station F** et **Paris&Co** · dossier **Bourse French Tech** · prêt d'honneur (Réseau Entreprendre / Initiative France).
- `POLITIQUE-CONFORMITE-YAMBA.md` (D9) — alimenté par les questionnaires assureurs.
- Prise de contact avec 2-3 acteurs d'embedded insurance (Wakam, Owen, Qover, bsurance — D22).
- Étude tarifs corridor GP (seed base_corridor D15).
- Préparation supply-seeding (20-30 voyageurs réguliers avant d'ouvrir la demande).

## 3.4 Prochaines actions immédiates (ordre d'exécution)

1. Solder §7.2 lignes bloquantes : **transactions Atlas** (prérequis B2, reporté deux fois) + idempotence seed + required checks + user.email.
2. **⭐ PR4bis `feat/notification-service`** — session dédiée, prompt prêt (handoff PR4 §7) ; architecture consumer à trancher AVANT le code (A25 pressenti : cohabitation trip-notifications.service.ts).
3. **PR5 front** — listes réelles (ferme B1).
4. Refonte pricing front (create-trip, wizard) puis **B2**.

---

# 4. Estimation & vélocité

## 4.1 Méthode (inchangée)

- **Unité : la session de travail** (un bloc Telama + Claude livrant 1 à 3 PRs mergées) — pas le jour-homme, pas le story point.
- **On estime des PRs, pas des chantiers** : un chantier = somme de PRs nommées ; ce qu'on ne sait pas découper, on ne sait pas l'estimer.
- **Fourchette optimiste / réaliste** (réaliste ≈ optimiste × 1,5 — absorbe webhooks Stripe récalcitrants, surprises Mongo).
- **Ré-estimation à chaque handoff** : suivi PRs prévues vs livrées + recalibrage du restant. Le cône d'incertitude se resserre à mesure qu'on avance.

## 4.2 Vélocité mesurée (historique consommé au 21 juillet 2026)

| Période | Chantier | Estimé (réaliste) | Réel |
|---|---|---|---|
| Pré-roadmap | Trip lifecycle, i18n trips, dashboard, hotfixes, gouvernance v1.1 | — | **≈ 5 sessions** (hors décompte roadmap) |
| Roadmap | CI (D10) | 0,5 | ≈ 1 (dette d'installation npm soldée, non récurrente) |
| Roadmap | Mockup pricing + arbitrages (0bis) | 1,5 | ≈ 1 |
| Roadmap | OpenAPI (0) complet | 2,5 | ≈ 2 |
| Roadmap | D30 (test/trip-state-machine) + D27 (session auth) | 2,5 | ≈ 1 (session double) |
| Roadmap | **B1** fondations (PR1→PR4 + clôture/consolidation) | 3 – 4,5 | **≈ 4** ✓ dans la fourchette |
| | **Total consommé depuis la roadmap** | | **≈ 9–10 sessions** |

**Recalibrage** : B1 réel ≈ 0,9× de l'estimation réaliste — le premier setup Kafka/outbox (zone de risque identifiée en v1.1) a coûté ce qui était prévu. Les fourchettes réalistes sont MAINTENUES pour B2–B5 ; B2 garde son statut de zone de risque n°1 (Stripe en conditions réelles).

## 4.3 Estimation du restant (hors mobile)

| Chantier restant | PRs | Optimiste | Réaliste |
|---|---|---|---|
| PR4bis notification-service | 1-2 | 1 | 1,5 |
| PR5 front (listes réelles) | 1 | 0,5 | 1 |
| Refonte pricing front (create-trip, wizard, migration enum, i18n) | 3 | 2 | 3 |
| **B2** argent entrant | 2-3 | 2 | 3 |
| **B3** transport | 2-3 | 2 | 3 |
| **B4** argent sortant | 2 | 1,5 | 2,5 |
| **B5** confiance (sans notification-service) | 2 | 1,5 | 2 |
| Micro-PRs continu | ~5 petites | 1,5 | 2,5 |
| **→ Fin jalon 1 (boucle transactionnelle)** | **~15 PRs** | **≈ 12** | **≈ 18,5** |
| C admin-ui | 3-4 | 3 | 4,5 |
| E profil public Voyageur | 1-2 | 1 | 2 |
| Solde session auth (SES-03/04/05) | 1 | 0,5 | 1 |
| Sentry + PostHog + micro-PRs confiance | 2-3 | 1 | 1,5 |
| **→ Fin jalon 2 = LANCEMENT PUBLIC** | **~23 PRs** | **≈ 17,5** | **≈ 27,5** |
| F chat · i18n restant + ES · PT | 8-9 | 6,5 | 10 |
| **→ Jalon 3 web (hors mobile G, estimé après B5)** | | **≈ 24** | **≈ 37,5** |

**Traduction calendaire** (à 3-4 sessions/semaine) : fin du jalon 1 ≈ **3 à 6 semaines** · **lancement public (fin jalon 2) ≈ 5 à 9 semaines** — compatible avec les échéances de septembre du flux « Telama seul » (§3.3) à cadence tenue. Le mobile (G) est un chantier d'ampleur comparable à lui seul — estimation sérieuse après B5, quand l'OpenAPI sera stable.

## 4.4 Limites et risques de l'estimation

1. **Ne couvre que le code.** Les flux « Telama seul » (§3.3) courent en parallèle et peuvent devenir le chemin critique — les échéances de septembre sont fixes, elles.
2. **Zones de risque planning** : **B2** (Stripe en conditions réelles — webhooks, échecs de capture, comptes de test Connect) et la **dette bloquante des transactions Atlas** (§7.2 — reportée deux fois, à prouver AVANT d'écrire le premier writer).
3. **Auto-correction** : la vélocité B1 (≈ 0,9×) valide la méthode ; chaque handoff recalibre.

---

# 5. Conventions de travail

Français · pédagogie **Quoi/Pourquoi/Vérification** à chaque tâche · architecture discutée et validée avant le code · mockup HTML interactif avant l'implémentation pour les décisions UX · livraison = PR **mergée** dans `dev`, numéro NOTÉ au handoff · fichiers complets, lots de 4-6 avec « ok » (volumineux : téléchargeables) · attendus COMPTÉS (`wc -l`, checks CI, opérations OAS) · rituel automatisé par la CI (D10), localement `npm ci` si les deps ont bougé · `npx nx` jamais global · tsc via `apps/<app>/tsconfig.app.json` · tests dans la même PR (D30) · surfaces publiques en anglais, docs internes en français · chaque décision livrée avec son pourquoi et son compromis · chaque session se termine par un handoff incluant le suivi de vélocité (§4) et le prompt prêt-à-coller de la session suivante.

---

# 6. Leçons capitalisées (fusion de tous les handoffs — v1.3)

> Une leçon entre ici quand un incident RÉEL l'a produite. Elle n'en sort jamais ; elle se durcit. Classement par domaine, source entre parenthèses quand l'incident mérite mémoire.

## 6.1 Git & workflow

- **Prouver la branche AVANT d'écrire** (`git branch --show-current`) ; ne jamais brancher depuis une feature branch — toujours depuis `dev` APRÈS merge (brancher d'un état non mergé = cause de régression vécue).
- **`git status --short` AVANT tout `git add -A`** : un fichier collé en avance s'embarque clandestinement (schema.prisma, PR1). Corollaire amend : `--amend` embarque TOUT ce qui est stagé — vérifier le tree d'abord.
- **`git add` sans pathspec ne stage RIEN** — un commit est parti à vide (PR3). Toujours `git add <chemins explicites>` puis `git log --oneline -1` après CHAQUE commit, puis `git log -1 --stat`.
- **`--amend` uniquement sur du non-poussé** ; exception unique : branche non encore en PR, seul dessus, avec `--force-with-lease` (clôture PR4).
- **PRs mergées IMMÉDIATEMENT après push — et le numéro NOTÉ au handoff** (l'oubli a coûté une anomalie « merged 1 commit » à élucider en session suivante : #73 mergée sans trace, clôture PR4).
- **`M` vs `??` dans git status** : M = fichier suivi modifié (un openapi.json régénéré = comportement VOULU du registre commun A22) ; ?? = nouveau.
- **`git config user.email`** se vérifie une fois par machine (un email sans @ a signé des commits).

## 6.2 TypeScript, alias & build

- **`npx tsc --noEmit --project apps/<app>/tsconfig.app.json` = LA source de vérité** — jamais le tsc de l'IDE ; `--project apps/<app>` SANS suffixe résout le tsconfig solution-style (zéro fichier vérifié : un faux vert).
- **TROIS résolveurs d'alias, TROIS configs** (leçon durcie PR4) : tsc (`tsconfig.base.json` paths) ≠ webpack (`webpack.config.js` alias, PAR service consommateur) ≠ jest (préset ; seul `@packages/api-contracts` y est prouvé → mocks `{ virtual: true }` pour les autres dans les specs). Tout nouvel alias = déclaration tsconfig + webpack de CHAQUE consommateur, PROUVÉE avant d'écrire le code qui l'importe.
- **Chemins prouvés, jamais devinés** : `grep tsconfig.base.json` + `ls` en OUVERTURE de session (2 échecs consécutifs sur le layout d'api-contracts, PR3). Réel : `@packages/api-contracts` → `packages/libs/api-contracts/src/`.
- **`npx tsx` ne lit PAS les alias tsconfig** → les scripts (seeds) importent en RELATIF (`../index`, `../../api-contracts/src`).
- **Clonage manuel > générateur Nx** pour l'homogénéité des services (cibles inférées par plugins — AUCUN project.json nulle part) ; `npx nx reset` = premier réflexe sur graphe/serve incohérent ; `npx nx` toujours, jamais global. **Nx Cloud** : la clé `nxCloudId` dans nx.json rend Nx Cloud fatal (exit 1) en CI — supprimée.

## 6.3 Tests & vérifications comptées

- **Tests dans la même PR que leur logique (D30)** — test ROUGE d'abord pour tout nouveau comportement.
- **`wc -l` avec attendu COMPTÉ (jamais estimé) après chaque collage** + `grep -c "^import"` anti-double-collage. Le rituel a attrapé en direct : un fichier non collé (PR3), un 204≠214 (PR4). Compter les opérations d'un OAS au `node -e` sur le JSON, pas de tête.
- **Un « CI Ok » ne vaut que si le check attendu EXISTE : COMPTER les checks** (11 aujourd'hui), pas leur couleur — une CI verte à 10 checks ne testait pas la machine (PR2).
- **Mocks jest de fonctions async = TOUJOURS `mockResolvedValue`** (un `jest.fn()` nu retourne undefined → `.catch` explose → crash des workers) ; toute promesse lancée dans un mock (`stop()`) se capture et s'attend (PR4).
- **Smoke test `nx serve` en fin de lot backend** : attrape les dettes de résolution webpack que tsc ne voit pas.

## 6.4 Infra & runtime

- **`unref()` les timers des boucles de fond** : le serveur HTTP porte la vie du process, jest sort proprement (PR4).
- **`nx serve` avale le SIGINT** : prouver un graceful shutdown via `node apps/<app>/dist/main.js` (PR4).
- **Le rpk embarqué dans l'image Redpanda a des flags RÉDUITS** : pas de `--format json`, pas de `--set` au démarrage — parser la sortie TEXTE ; configs cluster via `rpk cluster config set` (persistées) après démarrage (PR4).
- **kafkajs marque `retriable: false` ses pannes de CONNEXION** une fois ses retries épuisés (`KafkaJSNumberOfRetriesExceeded`) — sans exclusion explicite, une panne broker parquerait des événements sains (attrapé au smoke réel PR4, test de non-régression posé).
- **Docker Desktop doit tourner** : un mur d'ECONNREFUSED = daemon éteint, pas un bug (PR4). **Redis** : clé par session `refresh_jti:{userId}:{jti}`, jamais par user.

## 6.5 Prisma & MongoDB

- **Champ `@unique` nullable Mongo = P2002 sur null** (publicSlug, PR3) : tout @unique optionnel reçoit une valeur déterministe en seed (`seed-<key>`) ; jamais de `@@unique` naïf sur champ nullable (index partiel raw ou unicité service — noté pour Review B5, A9).
- **Prisma `in:` exige un tableau mutable** → `[...READONLY_CONST]`.
- **Import Prisma des services** : `import prisma from "@packages/libs/prisma"` (les seeds : relatif, cf. §6.2/tsx). Schéma : `prisma/schema.prisma` (RACINE).
- **Transactions multi-documents ≠ writes** : un seed qui tourne prouve les writes, JAMAIS les transactions (`session.startTransaction()` en mongosh = la preuve) — reporté deux fois, bloquant B2.

## 6.6 Sécurité

- **Anti-fuite avant chaque push** : `git ls-files | grep -iE "\.env|secret"` — JAMAIS `--others` (liste les ignorés → faux positifs effrayants).
- **DTOs par rôle = listes blanches spread-résistantes** (A13) — jamais spread+delete ; prouvé par test d'intrusion (makeLeakyBooking).
- Backlog vivant : redaction pino-http (`req.headers.cookie`, `req.headers.authorization` via option `redact`).

## 6.7 Front & Next.js

- **Props callback des composants `"use client"` : suffixe `*Action`** (convention App Router, évite TS71007).
- **`overflow-x: clip` (pas `hidden`)** pour préserver `position: sticky`.
- Frontière i18n : `isFr={locale==="fr"}` uniquement à la frontière, maps statiques enum→clé, jamais de ternaire de locale dans les composants.

## 6.8 Méthode de session

- **Architecture validée AVANT le code** ; maquette HTML interactive avant toute décision visuelle ; lots de 4-6 avec « ok » ; fichiers complets (éditions chirurgicales UNIQUEMENT pour les fichiers non transmis, avec before/after exacts + grep de vérification — zéro incident depuis PR3).
- **Les corrections de Telama font foi** (paths réels, schéma réel, comportement observé) — le handoff suivant les grave (§4 « corrections » du handoff PR4 = le modèle, désormais §0bis de ce registre).
- **Transfert de fichiers** : coller dans le message ou upload classique (pièces jointes « document » parfois vides) ; renommer les homonymes.
- Handoff + vélocité en fin de session, prompt prêt-à-coller pour la suivante.

---

# 7. Backlog maître (statuts au 21 juillet 2026)

> ✅ livré/prouvé · 🔄 en cours ou partiellement prouvé · 🔲 à faire · ⚠️ = bloquant pour le chantier indiqué. Le flux « Telama seul » vit en §3.3.

## 7.1 Chemin principal (jalon 1)

| Statut | Item | Note |
|---|---|---|
| ✅ | B1-PR1 squelette (#70) · PR2 modèle+machine (#71) · PR3 lecture+contrats (#72\*) · PR4 relay (#73+#74) | 218 tests, chaîne prouvée jusqu'à Redpanda |
| 🔲 | **⭐ PR4bis notification-service :6004** — premier consumer (A16) | Session dédiée, prompt prêt (handoff PR4 §7) |
| 🔲 | PR5 front — listes réelles (Mes envois, deals Mes trajets) consommant le seed A14 (`seed-output.json`) | Ferme B1 |
| 🔲 | B2 argent entrant — writers outbox EN TRANSACTION, PaymentIntent, accept/decline, cron 24 h, payment-service :6008, media-service :6009, champs différés (`cancelReason`, `pickupRefusalReason`, `deliveryCodeEncrypted` A13), **commissionRate 0.12 + plancher 300 (A11/D16)**, emails transactionnels Stripe (#2, #7, #14 de la matrice A15) | ⚠️ prérequis §7.2 ligne 1 |
| ✅ | **B3 transport** — PR1 serveur ✅ **#95** (D42/D43, A38–A42) : `POST /deals/:id/pickup` · `/pickup/refuse` · `/events` · `/code/regenerate` · `/deliver`, code bcrypt + AES révélé dans la vue Shipper, 4 emails B3, seed avec vrais codes · PR2 front ✅ **#96** (A43 : bascule des 4 mocks Voyageur + régénération Expéditeur, upload ImageKit) · PR3 boîte du Voyageur (A44 : `GET /me/deals`, bande « À traiter », deals sous chaque trajet, notifications cliquables, badge mobile) ✅ **#98** · PR4 page demande (A45 : photos déclarées enfin envoyées, colonne d'action dès md, GAR-02, RGP-02, Voir profil) ✅ **#100** — **B3 SOLDÉ** le 02/09 |
| 🔲 | B4 argent sortant · B5 confiance (rating, relances, stats D29①) | Séquence gravée §3.1 |

## 7.2 Dettes techniques (aucune ne doit devenir invisible)

| Statut | Item | Note |
|---|---|---|
| ⬜ | **B4 — 12 gabarits EJS `booking.*` à migrer vers le dictionnaire D44** (`settlement-emails.ts` est le modèle) ; webhook Stripe `transfer.*` à brancher (D40 étendu) ; retenue ANN-01 au prorata (D50, `feat/b4-late-cancel-payout`) | Deux mécanismes d'email cohabitent dans le notification-service |
| 🔄 | **⚠️ B2 — Transactions multi-documents Atlas : REPORTÉ DEUX FOIS.** Preuve exigée : `session.startTransaction()` en mongosh (le seed ne prouve que les writes) | Prérequis ABSOLU des writers B2 |
| 🔄 | Idempotence seed-deals au 2e run (wipe attendu : 20 bookings, 7 trips) | Reporté, 5 min |
| 🔲 | Redaction pino-http : `req.headers.cookie` + `req.headers.authorization` (option `redact`) | Sécurité, micro-PR |
| 🔲 | seed-deals : booking …ef45 a `shipperId === carrierId` (auto-expédition) — bug de seed si la règle métier l'interdit (à trancher) | Curiosité gravée PR4 |
| 🔲 | `chore/deps` : **43 vulnérabilités npm** (dont 6 critiques) — PR dédiée, JAMAIS `npm audit fix --force` en pleine PR · Prisma 6→7 idem | Chantier hygiène |
| 🔄 | `git config user.email` (`egoiomab.com`, @ manquant) | 10 secondes |
| 🔲 | Required checks : confirmer TypeScript (deal-service) dans Settings → Branches ; confirmer le numéro de la PR3 (#72 ?) dans Pull requests → Closed | Cf. §0bis |
| 🔲 | PR `fix/error-semantics` trip-service (400-partout → 404/401/403, impact front) — le deal-service est déjà propre (A21) | Porté au registre ✓ |
| 🔲 | Cleanup post-pricing : `maxSlots`/`bookedSlots` dépréciés, `WITH_INTERMEDIATE_STOPS`, types `handoffMoments`/`pickupMoments`, harmonisation `dark:bg-slate-950`→`900` | PR mécanique |
| 🔲 | Micro-PR `getImageKit()` paresseux (bloque `nx serve trip-service` sans env) · `AddDocumentsBody` en Zod dédié · harmonisation noms projets Nx · audit anglais OAS trip | — |

## 7.3 Front user-ui (haute priorité, hors B-chantiers)

| Statut | Item |
|---|---|
| 🔲 | `<Toaster />` (Sonner) au layout racine · `OnboardingBanner` après Header · cron onboarding-reminder démarré (branchement main.ts auth + `node-cron` à installer) · page carrier settings (Stripe) · test du flow complet |
| 🔲 | Priorité moyenne : page profil carrier public · formulaire création trajet (refonte pricing D13-D16, 3 PRs estimées) |

## 7.4 Auth & sessions (D27, solde — jalon 2)

🔲 SES-03 sudo mode · SES-04 modal front expiration · SES-05 liste des sessions (record prêt) · PR cleanup sessions legacy (≤30 j post-prod) · conversion OpenAPI auth-service (contrats Zod, swagger retiré) + `/docs` Scalar · page `/docs` d'index gateway (basse)

## 7.5 En continu (repris §3.2)

🔲 PRs i18n restantes (critère de fin : `feat/locale-es`, puis PT — jalon 3) · micro-PRs confiance (wording D28, Signaler, CTA alertes, page destinataire — jalon 2) · viewsCount Redis (D5) · PostHog + Sentry + vérif backups Atlas (jalon 2, constitutifs du lancement)
